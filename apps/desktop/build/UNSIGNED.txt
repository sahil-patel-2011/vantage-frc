This Vantage installer is unsigned.

Windows SmartScreen and macOS Gatekeeper will warn until an Authenticode
certificate (Windows) and Apple Developer ID + notarization (macOS) are
configured. The owner has to buy those; the build does not pretend they exist.

The window loads the hosted Vantage web app. It is not a second backend.
