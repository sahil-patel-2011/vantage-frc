"use client";
import { useEffect,useState } from "react";
export default function DataConnectorClient({orgId}:{orgId?:string}){const[credentials,setCredentials]=useState<Array<{id:string;opaqueKeyId:string;status:string;lastTestedAt:string|null;disabledAt:string|null}>>([]),[health,setHealth]=useState<Record<string,unknown>|null>(null),[key,setKey]=useState(""),[message,setMessage]=useState("");
 async function load(){const r=await fetch(`/api/admin/data-connectors${orgId?`?orgId=${orgId}`:""}`),d=await r.json();if(r.ok){setCredentials(d.credentials??[]);setHealth(d.health??null);}else setMessage(d.error);}useEffect(()=>{void load();},[orgId]);
 async function act(action:"save"|"test"|"disable",credentialId?:string){const r=await fetch("/api/admin/data-connectors",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({orgId,action,credentialId,apiKey:action==="save"?key:undefined})}),d=await r.json();setMessage(r.ok?action==="save"?"Encrypted TBA credential saved. Test it before relying on it.":`Credential ${action} completed.`:d.error);if(r.ok){setKey("");await load();}}
 return<main className="intel-app"><header className="intel-header"><div><span className="eyebrow">{orgId?"TEAM SETTINGS / FALLBACK":"PLATFORM / LIVE DATA"}</span><h1>TBA live-data connector</h1></div><a href={orgId?`/team?orgId=${orgId}`:"/admin"}>← Settings</a></header>{message&&<p role="status" className="telemetry-status">{message}</p>}<section className="admin-grid"><form className="intel-panel" onSubmit={e=>{e.preventDefault();void act("save");}}><h2>{orgId?"Optional fallback key":"Platform-managed Read API key"}</h2><p>{orgId?"Vantage normally uses one shared platform key. Add a team key only when platform health reports a sustained issue; it remains a controlled scheduler fallback and does not create duplicate polling.":"One encrypted key powers the global ingest worker for every organization. The key is write-only after save."}</p><label>TBA Read API v3 key<input type="password" autoComplete="off" required value={key} onChange={e=>setKey(e.target.value)} placeholder="Encrypted on save"/></label><button className="primary-action">Encrypt and save</button></form><article className="intel-panel"><h2>Official setup steps</h2><ol><li>Open the official TBA account page.</li><li>Sign in.</li><li>Create a Read API v3 key with a descriptive name such as “Vantage team fallback”.</li><li>Paste it in the encrypted field, save, then test.</li></ol><a href="https://www.thebluealliance.com/account" target="_blank" rel="noreferrer">Open official TBA account page →</a><p>Any credential pasted into chat or another public channel is exposed and must be rotated; never reuse it here.</p></article></section><section className="compare-panel"><h2>Credential status</h2>{credentials.length===0?<p>No TBA key is configured.</p>:credentials.map(item=><article className="saved-board" key={item.id}><div><strong>{item.opaqueKeyId}</strong><small>{item.status} · {item.lastTestedAt?`tested ${new Date(item.lastTestedAt).toLocaleString()}`:"never tested"}</small></div><button onClick={()=>void act("test",item.id)}>Test</button><button onClick={()=>void act("disable",item.id)}>Disable</button></article>)}<h2>Ingestion health</h2><pre className="health-summary">{health?JSON.stringify(health,null,2):"No health data yet. A key is not assumed to exist until configured."}</pre></section></main>;}
