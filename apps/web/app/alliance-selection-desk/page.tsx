import AllianceSelectionDeskClient from "./alliance-selection-desk-client";

export const metadata = {
  title: "Alliance desk",
};

export default function AllianceSelectionDeskPage() {
  return <AllianceSelectionDeskClient />;
}
