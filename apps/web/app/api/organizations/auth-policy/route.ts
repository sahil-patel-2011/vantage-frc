import { auth,validateOrgAuthPolicy,type OrgAuthPolicy } from "@vantage/core";
import { withRls } from "@vantage/db";
import { headers } from "next/headers";
async function session(){const value=await auth.api.getSession({headers:await headers()});if(!value)throw new Error("Authentication required");return value;}
export async function GET(request:Request){try{const user=await session();const orgId=new URL(request.url).searchParams.get("orgId");if(!orgId)throw new Error("orgId is required");const policy=await withRls({userId:user.user.id,orgId},async(client)=>(await client.query(`SELECT allow_password AS "allowPassword",allow_google AS "allowGoogle",allow_email_otp AS "allowEmailOtp",mfa_policy AS "mfaPolicy",remembered_device_days AS "rememberedDeviceDays" FROM org_auth_policies WHERE org_id=$1`,[orgId])).rows[0]);return Response.json({policy});}catch(error){return Response.json({error:error instanceof Error?error.message:"Policy unavailable"},{status:400});}}
export async function POST(request:Request){try{const user=await session();const body=await request.json() as OrgAuthPolicy&{orgId:string};validateOrgAuthPolicy(body);await withRls({userId:user.user.id,orgId:body.orgId},async(client)=>{const role=await client.query(`SELECT 1 FROM memberships WHERE org_id=$1 AND user_id=$2 AND role IN ('owner','admin')`,[body.orgId,user.user.id]);if(!role.rowCount)throw new Error("Organization administrator access required");await client.query(`INSERT INTO org_auth_policies(org_id,allow_password,allow_google,allow_email_otp,mfa_policy,remembered_device_days,updated_by) VALUES($1,$2,$3,$4,$5,$6,$7) ON CONFLICT(org_id) DO UPDATE SET allow_password=excluded.allow_password,allow_google=excluded.allow_google,allow_email_otp=excluded.allow_email_otp,mfa_policy=excluded.mfa_policy,remembered_device_days=excluded.remembered_device_days,updated_by=excluded.updated_by,updated_at=now()`,[body.orgId,body.allowPassword,body.allowGoogle,body.allowEmailOtp,body.mfaPolicy,body.rememberedDeviceDays,user.user.id]);await client.query(`INSERT INTO auth_policy_audit_events(org_id,actor_user_id,action,metadata) VALUES($1,$2,'org.auth_policy.updated',$3::jsonb)`,[body.orgId,user.user.id,JSON.stringify({allowPassword:body.allowPassword,allowGoogle:body.allowGoogle,allowEmailOtp:body.allowEmailOtp,mfaPolicy:body.mfaPolicy,rememberedDeviceDays:body.rememberedDeviceDays})]);});return Response.json({success:true});}catch(error){return Response.json({error:error instanceof Error?error.message:"Policy update failed"},{status:400});}}
