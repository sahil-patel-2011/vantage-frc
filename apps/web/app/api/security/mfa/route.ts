import { auth,beginMfaEnrollment,confirmMfaEnrollment,regenerateRecoveryCodes,revokeMfa,verifyMfaStepUp } from "@vantage/core";
import { withRls } from "@vantage/db";
import { headers } from "next/headers";
async function current(){const value=await auth.api.getSession({headers:await headers()});if(!value)throw new Error("Authentication required");return value;}
export async function GET(){try{const session=await current();const result=await withRls({userId:session.user.id},async(client)=>({enrollment:(await client.query(`SELECT confirmed_at AS "confirmedAt",updated_at AS "updatedAt" FROM user_mfa_enrollments WHERE user_id=$1`,[session.user.id])).rows[0]??null,devices:(await client.query(`SELECT id,label,expires_at AS "expiresAt",last_used_at AS "lastUsedAt" FROM remembered_mfa_devices WHERE user_id=$1 AND revoked_at IS NULL AND expires_at>now() ORDER BY created_at DESC`,[session.user.id])).rows}));return Response.json(result);}catch(error){return Response.json({error:error instanceof Error?error.message:"Security settings unavailable"},{status:400});}}
export async function POST(request:Request){try{const session=await current();const body=await request.json() as {action:string;code?:string;orgId?:string;rememberDays?:number};const result=await withRls({userId:session.user.id,orgId:body.orgId},async(client)=>{let output:unknown;if(body.action==="begin")output=await beginMfaEnrollment(client,{id:session.user.id,email:session.user.email});else if(body.action==="confirm")output={recoveryCodes:await confirmMfaEnrollment(client,session.user.id,String(body.code??""))};else if(body.action==="regenerate")output={recoveryCodes:await regenerateRecoveryCodes(client,session.user.id,String(body.code??""))};else if(body.action==="step-up"){if(!body.orgId)throw new Error("orgId is required");output=await verifyMfaStepUp(client,{userId:session.user.id,orgId:body.orgId,sessionId:session.session.id,code:String(body.code??""),rememberDays:body.rememberDays});}else throw new Error("Invalid MFA action");await client.query(`INSERT INTO auth_policy_audit_events(org_id,actor_user_id,action,metadata) VALUES($1,$2,$3,'{}')`,[body.orgId??null,session.user.id,`mfa.${body.action}`]);return output;});const response=Response.json(result);if(result&&typeof result==="object"&&"rememberToken" in result)response.headers.append("set-cookie",`vantage_mfa_device=${(result as {rememberToken:string}).rememberToken}; Path=/; HttpOnly; SameSite=Lax; Secure; Max-Age=${Math.max(0,Number(body.rememberDays??0))*86400}`);return response;}catch(error){return Response.json({error:error instanceof Error?error.message:"MFA action failed"},{status:400});}}
export async function DELETE(request:Request){try{const session=await current();const body=await request.json() as {deviceId?:string;revokeAll?:boolean};await withRls({userId:session.user.id},async(client)=>{if(body.revokeAll)await revokeMfa(client,session.user.id);else if(body.deviceId)await client.query(`UPDATE remembered_mfa_devices SET revoked_at=now() WHERE id=$1 AND user_id=$2`,[body.deviceId,session.user.id]);else throw new Error("Select a device or revoke MFA");await client.query(`INSERT INTO auth_policy_audit_events(actor_user_id,action,metadata) VALUES($1,$2,'{}')`,[session.user.id,body.revokeAll?"mfa.revoked":"mfa.device_revoked"]);});return Response.json({success:true});}catch(error){return Response.json({error:error instanceof Error?error.message:"MFA revocation failed"},{status:400});}}
