import BinShelfLocatorClient from "./bin-shelf-locator-client";

export const metadata = {
  title: "Bin/Shelf Locator",
};

export default function BinShelfLocatorPage() {
  return <BinShelfLocatorClient />;
}
