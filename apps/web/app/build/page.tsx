import BuildHub from "./build-hub";

export const metadata = {
  title: "Build",
  description: "Kickoff, CAD, code coach, FMEA, prototypes, and batteries in one build hub.",
};

export default function BuildPage() {
  return <BuildHub />;
}