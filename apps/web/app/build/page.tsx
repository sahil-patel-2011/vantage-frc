import BuildHub from "./build-hub";

export const metadata = {
  title: "Build · Vantage",
  description: "Kickoff, CAD, code coach, FMEA, and batteries in one build hub.",
};

export default function BuildPage() {
  return <BuildHub />;
}