"use client";
import { useEffect,useState } from "react";
import { VantageLogo } from "../../components/brand";
type Job={id:string;title:string;platform:string;executionMode:string;status:string;brief:Record<string,unknown>;briefConfirmedAt:string|null;updatedAt:string};type Step={id:string;sequence:number;operation:string;parameters:Record<string,unknown>;status:string;approvalStatus:string;progress:number};
export default function CadWorkspace({orgId}:{orgId:string}){const[jobs,setJobs]=useState<Job[]>([]),[selected,setSelected]=useState(""),[detail,setDetail]=useState<{steps:Step[];artifacts:Array<{id:string;type:string;title:string;content:Record<string,unknown>}>;checkpoints:Array<{id:string;topology:Record<string,unknown>;humanEditDetected:boolean}>}|null>(null),[title,setTitle]=useState("Strategy-linked mechanism"),[request,setRequest]=useState("Create a serviceable mechanism concept for the selected scoring task."),[teamKey,setTeamKey]=useState(""),[platform,setPlatform]=useState<"mock"|"onshape"|"fusion360">("mock"),[message,setMessage]=useState("");
 async function load(jobId=selected){const r=await fetch(`/api/cad?orgId=${orgId}${jobId?`&jobId=${jobId}`:""}`),d=await r.json();if(r.ok){setJobs(d.jobs??[]);setDetail(d.detail);}else setMessage(d.error);}useEffect(()=>{void load();const timer=setInterval(()=>void load(),5000);return()=>clearInterval(timer);},[orgId,selected]);
 async function act(action:string,extra:Record<string,unknown>={}){const r=await fetch("/api/cad",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({orgId,action,...extra})}),d=await r.json();setMessage(r.ok?action==="brief"?"Engineering brief created. Confirm assumptions before geometry.":"CAD workspace updated.":d.error);if(r.ok){if(d.jobId)setSelected(d.jobId);await load(d.jobId??selected);}return d;}
 const job=jobs.find(item=>item.id===selected),render=detail?.artifacts.find(item=>item.type==="render_checkpoint");
 async function createPlan(){if(!job)return;await act("plan",{jobId:job.id,actions:[{operation:"create_sketch",parameters:{plane:"Top",profile:"confirmed envelope"},requiresApproval:true,reason:"Create confirmed base profile"},{operation:"create_extrude",parameters:{depth:"confirmed by user"},requiresApproval:true,reason:"Create initial solid"},{operation:"verify_topology",parameters:{views:["iso","top","front"]},requiresApproval:true,reason:"Verify topology and rendered views"}]});}
 return<main className="cad-workspace"><header className="workspace-top"><VantageLogo href="/dashboard"/><strong>CAD BUILDER</strong><a href={`/workspace?orgId=${orgId}`}>Workspace</a><a href={`/cad/connections?orgId=${orgId}`}>Connections</a></header>{message&&<p className="telemetry-status" role="status">{message}</p>}<div className="cad-shell"><aside className="cad-jobs"><span className="eyebrow">ENGINEERING THREADS</span>{jobs.map(item=><button className={selected===item.id?"active":""} onClick={()=>setSelected(item.id)} key={item.id}><strong>{item.title}</strong><small>{item.platform} · {item.status}</small></button>)}</aside><section className="cad-conversation"><span className="eyebrow">STRATEGY → ENGINEERING BRIEF</span><h1>{job?.title??"Start with intent, not geometry"}</h1>{!job?<form onSubmit={e=>{e.preventDefault();void act("brief",{title,request,teamKey,platform,executionMode:platform==="fusion360"?"local":"hosted"});}}><label>Concept title<input value={title} onChange={e=>setTitle(e.target.value)} required/></label><label>Selected team (optional)<input value={teamKey} onChange={e=>setTeamKey(e.target.value)} placeholder="frc254"/></label><label>CAD path<select value={platform} onChange={e=>setPlatform(e.target.value as typeof platform)}><option value="mock">Deterministic mock</option><option value="onshape">Onshape hosted OAuth</option><option value="fusion360">Fusion 360 local relay</option></select></label><label>What should this mechanism accomplish?<textarea rows={7} value={request} onChange={e=>setRequest(e.target.value)}/></label><button className="primary-action">Build cited engineering brief</button></form>:<BriefEditor job={job} onConfirm={brief=>act("confirm",{jobId:job.id,brief})} onPlan={createPlan}/>}<p className="cad-disclaimer">Design review flags are suggestions, not engineering or safety certification.</p></section><section className="cad-visual"><div className="cad-render"><span>LIVE VERIFIED CHECKPOINT</span>{render?<iframe sandbox="" title="Isolated CAD checkpoint render" srcDoc={String(render.content.content??"")}/>:<div className="render-empty">No geometry mutation yet.<small>Confirm brief → preview plan → approve each step</small></div>}</div><div className="cad-timeline"><span className="eyebrow">JOB TIMELINE</span>{detail?.steps.map(step=><article key={step.id}><i className={step.status}/><div><strong>{step.sequence}. {step.operation.replaceAll("_"," ")}</strong><small>{step.status} · {step.progress}%</small></div>{step.approvalStatus==="pending"&&<><button onClick={()=>void act("approve",{jobId:job!.id,stepId:step.id,approved:true})}>Approve</button><button onClick={()=>void act("approve",{jobId:job!.id,stepId:step.id,approved:false})}>Reject</button></>}{step.approvalStatus==="approved"&&step.status==="planned"&&job?.platform==="mock"&&<button onClick={()=>void act("execute-mock",{jobId:job!.id,stepId:step.id})}>Run mock</button>}</article>)}{detail?.checkpoints.map(checkpoint=><article key={checkpoint.id}><i className="completed"/><div><strong>Topology verified</strong><small>{checkpoint.humanEditDetected?"Human edit detected · review before resume":JSON.stringify(checkpoint.topology)}</small></div></article>)}</div></section></div></main>;}
function BriefEditor({job,onConfirm,onPlan}:{job:Job;onConfirm:(brief:Record<string,unknown>)=>unknown;onPlan:()=>unknown}){const[brief,setBrief]=useState(job.brief);useEffect(()=>setBrief(job.brief),[job.id,job.brief]);const assumptions=(brief.assumptions??[]) as Array<{name:string;value:string;needsConfirmation:boolean}>;return<section className="brief-editor"><label>Summary<textarea rows={4} value={String(brief.summary??"")} onChange={e=>setBrief({...brief,summary:e.target.value})}/></label><h2>Assumptions to confirm</h2>{assumptions.map((item,index)=><label key={item.name}>{item.name}<input value={item.value} onChange={e=>setBrief({...brief,assumptions:assumptions.map((entry,i)=>i===index?{...entry,value:e.target.value,needsConfirmation:false}:entry)})}/></label>)}<details open><summary>Requirements, constraints, risk, and acceptance</summary><pre>{JSON.stringify({requirements:brief.requirements,constraints:brief.constraints,scoringTasks:brief.scoringTasks,risks:brief.risks,acceptanceCriteria:brief.acceptanceCriteria,sourceRefs:brief.sourceRefs},null,2)}</pre></details>{!job.briefConfirmedAt?<button className="primary-action" onClick={()=>onConfirm(brief)}>Confirm engineering brief</button>:<button className="primary-action" onClick={onPlan}>Preview safe action plan</button>}</section>;}
