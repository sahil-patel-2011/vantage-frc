import DegradedModeClient from "./degraded-mode-client";

export const metadata = {
  title: "Data-source degraded mode",
};

export default function DegradedModePage() {
  return <DegradedModeClient />;
}
