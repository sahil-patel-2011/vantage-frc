import EventReadinessClient from "./event-readiness-client";

export const metadata = {
  title: "Event readiness",
};

export default function EventReadinessPage() {
  return <EventReadinessClient />;
}
