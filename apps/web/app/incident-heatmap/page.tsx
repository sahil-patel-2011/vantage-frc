import IncidentHeatmapClient from "./incident-heatmap-client";

export const metadata = {
  title: "Incidents",
};

export default function IncidentHeatmapPage() {
  return <IncidentHeatmapClient />;
}
