"use client";
import { useEffect, useState } from "react";
import { useSearchParams } from "next/navigation";
import { VantageLogo } from "../../components/brand";
import { LegalAgreementCheckbox } from "../../components/legal-agreement-checkbox";
export const PENDING_INVITE_KEY="vantage.pendingInviteToken"; type InvitePreview={orgName:string;teamNumber:number;role:string;email:string;status:string;expiresAt:string};
export default function InviteClient(){const params=useSearchParams();const token=params.get("token")?.trim()??"";const [preview,setPreview]=useState<InvitePreview|null|undefined>(undefined);const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);const [termsAccepted,setTermsAccepted]=useState(false);useEffect(()=>{if(token)try{sessionStorage.setItem(PENDING_INVITE_KEY,token)}catch{}},[token]);useEffect(()=>{if(!token){setPreview(null);return}let active=true;void fetch(`/api/invites/preview?token=${encodeURIComponent(token)}`).then(async response=>{const data=await response.json();if(!active)return;if(!response.ok){setMessage(data.error??"Could not load invitation details.");setPreview(null);return}setPreview(data.preview??null)}).catch(()=>{if(active)setMessage("Could not load invitation details.")});return()=>{active=false}},[token]);async function accept(){if(!termsAccepted){setMessage("Please agree to the Terms of Service and Privacy Policy.");return}const inviteToken=token||sessionStorage.getItem(PENDING_INVITE_KEY)||"";if(!inviteToken){setMessage("This invitation link is incomplete.");return}setBusy(true);try{const response=await fetch("/api/invites/accept",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token:inviteToken,termsAccepted})});const data=await response.json() as {orgId?:string;error?:string};if(!response.ok){setMessage(data.error??"Could not accept invitation.");return}try{sessionStorage.removeItem(PENDING_INVITE_KEY)}catch{}window.location.assign(data.orgId?`/workspace?orgId=${encodeURIComponent(data.orgId)}`:"/dashboard")}finally{setBusy(false)}}return <main className="onboarding-page"><section className="onboarding-card" aria-labelledby="invite-title"><div className="onboarding-brand"><VantageLogo/></div><span className="eyebrow">Team invitation</span><h1 id="invite-title">Accept your invitation</h1><p className="onboarding-sub">Sign in with the verified email that received this invite before accepting.</p>{preview?<div className="onboarding-callout invite"><strong>{preview.orgName} · Team {preview.teamNumber}</strong><p>Role: {preview.role} · Sent to {preview.email} · Status {preview.status}</p></div>:null}<LegalAgreementCheckbox id="invite-terms" checked={termsAccepted} onChange={setTermsAccepted} className="invite-legal"/><button className="signin-submit" disabled={busy||!token||!termsAccepted} onClick={()=>void accept()}>{busy?"Accepting…":"Accept invitation"}</button>{message?<p className="signin-status" role="status">{message}</p>:null}</section></main>}