import PrototypeTrackerClient from "./prototype-tracker-client";

export const metadata = {
  title: "Prototype-to-Decision Tracker",
};

export default function PrototypeTrackerPage() {
  return <PrototypeTrackerClient />;
}
