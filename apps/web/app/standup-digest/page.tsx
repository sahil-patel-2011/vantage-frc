import StandupDigestClient from "./standup-digest-client";

export const metadata = {
  title: "Morning Standup Digest",
};

export default function StandupDigestPage() {
  return <StandupDigestClient />;
}
