"use client";

import type { BusinessRelatedId } from "../lib/business/business-related";

/** Retired: Business tools live in hub tabs and search. */
export function BusinessRelated(_props: {
  orgId?: string | null;
  active?: BusinessRelatedId;
  include?: BusinessRelatedId[];
  className?: string;
  ariaLabel?: string;
}) {
  return null;
}
