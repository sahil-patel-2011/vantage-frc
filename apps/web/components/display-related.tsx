"use client";

import type { DisplayRelatedId } from "../lib/display/display-related";

/** Retired: Display tools live in hub tabs and search. */
export function DisplayRelated(_props: {
  orgId?: string | null;
  active?: DisplayRelatedId;
  include?: DisplayRelatedId[];
  className?: string;
  ariaLabel?: string;
}) {
  return null;
}
