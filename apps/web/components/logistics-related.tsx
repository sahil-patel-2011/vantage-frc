"use client";

import type { LogisticsRelatedId } from "../lib/logistics/logistics-related";

/** Retired: Logistics tools live in hub tabs and search. */
export function LogisticsRelated(_props: {
  orgId?: string | null;
  active?: LogisticsRelatedId;
  include?: LogisticsRelatedId[];
  className?: string;
  ariaLabel?: string;
}) {
  return null;
}
