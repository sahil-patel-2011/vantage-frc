"use client";
import { type FormEvent, useState } from "react";
import { LegalAgreementCheckbox } from "../legal-agreement-checkbox";
import { track } from "../../lib/marketing/analytics";
export function WaitlistForm({ idPrefix = "waitlist", compact = false }: { idPrefix?: string; compact?: boolean }) { const [state,setState]=useState<"idle"|"sending"|"success"|"error">("idle"); const [message,setMessage]=useState(""); const [termsAccepted,setTermsAccepted]=useState(false); async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();if(!termsAccepted){setState("error");setMessage("Please agree to the Terms of Service and Privacy Policy.");return;}setState("sending");const form=new FormData(event.currentTarget);try{const response=await fetch("/api/waitlist",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:form.get("email"),teamNumber:form.get("teamNumber"),phone:form.get("phone"),smsConsent:form.get("smsConsent")==="on",termsAccepted,website:form.get("website")})});const result=await response.json() as {ok:boolean;message?:string};if(!response.ok||!result.ok)throw new Error(result.message??"Submission failed");setState("success");track("waitlist_success");}catch(error){setState("error");setMessage(error instanceof Error?error.message:"Please try again.");}} if(state==="success")return <div className="confirmation" role="status"><span className="signal">SIGNAL RECEIVED</span><h3>You&apos;re on the list.</h3><p>We&apos;ll email launch news. Joining does not create a Vantage account or team organization.</p></div>;return <form className={`waitlist-form ${compact?"compact-form":""}`} onSubmit={submit} aria-label="Join the Vantage waitlist"><div className="field"><label htmlFor={`${idPrefix}-email`}>Email</label><input id={`${idPrefix}-email`} name="email" type="email" autoComplete="email" required /></div><div className="field"><label htmlFor={`${idPrefix}-teamNumber`}>FRC team number</label><input id={`${idPrefix}-teamNumber`} name="teamNumber" type="number" min="1" max="99999" inputMode="numeric" required /></div><div className="field"><label htmlFor={`${idPrefix}-phone`}>Phone <span>optional</span></label><input id={`${idPrefix}-phone`} name="phone" type="tel" autoComplete="tel" placeholder="+12025550123" /></div><div className="honeypot" aria-hidden="true"><label htmlFor={`${idPrefix}-website`}>Website</label><input id={`${idPrefix}-website`} name="website" tabIndex={-1} autoComplete="off" /></div><label className="consent"><input name="smsConsent" type="checkbox" /><span>I agree to receive occasional Vantage launch texts at the number provided. Consent is not a condition of access.</span></label><LegalAgreementCheckbox id={`${idPrefix}-terms`} checked={termsAccepted} onChange={setTermsAccepted}/><p className="form-note">Email is used for waitlist and launch updates. Read our <a href="/privacy">Privacy Policy</a>.</p>{state==="error"&&<p className="form-error" role="alert">{message}</p>}<button className="button primary" type="submit" disabled={state==="sending"||!termsAccepted}>{state==="sending"?"Transmitting…":"Join the waitlist"}</button></form>; }