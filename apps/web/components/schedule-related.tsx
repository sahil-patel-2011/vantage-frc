"use client";

import type { ScheduleRelatedId } from "../lib/schedule/schedule-related";

/** Retired: Schedule tools live in hub tabs and search. */
export function ScheduleRelated(_props: {
  orgId?: string | null;
  active?: ScheduleRelatedId;
  include?: ScheduleRelatedId[];
  className?: string;
  ariaLabel?: string;
}) {
  return null;
}
