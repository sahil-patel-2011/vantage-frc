"use client";

import type { VisitRelatedId } from "../lib/visit-invites/visit-related";

/** Retired: Visit tools live in hub tabs and search. */
export function VisitRelated(_props: {
  orgId?: string | null;
  active?: VisitRelatedId;
  include?: VisitRelatedId[];
  className?: string;
  ariaLabel?: string;
}) {
  return null;
}
