import { readFileSync } from "node:fs";
import { join } from "node:path";
import { describe, expect, it } from "vitest";

const ROOT = join(__dirname, "..", "..", "..", "..");

describe("leftover Feature map Collaborative pick list needs setup student copy", () => {
  it("uses student chrome", () => {
    const src = readFileSync(join(ROOT, "docs/FEATURE_MAP.md"), "utf8");
    expect(src).toContain("Weighted votes + FAST-style rating roles from cached `team_event_metrics` + CSV export (Pick List Maker share-out) Setup badge is **Needs setup**.");
  });
});
