# Vantage status

*Working log of what is done, what is in progress, and what only the owner can unblock. Last updated 2026-09-12.*

Living record of the master engineering brief. Update this file at the end of every task. Numbers are from commands that were actually run, not memory.

**Branch:** `cursor/playwright-survive-c0b5` (from `origin/main` after Chat remaining `21af5ded`).
**Date opened:** 2026-09-11.
**This revision:** Leftover opening-leaves gold after leftover-opening-more. EmptyState boards say **Opening Playbook**, **Opening Learning**, **Opening Work**, **Opening Get unstuck** (was Loading…). leftover-product-chrome **Playbook**, leftover-schema-oauth **Learning**, leftover-opening-more **Opening Match sim**, leftover-fmea **Failure log**, leftover-pick-before **Choose your team** stay. Hub **My Day** / **Schema A/B** stay. Source lock `leftover-opening-leaves.test.ts`. Targeted vitest leftover-opening-leaves coordinated family **16 files / 27 passed** (2.35s). `npm run typecheck --workspace=@vantage/web` clean. Fixture Playwright leftover-opening-leaves **1/1** on `:3856` (6.7s). leftover-opening-more **1/1** on `:3855` (7.0s). leftover-loading-more **1/1** on `:3854` (7.3s). leftover-sponsor-more **1/1** on `:3853` (6.9s). leftover-related-more **1/1** on `:3852` (5.8s). leftover-fmea-hub **1/1** on `:3851` (6.3s). leftover-strips-more **1/1** on `:3850` (5.9s). leftover-cards-more **1/1** on `:3849` (5.8s). leftover-team-data-more **1/1** on `:3848` (4.6s). leftover-home-more **1/1** on `:3847` (4.6s). leftover-delta **1/1** on `:3846` (5.5s). leftover-video-more **1/1** on `:3845` (5.6s). leftover-match-more **1/1** on `:3844` (5.4s). leftover-tool-more **1/1** on `:3843` (5.5s). leftover-build-more **1/1** on `:3842` (5.6s). leftover-invites **1/1** on `:3841` (4.6s). leftover-visit-invites **1/1** on `:3840` (5.4s). leftover-community-impact **1/1** on `:3839` (5.5s). leftover-event-day-more **1/1** on `:3838` (5.4s). leftover-ops-more **1/1** on `:3837` (6.0s). leftover-business-more **1/1** on `:3836` (5.3s). leftover-scout-more **1/1** on `:3835` (4.6s). leftover-safety **1/1** on `:3834` (5.4s). leftover-media **1/1** on `:3833` (4.9s). leftover-cad-map **1/1** on `:3832` (5.6s). leftover-goals-kit **1/1** on `:3831` (5.6s). leftover-kit-titles **1/1** on `:3830` (5.7s). leftover-ops-titles **1/1** on `:3829` (5.6s). leftover-cad-titles **1/1** on `:3828` (5.5s). leftover-build-titles **1/1** on `:3827` (5.6s). leftover-people-more **1/1** on `:3826` (5.6s). leftover-business-titles **1/1** on `:3825` (5.7s). leftover-people-titles **1/1** on `:3824` (5.8s). leftover-event-day-titles **1/1** on `:3823` (6.0s). leftover-scout-titles **1/1** on `:3822` (5.0s). leftover-strategy-titles **1/1** on `:3821` (5.5s). leftover-hub-strips **1/1** on `:3820` (5.4s). leftover-hub-mismatch **1/1** on `:3819` (6.1s). leftover-hub-titles **1/1** on `:3818` (5.9s). leftover-judge-sim **1/1** on `:3817` (6.1s). leftover-pick-which **1/1** on `:3816` (6.2s). leftover-knowledge-gap **1/1** on `:3815` (6.5s). leftover-autopilot **1/1** on `:3814` (6.0s). leftover-code-coach **1/1** on `:3813` (5.0s). leftover-copilot **1/1** on `:3812` (6.2s). leftover-email-otp **1/1** on `:3811` (5.1s). leftover-pick-before **1/1** on `:3810` (5.1s). Local Postgres still down — RLS not re-proven this image. Earlier gates stay: `npm test` **1224 / 9102** (76.29s), eslint `--max-warnings=0` clean, typecheck all workspaces clean, production web build **695 pages**, **0 ⚠**. leftover-export **1/1** on `:3809`. leftover-llms **3/3** on `:3808`. leftover-fmea-links **1/1** on `:3807`. leftover-lxi-risks **1/1** on `:3806`. leftover-fmea-boards **1/1** on `:3805`. leftover-fmea-strips **1/1** on `:3804`. leftover-fmea-board **1/1** on `:3803`. leftover-fmea-briefing **2/2** on `:3802`. leftover-cad-onshape **1/1** on `:3801`. Connect TBA / TBA Read API stay. Did not wrap freebuff. Did not deploy. No season ±3 claim — fixture MAE **89.7** / band ±90. Standing brief stays open. Master brief is **not** done.

## Decisions the owner should know

1. **Stale remotes were not merged.** Every `origin/{fn,ui,feat,build,pi-freebuff-layer,…}` branch has **no merge-base** with current `main` (unrelated histories after a rewrite). They are 641 commits behind. Unique subjects from those tips (Drive, assembly manual, email spine, budget/roles, desktop shell, visual system) are already on `main` by commit message. Merging them would import an alternate universe of the repo.
2. **`origin/pi-freebuff-layer` “reuse the live official Freebuff session” was dropped on purpose.** Freebuff Terms of Service (effective 2026-09-02) forbid calling their inference “through scripts, custom clients, wrappers, integrations, or third-party software” and require a human to initiate each session. A browser extension that rides a logged-in Freebuff tab is also forbidden. Compliant path: a team pastes **their own relay endpoint + token** (KMS-encrypted), then the Pi uses the official SDK / BYO keys — never Freebuff’s free servers by proxy. Written in `docs/FREEBUFF.md`.
3. **Connectors (Task 10) and copy sweep (Task 11) already landed on `main`** (`379aeada` / `6ec683a4` and follow-ups through `1a5a9048`). This brief still audits them, extends the catalog (free-relay pairing), and keeps `copy-lint.test.ts` green. It does not redo the 548-file merge.
4. **AI path order is relay → team keys → hosted.** Hosted paid keys are never the assumption. Freebuff’s free product is 18+; FRC users are often minors, so team chats must not send student prompts to Freebuff’s hosted free product. The Pi runs DeepSeek via a **self-hosted or officially-licensed** endpoint the owner configures.
5. **Preview Vercel builds are skipped before a build slot is claimed.** PR #9 showed Deployment **Error** (not Ignored) because Vercel failed at `Resource provisioning failed` — no build logs, ignoreCommand never ran. `git.deploymentEnabled` (`**` false, `main` true) skips previews without provisioning. `ignoreCommand` is now `node scripts/vercel-ignore-build.mjs` (exit 0 skip, exit 1 on `main`). Do not call `deploy_to_vercel` from this agent.

## Owner must do (cannot be done from this agent)

1. Set `CRON_SECRET` in the Vercel project env. Deploy preflight FAILs without it. Season piggybacks will not run until it is set.
2. Disable Vercel Preview Comments on the project (toolbar / `enablePreviewFeedback`). Until then this branch sets `supportsImmutableAssets: false` and `github.silent` so Git deploys can finish instead of dying on `IMMUTABLE_STATIC_PATCH_PREVIEW_COMMENTS`. Restore immutable assets after Preview Comments are off. Root Directory stays the repo root.
3. Paste **Postgres connection strings** for the paid Supabase project when ready. The free-plan **management token** is in gitignored `.env.local` only (not committed, not on Vercel). Product data stays on Neon until cutover. Desktop signing/notarization credentials still owner-must.
4. For localhost Google sign-in: paste `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` from Vercel into `apps/web/.env.local` (do not `vercel env pull` the production database URLs into this image). Google Cloud must also list `http://localhost:3001/api/auth/callback/google`. `next dev` no longer follows a production `BETTER_AUTH_URL`.

---

## Task 1 — Land in-flight work, baseline, competitive notes

### In-flight branches

| Remote | Ahead of main | Merge-base with main | Verdict |
|---|---|---|---|
| `worktree-agent-ad5d6b95912e775e6` | already merged (`6ec683a4`) | n/a | copy sweep — on main |
| connectors agent | already merged (`379aeada`) | n/a | `/connectors` — on main |
| `worktree-agent-aa639af77947124e2` | Drive / assembly-manual history | **none** | already on main as `b54e6a7f` / `482bfc48`; drop |
| `pi-freebuff-layer` | official Freebuff session reuse | **none** | **drop** — ToS forbids wrappers |
| `ui/*`, `fn/*`, `feat/assembly-manual`, `build/complete-product-pass`, `cad/unix-packaging`, `tba/neon-cache-sync`, `marketing-brand-redesign`, `cursor/simplify-team-hub`, `fix/vercel-client-bundle` | 37–590 ahead, 641 behind | **none** | drop; unique work already on main |

Assumption stated: “a new FRC student can do everything from one login without help” is better served by not replaying 500-commit unrelated histories than by a heroic rebase that would reintroduce DEMO copy, client-bundled `pg`, and ToS-violating Freebuff wrappers.

### Baseline (recorded after `npm ci`)

| Check | Result | Evidence |
|---|---|---|
| `npm ci` | 718 packages, 9s | this session, no lockfile change |
| `npm test` | **883 files passed, 2 skipped (885), 8,354 passed, 10 skipped, 53.88s** | `vitest run` this session after weather/offline tests |
| `npm run typecheck` | clean across workspaces that changed | this session |
| `npx eslint .` | clean (exit 0) | this session, 30.7s |
| `npm run build --workspace=@vantage/web` | compiled **23.9s**, TypeScript **47s**, **695 pages**, **zero warnings** in the log; `/connectors` `/team/relays` `/video-analysis` in the route table. First-load uncompressed JS: Home 834 KB, Scouting 838 KB, Strategy 762 KB, Pit 729 KB, `/workspace` 702 KB. Unique on-disk chunks: Home 143 KB, Scouting 148 KB, Strategy 73 KB, Pit 41 KB, `/workspace` 15 KB. | `next build` this session after the CSS token swap |
| `node scripts/deploy-preflight.mjs` | 8 FAIL in this image (no Vercel env). Owner must still set `CRON_SECRET` in the project. | this session |
| `npm audit --omit=dev` | **0 vulnerabilities** | this session |
| `scripts/rls-proof.mjs` | **40/40 passed** this image | local Postgres 16 `vantage_ci`, `vantage_ci_app` (session login, effective `vantage_app`, `rolsuper=f`, `rolbypassrls=f`). Drive / outreach / dossier / funding / relay / video / assembly-manual. CI job now runs `rls-proof-prepare-app-login.mjs` then `rls-proof.mjs` after `postgres-integration.test.ts`. Local `postgres-integration.test.ts` **2 passed / 2** (7.39s) on `vantage_ci_int`. |
| Latest migration | `0654_library_into_drive.sql` | next free number ≥ `0655` |

### Competitive notes

See `docs/COMPETITIVE_NOTES.md`. That document is the brief for Tasks 2–4.

### UI pass log (Task 3 running list)

| Route | What was wrong | Commit |
|---|---|---|
| Leftover opening-leaves titles | EmptyState boards said **Loading…**. Now **Opening Playbook** / **Opening Learning** / **Opening Work** / **Opening Get unstuck**. leftover-product-chrome **Playbook**, leftover-schema-oauth **Learning**, leftover-opening-more **Opening Match sim**, leftover-fmea **Failure log**, leftover-pick-before **Choose your team** stay. Hub **My Day** / **Schema A/B** stay. Source lock `leftover-opening-leaves.test.ts`. Targeted vitest leftover-opening-leaves coordinated family **16 files / 27 passed** (2.35s). Fixture Playwright leftover-opening-leaves **1/1** on `:3856` (6.7s). | this commit |
| Leftover opening-more titles | EmptyState boards said **Loading…**. Now **Opening CAD review queue** / **Opening Match sim** / **Opening Match checklist** / **Opening Decision critic**. leftover-cad-map **CAD review queue**, leftover-autopilot **Match sim**, leftover-epa-boards no **TBA**, leftover-setup-vantage no **Setup required**, leftover-loading-more **Opening Weigh-in**, leftover-fmea **Failure log**, leftover-pick-before **Choose your team** stay. Hub **My Day** / **Schema A/B** stay. Source lock `leftover-opening-more.test.ts`. Targeted vitest leftover-opening-more coordinated family **18 files / 26 passed** (1.25s). Fixture Playwright leftover-opening-more **1/1** on `:3855` (7.0s). | previous |
| Leftover loading-more titles | Weigh-in / Pair said **Loading Robot Weigh-In** / **Loading Pair VS Code**. Now **Opening Weigh-in** / **Opening Pair VS Code**. leftover-cad **Pair VS Code**, leftover-related-more **Open Spare kit**, leftover-copilot no **Inspection Copilot** / **Match Copilot**, leftover-code-coach **Code**, leftover-join-or-pick **Choose your team**, leftover-sponsor-more **Open Sponsors**, leftover-fmea **Failure log** stay. Hub **My Day** / **Schema A/B** stay. Source lock `leftover-loading-more.test.ts`. Targeted vitest leftover-loading-more coordinated family **14 files / 18 passed** (721ms). Fixture Playwright leftover-loading-more **1/1** on `:3854` (7.3s). | previous |
| Leftover sponsor-more titles | Related strips said **Open Sponsor CRM** / **Open Sponsor Suite** / **Open Sponsor Wall**. Now hub **Open Sponsors** / **Open Sponsor suite** / **Open Sponsor wall**. leftover-business-titles **Sponsor suite** / **Sponsor wall**, leftover-business-more **Renewal ROI** / **Open Impact**, leftover-people-more **Matching gifts**, leftover-community-impact **Impact**, leftover-hub-strips **Open Media kit**, leftover-related-more **Open Tool checkout**, leftover-fmea **Failure log**, leftover-pick-before **Choose your team** stay. Hub **My Day** / **Schema A/B** stay. Source lock `leftover-sponsor-more.test.ts`. Targeted vitest leftover-sponsor-more coordinated family **20 files / 22 passed** (1.16s). Extra sponsor related family **6 files / 38 passed** (898ms). Fixture Playwright leftover-sponsor-more **1/1** on `:3853` (6.9s). | previous |
| Leftover related-more titles | Related strips said **Open Tool Checkout** / **Open Opponent Watchlist** / **Open Spare Robot Kit** / **Open Pit Command** / **Open Alliance Chemistry** / **Open Rule Impact** / **Open Team Knowledge** / **Open Scouting Heat** / **Open Health Forecast**. Briefing page said **Pre-Match Briefing**. Now hub labels. leftover-fmea **Failure log**, leftover-fmea-hub no **Failure notes**, leftover-fmea-strips no **Open FMEA**, leftover-strips-more **Pick list** / **Shifts**, leftover-cards-more **Match cards**, leftover-kit-titles **Opening Equipment** / **Open Safety**, leftover-match-more **Counter-book**, leftover-pick-before **Choose your team**, leftover-epa-rating no **TBA**, leftover-event-day-more **Event day**, leftover-event-day-titles **Charge plan** / **Drive-team board**, leftover-related-tba no **TBA**, leftover-copilot no **Inspection Copilot** / **Match Copilot**, leftover-hub-strips **Open Repair triage** / **Open Spares forecast** stay. Hub **My Day** / **Schema A/B** stay. Source lock `leftover-related-more.test.ts`. Targeted vitest leftover-related-more coordinated family **20 files / 24 passed** (1.16s). Fixture Playwright leftover-related-more **1/1** on `:3852` (5.8s). | previous |
| Leftover fmea-hub titles | Team hub said **Failure notes**. Now leftover-fmea **Failure log**. leftover-fmea-strips no **Open FMEA**, leftover-strips-more **Pick list** / **Shifts**, leftover-cards-more **Match cards**, leftover-event-day-more **Event day**, leftover-hub-mismatch **Impact essay**, leftover-community-impact **Impact**, leftover-invites **Invites**, leftover-event-day-titles **Drive-team board** / **Day plan** stay. Hub **My Day** / **Schema A/B** stay. Source lock `leftover-fmea-hub.test.ts`. Targeted vitest leftover-fmea-hub coordinated family **25 files / 37 passed** (1.12s). Extra related family **10 files / 22 passed** (545ms). Fixture Playwright leftover-fmea-hub **1/1** on `:3851` (6.3s). | previous |
| Leftover strips-more titles | Alliance desk / Day plan related strips said **Collaborative Pick List** / **Shift Balancer**. Now leftover-strategy **Pick list** and leftover-scout-titles **Shifts**. leftover-hub-mismatch **Alliance desk**, leftover-event-day-titles **Day plan** / **Drive-team board**, leftover-event-day-more **Event day** / **Open Event day**, leftover-cards-more **Match cards**, leftover-match-more **Pick clock**, leftover-pick **Choose your team** stay. Source lock `leftover-strips-more.test.ts`. Targeted vitest leftover-strips-more coordinated family **12 files / 33 passed** (635ms). Fixture Playwright leftover-strips-more **1/1** on `:3850` (5.9s). | previous |
| Leftover cards-more titles | Match cards said **Match Strategy Cards** / **Match strategy cards** / **Strategy Cards**. Now hub **Match cards**. Related strips say **Open Match cards**. leftover-event-day extras say **Open Event day**. leftover-strategy extras say **Pick list**. leftover-event-day-titles **Drive-team board**, leftover-delta **Match delta**, leftover-team-data **Sync Team data** / **Connect TBA**, leftover-home-more **Home screens** / **Season impact**, leftover-video-more **Video index**, leftover-match-more **Pick clock**, leftover-fmea **Failure log**, leftover-pick **Choose your team**, leftover-invites **Invites**, leftover-copilot **Briefing**, leftover-help **Connect TBA** / **Alliance desk** stay. Source lock `leftover-cards-more.test.ts`. Targeted vitest leftover-cards-more coordinated family **25 files / 84 passed** (1.28s). Extra related family **6 files / 31 passed** (460ms). Fixture Playwright leftover-cards-more **1/1** on `:3849` (5.8s). | previous |
| Leftover team-data-more titles | Team data said **Team Data** / **Sync Team Data** / **Open Team Data**. Now sentence-case **Team data** / **Sync Team data**. leftover-team-data **Connect TBA** / **TBA Read API** / **Save a TBA key** stay. leftover-home-more **Home screens** / **Season impact**, leftover-delta **Match delta**, leftover-video-more **Video index**, leftover-match-more **Pick clock** / **Match notes** / **Counter-book** / **Retro**, leftover-fmea **Failure log**, leftover-pick **Choose your team**, leftover-invites **Invites**, leftover-visit-invites **Visit invites**, leftover-community-impact **Impact**, leftover-hub-mismatch **Impact essay**, leftover-event-day-more **Event day**, leftover-ops-more **Skills** / **Risk register** / **Burndown**, leftover-build-more **Parts relay** / **Subsystem sign-off**, leftover-tool-more **Tool checkout** stay. Source lock `leftover-team-data-more.test.ts`. Targeted vitest leftover-team-data-more coordinated family **25 files / 65 passed** (1.45s). Fixture Playwright leftover-team-data-more **1/1** on `:3848` (4.6s). | previous |
| Leftover home-more titles | Home screens / Season impact said **Home Screens** / **Season Impact**. Now sentence-case. leftover-delta **Match delta**, leftover-video-more **Video index**, leftover-match-more **Pick clock** / **Match notes** / **Counter-book** / **Retro**, leftover-team-data **Sync Team data**, leftover-fmea **Failure log**, leftover-pick **Choose your team**, leftover-invites **Invites**, leftover-visit-invites **Visit invites**, leftover-community-impact **Impact**, leftover-hub-mismatch **Impact essay**, leftover-event-day-more **Event day**, leftover-ops-more **Skills** / **Risk register** / **Burndown**, leftover-build-more **Parts relay** / **Subsystem sign-off**, leftover-tool-more **Tool checkout** stay. Hub **Home** / **Showcase** stay. Source lock `leftover-home-more.test.ts`. Targeted vitest leftover-home-more coordinated family **25 files / 43 passed** (1.37s). Fixture Playwright leftover-home-more **1/1** on `:3847` (4.6s). | previous |
| Leftover delta titles | Match delta said **Match-Delta Watcher** / **Match-Delta** / **Match-delta watcher**. Now hub **Match delta**. leftover-video-more **Video index**, leftover-match-more **Pick clock** / **Match notes** / **Counter-book** / **Retro**, leftover-team-data **Sync Team Data**, leftover-fmea **Failure log**, leftover-pick **Choose your team**, leftover-invites **Invites**, leftover-visit-invites **Visit invites**, leftover-community-impact **Impact**, leftover-event-day-more **Event day**, leftover-ops-more **Skills** / **Risk register** / **Burndown**, leftover-build-more **Parts relay** / **Subsystem sign-off**, leftover-tool-more **Tool checkout** stay. Source lock `leftover-delta.test.ts`. Targeted vitest leftover-delta coordinated family **25 files / 60 passed** (1.36s). Fixture Playwright leftover-delta **1/1** on `:3846` (5.5s). | previous |
| Leftover video-more titles | Video index said **Match Video Index**. Now hub **Video index**. leftover-match-more **Pick clock** / **Match notes** / **Counter-book** / **Retro**, leftover-team-data **Sync Team Data**, leftover-fmea **Failure log**, leftover-pick **Choose your team**, leftover-invites **Invites**, leftover-visit-invites **Visit invites**, leftover-community-impact **Impact**, leftover-event-day-more **Event day**, leftover-ops-more **Skills** / **Risk register** / **Burndown**, leftover-build-more **Parts relay** / **Subsystem sign-off**, leftover-tool-more **Tool checkout** stay. Hub **Match video** stays. Source lock `leftover-video-more.test.ts`. Targeted vitest leftover-video-more coordinated family **25 files / 44 passed** (1.35s). Fixture Playwright leftover-video-more **1/1** on `:3845` (5.6s). | previous |
| Leftover match-more titles | Pick clock / Match notes / Counter-book / Retro said **Pick Clock** / **Match Note Timeline** / **Opponent Counter-book** / **Team Retrospective**. Now hub labels. leftover-team-data **Sync Team Data**, leftover-fmea **Failure log**, leftover-pick **Choose your team**, leftover-invites **Invites**, leftover-visit-invites **Visit invites**, leftover-community-impact **Impact**, leftover-event-day-more **Event day**, leftover-ops-more **Skills** / **Risk register** / **Burndown**, leftover-build-more **Parts relay** / **Subsystem sign-off**, leftover-tool-more **Tool checkout** stay. Source lock `leftover-match-more.test.ts`. Targeted vitest leftover-match-more coordinated family **25 files / 42 passed** (1.04s). Fixture Playwright leftover-match-more **1/1** on `:3844` (5.4s). | previous |
| Leftover tool-more titles | Tool checkout / Training matrix / Vendor directory said **Tool Checkout** / **Training Matrix** / **Vendor Directory**. Now hub labels. leftover-offline feature **Training** / **Vendors** stay. leftover-fmea **Failure log**, leftover-pick **Choose your team**, leftover-invites **Invites**, leftover-visit-invites **Visit invites**, leftover-community-impact **Impact**, leftover-event-day-more **Event day**, leftover-ops-more **Skills** / **Risk register** / **Burndown**, leftover-build-more **Parts relay** / **Subsystem sign-off** stay. Source lock `leftover-tool-more.test.ts`. Targeted vitest leftover-tool-more coordinated family **24 files / 38 passed** (1.01s). Fixture Playwright leftover-tool-more **1/1** on `:3843` (5.5s). | previous |
| Leftover build-more titles | Parts relay / Subsystem sign-off said **Parts Relay** / **Subsystem Sign-off**. Now sentence-case. Routes stay. leftover-fmea **Failure log**, leftover-fmea-strips no **Open FMEA**, leftover-pick **Choose your team**, leftover-invites **Invites**, leftover-visit-invites **Visit invites**, leftover-community-impact **Impact**, leftover-event-day-more **Event day**, leftover-ops-more **Skills** / **Risk register** / **Burndown** stay. Source lock `leftover-build-more.test.ts`. Targeted vitest leftover-build-more coordinated family **24 files / 38 passed** (1.32s). Fixture Playwright leftover-build-more **1/1** on `:3842` (5.6s). | previous |
| Leftover invites titles | Invites said **Team admin**. Now hub **Invites**. leftover-pick-before now expects **connect GitHub under Invites**. Routes stay. leftover-join-or-pick **Choose your team**, leftover-help-workspace **Connect TBA**, leftover-event-day-more **Event day**, leftover-community-impact **Impact**, leftover-visit-invites **Visit invites**, leftover-ops-more **Skills** / **Risk register** / **Burndown**, leftover-fmea **Failure log** stay. Source lock `leftover-invites.test.ts`. Targeted vitest leftover-invites coordinated family **22 files / 92 passed** (1.74s). Fixture Playwright leftover-invites **1/1** on `:3841` (4.6s). | previous |
| Leftover visit-invites titles | Visit invites said **Visit Invites**. Now sentence-case **Visit invites**. Routes stay. leftover-event-day-more **Event day**, leftover-pick-a-team **Choose your team**, leftover-community-impact **Impact**, leftover-ops-more **Skills** / **Risk register** / **Burndown** stay. Source lock `leftover-visit-invites.test.ts`. Targeted vitest leftover-visit-invites coordinated family **8 files / 25 passed** (545ms). Fixture Playwright leftover-visit-invites **1/1** on `:3840` (5.4s). | previous |
| Leftover community-impact titles | Impact said **Community Impact**. Now hub **Impact**. Related strips say **Open Impact**. Routes stay. leftover-hub-strips **Open Impact essay**, leftover-hub-mismatch **Impact essay**, leftover-judge-sim **Judge pitch**, leftover-event-day-more **Event day**, leftover-ops-more **Skills** / **Risk register** / **Burndown**, leftover-fmea **Failure log**, leftover-lxi **How likely** / **How bad**, leftover-join-or-pick **Choose your team**, leftover-pick-before **Choose your team**, leftover-help-workspace **Connect TBA**, leftover-business-more Eligibility family stay. Source lock `leftover-community-impact.test.ts`. Targeted vitest leftover-community-impact coordinated family **22 files / 75 passed** (983ms). Fixture Playwright leftover-community-impact **1/1** on `:3839` (5.5s). | previous |
| Leftover event-day-more titles | Event day said **Event Day** / **Event Day Command**. Now hub **Event day**. Related strips say **Open Event day**. Routes stay. leftover-event-day-titles Districts family, leftover-ops-more **Skills** / **Risk register** / **Burndown**, leftover-fmea **Failure log**, leftover-lxi **How likely** / **How bad**, leftover-join-or-pick **Choose your team**, leftover-pick-before **Choose your team**, leftover-help-workspace **Connect TBA**, leftover-epa **studentRatingLabel**, leftover-team-data **Connect TBA** / **TBA Read API**, leftover-code-coach **Code**, leftover-llms **Failure log** stay. leftover-community-impact **Impact** stays. Source lock `leftover-event-day-more.test.ts`. Targeted vitest leftover-event-day-more family **23 files / 88 passed** (1.12s). Fixture Playwright leftover-event-day-more **1/1** on `:3838` (5.4s). | previous |
| Leftover ops-more titles | Skills / Risk register / Burndown said **Skills & Mentorship Graph** / **Risk Register** / **Build Burndown**. Now hub labels. leftover-event-day-more Event day stays. Routes stay. leftover-ops-titles Lead times family, leftover-goals-kit **Goals**, leftover-fmea **Failure log**, leftover-lxi **How likely** / **How bad**, leftover-join-or-pick **Choose your team**, leftover-pick-before **Choose your team**, leftover-help-workspace **Connect TBA**, leftover-media **Photos & video**, leftover-safety **Safety incidents**, leftover-scout-more Accuracy family, leftover-business-more Eligibility family stay. leftover-community-impact **Impact** stays. Source lock `leftover-ops-more.test.ts`. Targeted vitest leftover-ops-more family **22 files / 32 passed** (1.14s). Fixture Playwright leftover-ops-more **1/1** on `:3837` (6.0s). | previous |
| Leftover business-more titles | Eligibility / Renewal ROI / Tier calculator / Season costs / Mock judging said **Grant Eligibility Matcher** / **Sponsor Renewal ROI** / **Sponsor Tier Calculator** / **Season Costs** / **Mock Judging**. Now hub labels. Routes stay. leftover-business-titles Award tracker family, leftover-people-more **Matching gifts**, leftover-pick-before **Choose your team**, leftover-fmea **Failure log**, leftover-help-workspace **Connect TBA**, leftover-media **Photos & video**, leftover-safety **Safety incidents**, leftover-scout-more Accuracy family stay. leftover-ops-more Skills / Risk register / Burndown stay. leftover-community-impact **Impact** stays. Source lock `leftover-business-more.test.ts`. Targeted vitest leftover-business-more family **18 files / 58 passed** (772ms). Fixture Playwright leftover-business-more **1/1** on `:3836` (5.3s). | previous |
| Leftover scout-more titles | Accuracy / Cross-check / Disagreements / Data impact / Heat signals / Assisted count said **Scout Accuracy** / **Scout Cross-Validation** / **Scout Disagreements** / **Scout Data Impact** / **Scouting Heat Signals** / **Scout-Assisted Count**. Now hidden-scout hub labels. Routes stay. leftover-scout-titles Coverage family, leftover-pick-before **Choose your team**, leftover-fmea **Failure log**, leftover-help-workspace **Connect TBA**, leftover-media **Photos & video**, leftover-safety **Safety incidents** stay. leftover-business-more Eligibility family stays. Source lock `leftover-scout-more.test.ts`. Targeted vitest leftover-scout-more family **26 files / 55 passed** (1.06s). Fixture Playwright leftover-scout-more **1/1** on `:3835` (4.6s). | previous |
| Leftover safety titles | Safety incidents said **Safety Incident Log**. Now sentence-case **Safety incidents**. Route `/incidents` stays. Hub **Safety log**, leftover-kit **Safety**, leftover-build **Incidents**, leftover-media **Photos & video** stay. leftover-fmea **Failure log**, leftover-pick-before **Choose your team**, leftover-help-workspace **Connect TBA** stay. leftover-scout-more Accuracy family stays. `safety-incidents-chrome` rewritten to require sentence-case. Source lock `leftover-safety.test.ts`. Targeted vitest leftover-safety family **34 files / 49 passed** (1.45s). Fixture Playwright leftover-safety **1/1** on `:3834` (5.4s). | previous |
| Leftover media titles | Photos & video said **Media Library**. Now Business hub **Photos & video**. Hidden hub **Media library** and Drive folder **Media Library** stay. Route stays. leftover-fmea **Failure log**, leftover-pick-before **Choose your team**, leftover-help-workspace **Connect TBA** stay. leftover-safety **Safety incidents** stays. Source lock `leftover-media.test.ts`. Targeted vitest leftover-media family **31 files / 44 passed** (1.30s). Fixture Playwright leftover-media **1/1** on `:3833` (4.9s). | previous |
| Leftover cad-map titles | CAD review queue / Control map said **CAD Review Queue** / **Control Map**. Now sentence-case. Routes stay. leftover-fmea **Failure log** and leftover-pick-before **Choose your team** stay. leftover-media **Photos & video** stays. Source lock `leftover-cad-map.test.ts`. Targeted vitest leftover-cad-map family **29 files / 44 passed** (1.65s). Fixture Playwright leftover-cad-map **1/1** on `:3832` (5.6s). | previous |
| Leftover goals-kit titles | Goals / Standup / My kit / Field reset / Season report said **Season Goals** / **Morning standup** / **My Kit** / **Field reset timer** / **Season Report**. Now hub labels. Routes stay. `season-goals-standup-chrome` rewritten to require hub labels. leftover-fmea **Failure log** and leftover-pick-before **Choose your team** stay. Help **Connect TBA** stays. Source lock `leftover-goals-kit.test.ts`. Targeted vitest leftover-goals-kit family **24 files / 58 passed** (1.51s). Fixture Playwright leftover-goals-kit **1/1** on `:3831` (5.6s). | previous |
| Leftover kit titles | Equipment / Safety / Alumni / Search / Reports said **Equipment Maintenance** / **Safety Training** / **Alumni Network** / **Decision Search** / **Grant Report**. Now hub labels. Routes stay. leftover-goals-kit golds Season Goals / Morning standup. leftover-fmea **Failure log** and leftover-pick-before **Choose your team** stay. Source lock `leftover-kit-titles.test.ts`. Targeted vitest leftover-kit-titles family **28 files / 46 passed** (1.16s). Fixture Playwright leftover-kit-titles **1/1** on `:3830` (5.7s). | previous |
| Leftover ops titles | Lead times / Deploy log / Season plan / Scrims / Risk burndown said **Vendor Lead Times** / **Code Deploy Log** / **Season Planning** / **Cross-Team Scrim Scheduling** / **Risk-Register Burndown**. Now hub labels. Routes stay. Help / llms Season plan gold stays off this lock. leftover-fmea **Failure log**, leftover-pick-before **Choose your team**, leftover-hub-strips **Open Spares forecast** / **Open Readiness** stay. Source lock `leftover-ops-titles.test.ts`. Targeted vitest leftover-ops-titles family **31 files / 47 passed** (1.24s). Fixture Playwright leftover-ops-titles **1/1** on `:3829` (5.6s). | previous |
| Leftover CAD titles | CAD vault / Learn CAD / Design reviews / Prototypes / Sketch to brief said **CAD Vault** / **CAD Learn** / **Design Reviews** / **Prototype-to-Decision Tracker** / **Sketch-to-Brief**. Now hub labels. Routes stay. Drive folder names stay. Source lock `leftover-cad-titles.test.ts`. Targeted vitest leftover-cad-titles family **30 files / 65 passed** (1.20s). Fixture Playwright leftover-cad-titles **1/1** on `:3828` (5.5s). | previous |
| Leftover build titles | Failure patterns / Incidents / Rule impact / Bin locator / Pack health said **Repeat Failure Patterns** / **Incident Heatmap** / **Rule Impact Analyzer** / **Bin/Shelf Locator** / **Battery Health Forecast**. Now hub labels. Routes stay. leftover-fmea **Failure log ·** and leftover-hub-strips **Open Repair triage** stay. Source lock `leftover-build-titles.test.ts`. Targeted vitest leftover-build-titles family **30 files / 78 passed** (1.23s). Fixture Playwright leftover-build-titles **1/1** on `:3827` (5.6s). | previous |
| Leftover people-more titles | Exit interviews / Driver tryouts / Mentor hours / Onboarding buddy / Matching gifts said **Graduation Exit Interviews** / **Driver Tryouts** / **Mentor Hours** / **Onboarding Buddy** / **Matching Gift Finder**. Now hub labels. Routes stay. Source lock `leftover-people-more.test.ts`. Targeted vitest **30 files / 72 passed** (1.31s). Fixture Playwright leftover-people-more **1/1** on `:3826` (5.6s). | previous |
| Leftover business titles | Award tracker / Sponsor suite / Sponsor wall / Outreach calendar / Team health said **Award Tracker** / **Sponsor Suite** / **Sponsor Wall** / **Outreach Calendar** / **Team Health**. Now hub labels. Routes stay. Source lock `leftover-business-titles.test.ts`. Targeted vitest **32 files / 87 passed** (1.85s). Fixture Playwright leftover-business-titles **1/1** on `:3825` (5.7s). | previous |
| Leftover people titles | Leadership / Pit map / Decision notes / Checklists / My hours / Team setup said **Leadership Continuity** / **Pit Map Planner** / **Decision Log** / **Checklist Library** / **My Hours** / **Getting started**. Now hub labels. Routes stay. Help category **Getting started** stays. Source lock `leftover-people-titles.test.ts`. Targeted vitest **29 files / 79 passed** (1.42s). Fixture Playwright leftover-people-titles **1/1** on `:3824` (5.8s). | previous |
| Leftover event-day titles | Districts / Rank projection / Day plan / Drive-team board / Charge plan said **District advancement** / **Ranking projection** / **Event-Day Stress Planner** / **Drive-Team Signal Board** / **Battery Rotation**. Now hub labels. Routes stay. Source lock `leftover-event-day-titles.test.ts`. Targeted vitest **22 files / 50 passed** (1.07s). Fixture Playwright leftover-event-day-titles **1/1** on `:3823` (6.0s). | previous |
| Leftover scout titles | Coverage / Shifts / Pit link / Field value / Data quality / Pairwise said **Scout Coverage Live** / **Scout shift balancer** / **Pit mesh** / **Field-Count Budget** / **Data Quality Scorecard** / **Pairwise ranking**. Now hub labels. Routes stay. Source lock `leftover-scout-titles.test.ts`. Targeted vitest **29 files / 75 passed** (1.29s). Fixture Playwright leftover-scout-titles **1/1** on `:3822` (5.0s). | previous |
| Leftover strategy titles | Alliance sim / Partner brief / Justifier / Watchlist / Pick list / Defense said **Alliance Sim** / **Alliance-Partner Brief** / **Pick-list Auto-Justifier** / **Opponent Watchlist** / **Collaborative pick list** / **Defense Planner**. Help / public strategy said **Alliance Selection Desk** / **Media Kit**. Now hub labels. Connect TBA stays. Source locks `leftover-strategy-titles.test.ts` and `leftover-hub-help.test.ts`. Targeted vitest **30 files / 54 passed** (1.26s). Fixture Playwright leftover-strategy-titles **1/1** on `:3821` (5.5s). | previous |
| Leftover hub strips | Related strips still said **Spare Forecast** / **Impact Essay** / **Alliance Selection Desk** / **Pit Repair Triage** / **Media Kit** / **Readiness Score**. Now hub labels. Source lock `leftover-hub-strips.test.ts`. Targeted vitest **23 files / 69 passed** (1.00s). Fixture Playwright leftover-hub-strips **1/1** on `:3820` (5.4s). | previous |
| Leftover hub mismatches | Spares forecast / Spare kit / Impact essay / Alliance desk / BOM cost / Repair triage said **Spare-Parts Failure Forecast** / **Spare Robot Kit Checklist** / **FIRST Impact Essay Generator** / **Alliance Selection Desk 2.0** / **BOM Cost Rollup** / **Pit Repair Triage**. Now hub labels. Routes stay. Source lock `leftover-hub-mismatch.test.ts`. Targeted vitest **24 files / 35 passed** (986ms). Fixture Playwright leftover-hub-mismatch **1/1** on `:3819` (6.1s). | previous |
| Leftover hub titles | Wiring check / Reuse / Change radar / Bus factor / Readiness / Media kit said **Wiring / Power Fault Diagnoser** / **Reuse Advisor** / **CAD Change Impact Radar** / **Bus-Factor & Burnout** / **Readiness Score** / **Media Kit**. Now hub labels. Routes stay. Source lock `leftover-hub-titles.test.ts`. Targeted vitest **20 files / 31 passed** (873ms). Fixture Playwright leftover-hub-titles **1/1** on `:3818` (5.9s). | previous |
| Leftover Judge-Pitch Simulator | Judge pitch said **Judge-Pitch Simulator**. Now hub **Judge pitch**. Route `/judge-sim` stays. Meeting and Inspection manifests say **Meeting agenda** / **Inspection**. Source lock `leftover-judge-sim.test.ts`. Targeted vitest **13 files / 31 passed** (669ms). Fixture Playwright leftover-judge-sim **1/1** on `:3817` (6.1s). | previous |
| Leftover Pick which FRC team | Setup step details said **Pick which FRC team you are working as.** Now **Choose which FRC team you are working as.** **Pick a teammate** stays. Source lock `leftover-pick-which.test.ts`. Targeted vitest **10 files / 23 passed** (644ms). Fixture Playwright leftover-pick-which **1/1** on `:3816` (6.2s). | previous |
| Leftover Knowledge-gap detective | Knowledge gaps said **Knowledge-gap detective**. Now hub **Knowledge gaps**. Route `/knowledge-gap` stays. Source lock `leftover-knowledge-gap.test.ts`. Targeted vitest **13 files / 38 passed** (686ms). Fixture Playwright leftover-knowledge-gap **1/1** on `:3815` (6.5s). Leftover Autopilot loading / error / metered-AI messages also say **Tuning advisor**. | previous |
| Leftover Autopilot / Simulator | Tuning advisor, Match sim, and Meeting agenda said **Tuning Autopilot** / **Match Simulator** / **Meeting Autopilot**. Now hub labels. Routes stay. Source lock `leftover-autopilot.test.ts`. Targeted vitest **13 files / 47 passed** (1.05s). Fixture Playwright leftover-autopilot **1/1** on `:3814` (6.0s). | previous |
| Leftover Code Coach | Code, Code vs match, Deploy log, Pair VS Code, public `/features/code`, and llms.txt said **Code Coach** / **FRC Code Coach**. Now **Code**. Route `/code` stays. Source lock `leftover-code-coach.test.ts`. Targeted vitest **19 files / 95 passed** (1.14s). Fixture Playwright leftover-code-coach **1/1** on `:3813` (5.0s). | previous |
| Leftover Inspection Copilot / Match Copilot | Readiness, Weigh-in, Inspection, and the redirected match-brief page said **Inspection Copilot** / **Match Copilot**. Now **Inspection** / **Briefing**. Routes and cache keys stay. Source lock `leftover-copilot.test.ts`. Targeted vitest **19 files / 113 passed** (1.52s). Fixture Playwright leftover-copilot **1/1** on `:3812` (6.2s). | previous |
| Leftover email OTP next-actions | AI Governance / Budgets / Usage / Memory said **email OTP**. Now **emailed sign-in code**. Source lock `leftover-email-otp.test.ts`. Targeted vitest **5 files / 37 passed** (319ms). Fixture Playwright leftover-email-otp **1/1** on `:3811` (5.1s). Admin integration-health stays. | previous |
| Leftover Pick a team before / PAT OAuth | Related next-actions said **Pick a team before…**. Bugbot / Help said **PAT or OAuth** / **OAuth app**. Account / Connections said Resend / **setup-required** / **encrypted PAT**. Now **Choose your team**, **connect GitHub**, and student Account copy. Source lock `leftover-pick-before.test.ts`. Targeted vitest **6 files / 34 passed** (349ms). `npm run typecheck --workspace=@vantage/web` clean. Fixture Playwright leftover-pick-before **1/1** on `:3810` (5.1s). Earlier leftover-export **1/1** on `:3809` and deferred gates stay. | previous |
| Playwright survive + leftover chrome + verification | Exports / CAD Change Radar / public facts / Risks / Tuning / Failure log leftover golds stay. This revision re-ran the deferred gates: `npm test` **1224 / 9102** (76.29s). `eslint --max-warnings=0` clean. typecheck all workspaces clean. Production web build **695 pages**, **0 ⚠**. Local Postgres down — RLS not re-proven. Fixture leftover-export **1/1** on `:3809`. leftover-llms **3/3** on `:3808`. leftover-fmea-links **1/1** on `:3807`. leftover-lxi-risks **1/1** on `:3806`. leftover-fmea-boards **1/1** on `:3805`. leftover-fmea-strips **1/1** on `:3804`. leftover-fmea-board **1/1** on `:3803`. leftover-fmea-briefing **2/2** on `:3802`. leftover-cad-onshape **1/1** on `:3801`. | previous |
| Chat remaining student chrome | Empty Chat could grow extra + Private / + Team-shared and Next-actions. Setup still said provider-key jargon. Now one **New private chat** on empty; **Needs setup** + one **Open Team Admin**; Next-actions only on ready; no `getFeatureSnapshot` (skip-list). Source lock: `chat-remaining.test.ts`. Fixture Playwright `chat-remaining.spec.ts` **1 passed / 1** (3.3s) on `:3598`. | this commit |
| Video remaining student chrome | Match video paste-and-confirm already on main. Remaining `/video-analysis` empty still had a second **Analyze this video** link beside the paste submit, printed raw file ids / **min behind**, and API said **TBA**. Now one paste **Analyze this video**, EmptyState copy only, FormRow, **Uploaded file**, **Needs setup**, related in the header, Next-actions on ready. Source lock: `video-remaining-chrome.test.ts`. `npm run typecheck --workspace=@vantage/web` clean. Targeted vitest **4 files / 24 passed** (403ms); copy-lint + empty-state-primary **2 files / 14 passed** (3.66s). Fixture Playwright `video-analysis-shell.spec.ts` **1 passed / 1** (23.2s) on `:3591` + local `vantage_ci` (127.0.0.1, not production DATABASE_*). Stayed off `/video` and web-performance. | this commit |
| Code vs match / Software versions student chrome | `/code-perf` still said **Code-vs-Match Detective** and **Setup required**; `/software-versions` still badged **Setup required**. Now **Code vs match** / **Software versions**, **Needs setup**, one EmptyState primary, related in the header, Next-actions on ready, last snapshot `if (!view)`. `code-client.tsx` skip-list — student copy only, no fake last-snapshot. Source lock: `code-vs-match-chrome.test.ts`. Fixture Playwright `code-vs-match.spec.ts` **1 passed / 1** (11.5s) on `:3544` + local `vantage_ci`. Merge session: typecheck clean; targeted vitest **5 files / 19 passed** (311ms); fixture Playwright **1 passed / 1** (6.1s) on `:3555`. Leftover-product already on main — not redone. | this commit |
| Leftover-product student chrome | Billing still said **Setup required** / **Hosted by Vantage** / **Hard cut-off** and could invent `$0.00`. Files/media, Help / What’s new, Print farm (**3D Print Farm**), Playbook, Inspection copilot, and remaining scout boards needed gold chrome. Now **Needs setup**, one EmptyState primary, related in the header, Next-actions on ready, last snapshot `if (!view)` (`feature: "knowledge"`). Spend tiles stay **—** until a ledger exists. Student title is **Print farm**. Inspection drops copilot jargon. Copy-lint Setup required / VANTAGE / TBA on student-week boards only. Source lock: `leftover-product-chrome.test.ts`. Fixture Playwright `leftover-product.spec.ts` + `print-farm-shell.spec.ts` **2 passed / 2** (9.8s) on `:3535` + local `vantage_ci`. Stayed off Hours, relays, and code-vs-match. Remaining-boards-2 already on main. | this commit |
| Inbox remaining student chrome | Last-snapshot already on main. Remaining: empty inbox still painted a Next-actions wall of related links; prefs/Account still said **Setup required**, **Sponsor CRM**, **TBA/reference ingest**, and Resend env names. Empty keeps one EmptyState primary; related in the header; Next-actions stay off empty. Prefs API uses student email copy. Source lock: `inbox-remaining.test.ts`. Fixture Playwright `inbox-remaining.spec.ts` on `:3527`. Stayed off Team people/admin. | this commit |
| Safety / incidents student boards | Safety log still said **Setup required** / **VANTAGE / SAFETY** and used `primary-action`. Incidents, Safety Training, and Incident Heatmap still badged **Setup required**. Now **Needs setup**, one **Choose your team** primary, related in the header, gold last-snapshot. Source lock: `safety-incidents-chrome.test.ts`. Fixture Playwright `safety-incidents.spec.ts` on `:3527`. | this commit |
| Parents / Leadership student-readable boards | Parent updates still said **Setup required** and nested Calendar/Forms in Next-actions. Leadership empty painted 0% succession tiles, **Setup required**, and a Next-actions wall beside the add-role form. Both now **Needs setup**, one primary, related in the header, Next-actions only on ready. `/leadership` sits under Team › People. Source lock: `parents-leadership-chrome.test.ts`. Fixture Playwright `parents-leadership-shell.spec.ts` on `:3527`. | this commit |
| Impact / community remaining student chrome | Community Impact, Awards, essay, outreach, judge-pitch, tracker, and mock judging still risked invented readiness scores. Setup stays **Needs setup**. Empty keeps one primary; readiness / hours / reach stay blank until logged. Related strip is Outreach · Awards · Writer. Awards reads `/api/me` when the URL has no orgId. Source lock: `impact-remaining.test.ts`. Fixture Playwright `impact-remaining.spec.ts` on `:3527`. | this commit |
| Research / Overnight brief remaining chrome | Setup badge was **Setup**; student chrome still said EPA. Now **Needs setup**, Rating not EPA, one EmptyState primary, related in the header, Next-actions only after a team is open. Last snapshot `feature: "intel"` / `"overnight-intel"` uses `if (!view)`. Research reads `/api/me` when the URL has no orgId. Source lock: `research-remaining.test.ts`. Fixture Playwright `research-remaining.spec.ts` on `:3527`. | this commit |
| Pit wiring / consumables remaining chrome | CAN-bus map, Wiring check, and Consumables still badged **Setup required**. Now **Needs setup**, one Choose your team primary, gold last-snapshot `if (!view)`. Stayed off Event day packing. Source lock: `pit-wiring-consumables.test.ts`. Fixture Playwright `pit-wiring-consumables.spec.ts` on `:3527`. | this commit |
| Season Goals / Morning standup / Meeting agenda | Those three still badged **Setup required**; Meeting heading said **Meeting-agenda autopilot**. Now **Needs setup**, one EmptyState primary, related in the header, Next-actions on ready, last snapshot `if (!view)`. Source lock: `season-goals-standup-chrome.test.ts`. Fixture Playwright `season-goals-standup.spec.ts` **1 passed / 1** (6.8s) on `:3482` + local `vantage_ci`. Already on main (`08fd172b`). | this commit |
| `/video-analysis` Video | Title said Analyze video; empty said Pick a team first even when the header already had a team; extra Pair a video Pi CTA; "video jobs" / file id / confidence numbers. Now **Video**: session team from `/api/me` when the URL has no orgId, one Choose your team primary only when there is no team, related in the header (**Event day · Match notes · Match video**), Next-actions only after a video is queued, fetches abort at 8s. `/video` stays Match video. Fixture Playwright `video-analysis-shell.spec.ts`. | this commit |
| Home + remaining Competition boards | Home widgets still painted TBA/EPA API messages; Draft / Pick clock / Chemistry empty said **Sync event metrics**; Alliance desk badged TBA/EPA; Match video related still listed AI relays; Chat empty could grow extra + Private chat. Now catalog hints, **Needs setup**, one primary, Event day related, last-snapshot gold on Strategy / Chemistry / Pick clock / Draft. Live chips say **Rating**, not EPA. Source lock: `home-competition-polish.test.ts`. Fixture Playwright `home-competition-polish.spec.ts` + `video-analysis-shell.spec.ts` on local `vantage_ci` (127.0.0.1): Home+Strategy **1 passed** (52.9s) and Match video **1 passed** (1.1m) on `:3461`; Event day + Chat **2 passed / 2** (20.7s) on `:3463`; Video **1 passed / 1** (4.0s) on `:3460`. | this commit |
| Next match / Briefing honest MAE copy | Empty prediction still said event/year EPA. Score line is **typical error ±90 (last measured set)** from fixture MAE **89.7** — not a season ±3 claim. Skip reasons say missing ratings. Briefing setup is **Needs setup**. Source lock: `prediction-honest.test.ts`. | this commit |
| Fusion CAD GUI human-edit | Onshape Edit-in-Onshape already on main. Fusion still said **Open in Fusion** and pairing-only. Students can paste an `a360.co` / Autodesk hub link and **Edit in Fusion** from CAD vault, `/cad/setup` (Fusion radio), and `/cad/connections` Fusion tile. Missing Fusion config → **Needs setup** (no env-var names). 401/403 → **Choose your team**. Onshape viewport untouched. Source lock: `fusion-cad-gui.test.ts`. Fixture Playwright `fusion-cad-gui.spec.ts` **3 passed / 3** (9.6s) on `:3448` + local `vantage_ci` (127.0.0.1, not production DATABASE_*). `npm run typecheck` clean across workspaces. Targeted vitest **9 files / 31 passed** (0.45s). | this commit |
| `/competition` Event day | Duplicate page header when embedded in the hub; poll ran while the tab was hidden | this branch |
| `/strategy` From our scouting | "pEPA" and "Private Edge" were jargon | this branch |
| `/files` | "enforced by the database" | this branch |
| `/strategy` pick list | "org pEPA" in the event pool | this branch |
| `/workflow` | "platform admin", "setup-required", "Private Edge", "BYOK" | this branch |
| `/features/strategy` | "Private Edge" | this branch |
| Settings | "My AI keys" duplicated "AI keys" | this branch |
| Team admin / GitHub related | Connections pointed at a dead Account tab; now `/connectors` | this branch |
| `/dashboard` | Student/mentor default widgets were empty stamps with no live view | this branch |
| `/dashboard` Home fallbacks | Empty stored layout, first paint, no-org, switch/reset/create still used the competition board (`competition_snapshot` / robot readiness / scouting coverage) instead of My Day / Learn / Files / Chat | this commit |
| `/signin` light | Hardcoded Apple gray (`#ffffff` / `#f5f5f7` / `#6e6e73` / `#1d1d1f`) so a 26-rule dark counterpart was still required | this commit |
| Leaf dark palettes | cad-learn, my-kit, reconcile, ranking-projection, Nexus queue, usage-cutoff, data-source banner, action-menu, and export panel flipped a second hex set at night | this commit |
| `/ai` Finance | "Finance-in-AI" on student screens | this branch |
| `/offline` | "Offline shell ready" | this branch |
| `/ai` Agent | "setup_required" shown as a pill | this branch |
| `/team/relays` | Connector card only — no list of paired Pis, roles, or queue | this branch |
| `/account` Connections tab | Duplicate of `/connectors`; now redirects | this branch |
| `/hours` `/messages` `/match-checklist` `/match-notes-timeline` | Offline writes said they would wait, then dropped the change | this branch |
| `/packing` `/batteries` `/pit` `/tasks` | Last snapshot was dropped when venue Wi-Fi died; packing ticks and battery logs were not queued | this branch |
| `/pit` | "no invented percentage" on the board | this branch |
| `/files` | No way to keep a file on the device | this branch |
| `/strategy` empty | "Neon cache", TBA_AUTH_KEY, "will not invent EPA" | this commit |
| `/calendar` | Breadcrumb repeated "Calendar / Season Calendar"; offline edits lied then dropped; ⌘K "season calendar" opened the shop calendar | this commit |
| Chat limits / Usage / Pricing / Help | PAYG, kill switch, BYOK, TBA_AUTH_KEY leaked into student copy; copy-lint now scans related-copy + the manual | this commit |
| Rankings banner | "Data source degraded", ETag/Neon | this commit |
| `/ai` Agent | `setup_required` / `running` shown as pills | this commit |
| `/offline` boot status | "workspace remembered" / "pick an org first" on the cold-offline card | this commit |
| `/workspace` empty states | "Select a team workspace" / "Open Workspace" | this commit |
| `/competition` Scouting empty | Coverage/Strategy/Offline duplicated the hub strip and next-actions | this commit |
| Competition empty cards | Empty/setup walls still offered 3–4 sibling buttons (Open Strategy, Open Coverage, …) | this commit |
| `/scouting/forms` | Match / Pit used a TabBar under the hub tabs | this commit |
| `/team/data` | "Workspace inventory" | this commit |
| Home widgets / related strips / intel nav | Student copy still said Workspace on Home empty cards, related strips, Event Day setup, and `Workspace →` crumbs | this commit |
| `/workspace` document title | Browser tab said Workspace | this commit |
| `/video` review detail | Re-scout / Notes used a TabBar under the page header | this commit |
| `/my-day` `/schedule` | Last snapshot dropped when venue Wi-Fi died | this commit |
| Native product buttons | leftover `<button className="app-button">` on submit/in-page actions | this commit |
| `/command` | Event Day not on the service-worker shell list | this commit |
| Product CSS | leaf sheets still consumed `--soft-*` color aliases | this commit |
| Product CSS type/space | `--title-*` `--text-*` `--space-*` `--control-h` `--island` replace leftover `--soft-*` consumption | this commit |
| `/code` Bugbot | scan time unprinted; "BYO key" / `feature=coding` on student chrome | this commit |
| Fixture walk | `/` `/dashboard` `/bugbot` `/connectors` `/offline` under `E2E_AUTH_FIXTURE` | this commit |
| Playwright marketing / Home / Account | Landing h1, pricing h1, Home tap-to-place coarse pointer, Account 401 without hanging getSession | this commit |
| Competition Scouting strip | Accuracy and seven other meta jobs left the strip; Field value + Data quality stay | this commit |
| Product CSS `--app-*` | leaf sheets still consumed `--app-*` color aliases; now canonical `--accent` / `--surface` / `--positive-fill` / `--critical-fill` / `--accent-hover` | this commit |
| Next match error band | placeholder `DEFAULT_ERROR_BAND = 8` replaced with fixture MAE **90** (`typical error ±90 (last measured set)`) | this commit |
| Product CSS `--m-*` | product chrome must not use the marketing paper palette; css-integrity allows `--m-*` only in marketing/legal sheets | this commit |
| Sign-in / Event Day / onboarding dark | competing `#0f141a` / `#171d25` / `#111823` / `#152036` wells replaced with `--bg` / `--surface` / `--ink` / `--accent` / `--accent-soft` / `--positive-soft`. Onboarding no longer needs a 50-rule dark counterpart. css-integrity fails if those hexes return. | this commit |
| Home venue weather | off-day copy and mocked Houston 26.1 → 26°C Clear; fixture library card stays empty without an event city | this commit |
| Offline packing / batteries / pit | Playwright reloads those shell routes after `context.setOffline` | this commit |
| Signed-in `next start` catalog | FEATURE_MAP HTTP walk **143/143 OK** with Better Auth (admin 404s were fixture-only); Playwright Home / Admin / Connectors | this commit |
| Signed-in venue weather | Seeded Houston event-day card on `next start`; Open-Meteo live (not mocked) | this commit |
| Signed-in offline shells | Playwright packing / batteries / pit / competition / Event Day survive `setOffline` on `next start` | this commit |
| `/docs` `/help` `/account` `/notifications` | Non-hub TabBars (manual views, season moments, account sections, inbox filters) became ToolStrip chips | this commit |
| `/business` school-funded no sponsors | Overview still offered Open CRM / Open sponsors after the hub hid those tabs; funding default no longer steals a bounced `?tab=sponsors` | this commit |
| `/team/calendar` | Hub shell was 2335 lines; event editors, duties/trip/subteams, ICS, GitHub hint, and timed grid now live in sibling modules. Calendar tabs stay custom buttons, not a TabBar. | this commit |
| `/scouting` | Hub shell was 1882 lines; chrome, field widgets, quarantine, media downscale, and the ready workbench now live in sibling modules. Match/Pit stay a ToolStrip, not a nested TabBar. | this commit |
| `/team` Chat | Hub shell was 1455 lines; labels/helpers and the painted workbench now live in sibling modules. Channels stay sidebar buttons, not a TabBar. | this commit |
| `/files` | Standalone Drive had no next step into Playbook / Chat / CAD | this commit |
| All panel | Hub rows hid Scouting / Chat / CAD behind a second TabBar hop; other workbenches now hang under the hub row (default tab stays the hub link) | this commit |
| Inspection Copilot error copy | still said Subsystems after the strip became Weigh-in | this commit |
| `/scouting/forms` | Hub shell was 1312 lines; chrome, option/settings editors, preview, and starter drafts now live in sibling modules. Match / Pit stay a ToolStrip, not a nested TabBar. | this commit |
| `/build` Kickoff | Hub shell was 1249 lines; intelligence, scoring, priorities, and rules now live in sibling modules. Shell is 257 lines. Hub TabBar stays Kickoff · CAD · Code · Robot. FMEA / prototypes / batteries / Kickoff sit behind HubOrgGate so GHA without Postgres paints **Choose a team** instead of hanging on `/api/kickoff`. | this commit |
| `/team/ai-keys` | Hub shell was 1186 lines; chrome, provider cards, and the painted workbench now live in sibling modules. Shell is 485 lines. Mine / Team stay sections, not a nested TabBar. Empty copy says team, not workspace. | this commit |
| `/logistics` | Hub shell was 1162 lines; chrome, day-of, mentor planning, and trips now live in sibling modules. Shell is 286 lines. Trip switcher is a ToolStrip, not a nested TabBar. Empty keeps one primary (Add a trip); next actions no longer repeat the header related strip. | this commit |
| `/team/admin` | Hub shell was 1094 lines; chrome, access requests, invites, GitHub, and leftover providers now live in sibling modules. Shell is 743 lines. No nested TabBar. No-org empty keeps one **Choose your team** primary. | this commit |
| `/onboarding` | Hub shell was 1092 lines; chrome, funding fields, step forms, pending, and landing now live in sibling modules. Shell is 513 lines. No nested TabBar. Pending copy says team, not workspace. | this commit |
| `/inventory` | Hub shell was 1086 lines; chrome, item rows, locations, BOM, and stock toolbar now live in sibling modules. Stock / Locations / BOM are a ToolStrip, not a nested TabBar. Empty keeps one **Add a part** primary; Vendors / Orders / Spare Forecast stay in the header related strip. | this commit |
| `/command` | Hub shell was 1082 lines; chrome, event picker, and the painted workbench now live in sibling modules. Shell is 326 lines. `/command` lands on Competition › Event day. Empty keeps one **Check schedule sync** primary. Hub TabBar stays Event day · Scouting · Strategy · Pit. | this commit |
| `/signin` | Public shell was 1066 lines; chrome, identity, code, and already-signed-in panels now live in sibling modules. Shell is **653** lines. No nested TabBar. One **Email me a sign-in code** primary; waitlist + pricing stay in the footer. Setup copy no longer says "workspace database". | this commit |
| `/print-farm` | Hub shell was 1057 lines; chrome, queue, printers, filament, and recently finished now live in sibling modules. Shell is **121** lines. No nested TabBar. Setup keeps one **Choose your team** primary. Status stays human-reported — never live telemetry. | this commit |
| `/batteries` | Hub shell was 1056 lines; chrome, fleet, forms, and assign row now live in sibling modules. Shell is **250** lines. `/batteries` lands on Build › Robot › Batteries. Unmeasured packs stay **Unknown**, never Good. Hub TabBar stays Kickoff · CAD · Code · Robot. | this commit |
| `/strategy` | Hub shell was 1043 lines; chrome and live matchup now live in sibling modules. `/library` client deleted — Drive already owns files. MODEL/FACT badges on the live panel became student copy. | this commit |
| `/inspection-copilot` | Log-check form was 1034 lines; catalog, payload builder, and section panels now live in sibling modules. Shell form is **94** lines. Setup keeps one **Choose your team** primary; Batteries / FMEA / Weigh-in stay in the header related strip. Fetches abort at 8s so GHA without Postgres recovers. Binder hint no longer says "never invent". | this commit |
| Next match widget | Red/Blue printed as a dash “range”; live EPA drivers never shown; MODEL leftover on Event Day / Chemistry / kiosk | this commit |
| `/dashboard` First-run banner | GHA Playwright hid the region until `/api/me` finished; GitHub Actions has no Postgres so that fetch hung past the 5s default. Banner now paints on the loading shell; session abort is 8s. | this commit |
| `/hours` | Custom header, native clock buttons, hung `/api/hours` without a timeout, and a mentor “engagement digest” on the student clock-in page. Shell is now PageHeader + EmptyState, fetches abort at 8s, Clock in is a Button, digest is owner/admin only. | this commit |
| `/competition` Chemistry / Pick clock / My Day / Pit | Chemistry and Pick clock fetched `/api/me` with no timeout; GHA without Postgres hung. HubOrgGate now wraps those tabs plus My Day and the pit checklist so a missing team paints **Choose a team**. Fetches abort at 8s. Setup copy no longer says org-/event-scoped. Pick clock no longer paints a 6-step checklist beside the related strip. Team dossier and hub access also abort `/api/me` at 8s. | this commit |
| `/intel` | No-org setup painted four sibling buttons plus a 5-step checklist. Now one **Choose your team** primary; Strategy / Dossier / Scouting stay on the related strip. Fetches abort at 8s. | this commit |
| `/alliance-selection-desk` `/video` `/my-day` | Saturday shells painted 4–5 setup steps beside the related strip. Each now has one primary (Choose your team / Set active event / Sync the schedule). Fetches abort at 8s. Account and narration `/api/me` abort at 8s. | this commit |
| `/picklist-collab` `/defense-planner` `/strategy/draft` `/scouting/lineup` `/match-strategy-cards` `/dossier` `/strategy?tab=picks` | Saturday boards still painted a Setup steps checklist beside the related strip (Dossier twice). Each now has one primary (Choose your team / Set active event / Sync Team Data). Next-actions panel only on ready. Fetches abort at 8s. | this commit |
| `/pit-repair-triage` `/drive-team-signals` `/field-reset-timer` `/robot-weigh-in` `/event-day-plan` `/match-delta-watcher` `/match-video-index` | Saturday/pit boards still painted a Setup steps checklist beside the related strip. Each now has one primary (Choose your team / Set active event / Open Inventory / Sync Team Data). Next-actions panel only on ready. Fetches abort at 8s. | this commit |
| Remaining product Setup steps walls | Every remaining `<h2>Setup steps</h2>` wall (tool checkout, burndown, scout meta jobs, team admin, Hours self-view, CAD radar, and the rest of the 37 shells that still painted the checklist) now keeps one EmptyState primary. Zero `<h2>Setup steps</h2>` left under `apps/web`. | this commit |
| Leftover `strategy-setup-steps` / `view.steps` lists | Command, Inventory, Strategy, Inspection, Logistics, Scouting, Pit, Match checklist, FMEA, Discord, Presence, Event readiness, and the rest of the named `*-setup-steps` EmptyState checklists are gone. Setup is one EmptyState primary; related strip stays in the header; next-actions paint on ready. CAD wizard progress and lineup form-mapping warnings stay (not setup walls). `empty-state-primary.test.ts` fails if `className="strategy-setup-steps"` returns. | this commit |
| EmptyState titles vs buttons | Titles still said **Select a team** while the primary said **Choose your team**. Titles, compute/API `message`s, Home header, HubOrgGate, and leftover **Choose team** / **Choose a team** CTAs now say **Choose your team**. Event titles say **Set active event** (sentences: **Set your active event**). `copy-lint.test.ts` + `copy-assertions.ts` ban the old phrases, including compute/API source. | this commit |
| Event picker leftovers | Home header, Event Day, Strategy, onboarding, and workspace still said **Select event**. Scout voice notes, Event Day queue, scouting submit, and attach-media refused with **Select an event**. Event Day picker heading and My Day / Match copilot / Command loaders still said **Select active event**. All now say **Set active event** / **Set your active event**. copy-lint + copy-assertions ban **Select event**, **Select an event**, and **Select active event** across `apps/web` source (SQL `SELECT event_key` is a different token). | this commit |
| Home first-paint JS | Student Home statically loaded mentor/competition widgets, venue weather, YouTube/prediction ops, and Edit Home chrome. Extra widgets now load through `ExtraWidgetView`; Edit Home palette/library/docks/boards and the partner strip load through `next/dynamic` only while editing or the modal is open. Source lock: `home-split.test.ts`. Production `next build` this revision: Home first-load uncompressed **797 KB** (was 834 KB); unique on-disk **122 KB** (was 143 KB). route-bundle-stats still lists the YouTube/ops and Edit Home async chunks on `/dashboard` because `next/dynamic` stays in the page graph. | this commit |
| Marketing CSS on product | Root layout imported `marketing.css` + `marketing-v3.css` on every product route. Those sheets now load from `marketing-styles.ts` via SiteHeader. Playbook used leftover marketing `.button.primary`; it now uses product `Button`. css-integrity fails if `layout.tsx` imports the marketing sheets again. | this commit |
| Org-gate leftover Select | Exports, Prompts, Knowledge history, Audit, AI Runs, and Showcase still said **Select one and come back** / **Select the team**. Playbook empty said **Select a page**. Now **Choose your team** / **Choose a page**. copy-lint bans those phrases. | this commit |
| Home last snapshot | `/dashboard` was on the service-worker shell list with an `OfflineFeature` of `"dashboard"`, but no Home code called `getFeatureSnapshot` / `putFeatureSnapshot`. `loadHome` now paints the last IndexedDB copy first, fetches with an 8s timeout, persists on success, and keeps the cache when the network fails. `OfflineBanner` names Home; the header does not claim **Synced** from cache. Source lock: `dashboard-offline.test.ts`. Playwright offline Home not run this revision. | this commit |
| Assembly manual last snapshot | `/assembly-manual` already kept finished PDFs via `keepOfflineFile`, but the run list itself died without a network. The last overview now lives in IndexedDB (`feature: "assembly-manual"`), fetches abort at 8s, and `OfflineBanner` names Assembly manual. Source lock: `assembly-manual-offline.test.ts`. Playwright offline assembly-manual not run this revision. | this commit |
| Chemistry + Pick clock last snapshot | Event Day embeds Alliance chemistry and Pick clock, but those fetches went blank offline. Both now restore the last IndexedDB copy (`feature: "chemistry"` / `"pick-clock"`), abort at 8s, and stay on the last recommendation instead of the error shell. `/chemistry` and `/pick-clock` are on the service-worker shell list. Source locks: `chemistry-offline.test.ts`, `pick-clock-offline.test.ts`. Playwright offline chemistry/pick-clock not run this revision. | this commit |
| Select a run | Agent step log said **Select a run or start a new goal**. Now **Choose a run**. copy-lint + copy-assertions ban **Select a run**. Pick clock no longer says "the pick clock API" when a record fails. | this commit |
| One token family | Product CSS already consumed `--bg` / `--accent`, but `system.css` still declared a second `--soft-*` / `--app-*` palette and AppearanceRuntime still wrote those names inline. Alias declarations are gone. Team accent writes only `--accent` / `--accent-soft` / `--accent-ink` / `--accent-hover` and strips leftover alias properties from a previous visit. css-integrity fails if `--soft-*` / `--app-*` declarations return. `--m-*` stays for marketing. `soft-ui.css` class names remain on the product shell. | this commit |
| Alliance desk / Print Farm / Inventory last snapshot | `/alliance-selection-desk`, `/print-farm`, and `/inventory` timed out at 8s then painted the error shell. Each now restores the last IndexedDB copy (`feature: "alliance-desk"` / `"print-farm"` / `"inventory"`), keeps the cached board when the fetch fails, and shows `OfflineBanner`. Those routes are on the service-worker shell list. Source locks: `alliance-desk-offline.test.ts`, `print-farm-offline.test.ts`, `inventory-offline.test.ts`. Playwright offline alliance-desk / print-farm / inventory not run this revision. | this commit |
| Defense planner last snapshot | `/defense-planner` timed out at 8s then painted the error shell. The last IndexedDB copy (`feature: "defense-planner"`) now paints first; `/defense-planner` is on the service-worker shell list. Source lock: `defense-planner-offline.test.ts`. Playwright offline defense-planner not run this revision. | this commit |
| Select a preset | Display board empty said **Select a preset or custom widgets**. Now **Choose a preset or add widgets**. copy-lint + copy-assertions ban **Select a preset**. | this commit |
| Pick list / Dossier / Pit repair / Event-day plan last snapshot | Collaborative pick list, Dossier, Pit repair triage, and Event-day plan timed out at 8s then painted the error shell. Each now restores the last IndexedDB copy (`feature: "picklist-collab"` / `"dossier"` / `"pit-repair"` / `"event-day-plan"`). Those routes are on the service-worker shell list. Source locks: `picklist-collab-offline.test.ts`, `dossier-offline.test.ts`, `pit-repair-offline.test.ts`, `event-day-plan-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Field reset / Drive-team signals / Weigh-in / Match-delta / Match video index last snapshot | Five more Event Day boards timed out at 8s then painted the error shell. Each now restores the last IndexedDB copy (`feature: "field-reset"` / `"drive-signals"` / `"weigh-in"` / `"match-delta"` / `"match-video-index"`). Those routes are on the service-worker shell list. Source locks: `field-reset-offline.test.ts`, `drive-team-signals-offline.test.ts`, `robot-weigh-offline.test.ts`, `match-delta-offline.test.ts`, `match-video-index-offline.test.ts`. Playwright offline those five not run this revision. | this commit |
| Draft / Lineup last snapshot | `/strategy/draft` cleared the alliance board on a failed fetch (`setData(null)`). `/scouting/lineup` kept the coverage payload but `fetchFailed` still forced the error shell, including on the 15s poll. Both now restore the last IndexedDB copy (`feature: "draft"` / `"lineup"`), keep a painted board when the network fails, and show `OfflineBanner`. Those routes are on the service-worker shell list. `/strategy/draft` is labeled Alliance board, not Strategy. `/scouting/lineup` is labeled Lineup & coverage, not Scouting. Source locks: `draft-offline.test.ts`, `lineup-offline.test.ts`. Playwright offline draft/lineup not run this revision. | this commit |
| Match strategy cards / Match Copilot / Event readiness last snapshot | Three more Saturday boards timed out or painted the error shell when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "strategy-cards"` / `"match-copilot"` / `"event-readiness"`). Match Copilot and Event readiness fetches abort at 8s. Those routes are on the service-worker shell list. Source locks: `match-strategy-cards-offline.test.ts`, `match-copilot-offline.test.ts`, `event-readiness-offline.test.ts`. Playwright offline those three not run this revision. | this commit |
| Match video / Inspection / Inspection Copilot / FMEA last snapshot | Four pit and video boards timed out at 8s then painted the error shell. Each now restores the last IndexedDB copy (`feature: "match-video"` / `"inspection"` / `"inspection-copilot"` / `"fmea"`). `/video-analysis` stays labeled Video; `/video` is Match video. `/inspection-copilot` is labeled Inspection Copilot, not Inspection. Those routes are on the service-worker shell list. Source locks: `video-offline.test.ts`, `inspection-offline.test.ts`, `inspection-copilot-offline.test.ts`, `fmea-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Match Simulator / Pit Map / Pairwise / Drive-team tags last snapshot | Four more Event Day boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "match-sim"` / `"pit-map"` / `"pairwise"` / `"team-tags"`). Those routes are on the service-worker shell list. Source locks: `match-sim-offline.test.ts`, `pit-map-planner-offline.test.ts`, `pairwise-offline.test.ts`, `team-tags-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Shift balancer / Counter-book / Overnight Intel / Alliance-Partner Brief last snapshot | Four more Event Day boards timed out then painted the error shell. Each now restores the last IndexedDB copy (`feature: "shift-balancer"` / `"counter-book"` / `"overnight-intel"` / `"alliance-brief"`). Those routes are on the service-worker shell list. Source locks: `shift-balancer-offline.test.ts`, `counter-book-offline.test.ts`, `overnight-intel-offline.test.ts`, `alliance-partner-brief-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Pick-list Justifier / Opponent Watchlist / Alliance Sim / Pre-match briefing last snapshot | Four more Event Day boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "picklist-justifier"` / `"opponent-watchlist"` / `"alliance-sim"` / `"briefing"`). Those routes are on the service-worker shell list. Source locks: `picklist-justifier-offline.test.ts`, `opponent-watchlist-offline.test.ts`, `alliance-sim-offline.test.ts`, `briefing-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Award Tracker / EPA Trend Alerts / Rankings last snapshot | Award Tracker, EPA Trend Alerts, and Rankings went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "award-tracker"` / `"epa-trend"` / `"rankings"`). Rankings status is `ready`, not `live`. Award Tracker keys the cache by season. Those routes are on the service-worker shell list. Source locks: `award-tracker-offline.test.ts`, `epa-trend-alerts-offline.test.ts`, `rankings-offline.test.ts`. Playwright offline those three not run this revision. | this commit |
| Grant Report / Media Kit / Sponsor Wall / Sponsor Suite last snapshot | Four more Business boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "grant-report"` / `"media-kit"` / `"sponsor-wall"` / `"sponsor-suite"`). Grant Report, Media Kit, and Sponsor Suite key the cache by season. Those routes are on the service-worker shell list. Source locks: `grant-report-offline.test.ts`, `media-kit-offline.test.ts`, `sponsor-wall-offline.test.ts`, `sponsor-suite-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Outreach Calendar / Visit Invites / Judge-Pitch / Impact Essay last snapshot | Four more Business boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "outreach-calendar"` / `"visit-invites"` / `"judge-sim"` / `"impact-essay"`). Visit Invites status is `ready`, not `live`. Outreach Calendar, Judge-Pitch, and Impact Essay key the cache by season. Those routes are on the service-worker shell list. Source locks: `outreach-calendar-offline.test.ts`, `visit-invites-offline.test.ts`, `judge-sim-offline.test.ts`, `impact-essay-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Battery Rotation / Vendors / Season Report / Media last snapshot | Four more shop and ops boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "battery-rotation"` / `"vendors"` / `"season-report"` / `"media"`). Season Report and Media key the cache by season. Media 403 still shows the access-denied shell instead of a cached board. `/media-kit` stays labeled Media Kit; `/media` is Media. Those routes are on the service-worker shell list. Source locks: `battery-rotation-offline.test.ts`, `vendors-offline.test.ts`, `season-report-offline.test.ts`, `media-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Battery Health Forecast / Vendor Lead Times / Bin/Shelf Locator / Build Burndown last snapshot | Four more shop and ops boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "battery-health-forecast"` / `"vendor-lead-times"` / `"bin-shelf-locator"` / `"build-burndown"`). Build Burndown keys the cache by season. Those routes are on the service-worker shell list. Source locks: `battery-health-forecast-offline.test.ts`, `vendor-lead-times-offline.test.ts`, `bin-shelf-locator-offline.test.ts`, `build-burndown-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| CAD Change Radar / Code Deploy Log / Control Map / Cross-Team Scrims last snapshot | Four more shop and ops boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "cad-change-radar"` / `"code-deploy-log"` / `"control-map"` / `"cross-team-scrim"`). Code Deploy Log, Control Map, and Cross-Team Scrims key the cache by season. Control Map status is `ready`, not `live`. Those routes are on the service-worker shell list. Source locks: `cad-change-radar-offline.test.ts`, `code-deploy-log-offline.test.ts`, `control-map-offline.test.ts`, `cross-team-scrim-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Decision Search / Decision Log / Failure Patterns / Grant Eligibility last snapshot | Four more AI, build, and business boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "decision-search"` / `"decisions"` / `"failure-patterns"` / `"grant-eligibility-matcher"`). Decision Search, Decision Log, and Failure Patterns key the cache by season. Decision Search still shows the metered cutoff banner. Decision Log still embeds on `/ai`. `/decision-search` stays labeled Decision Search; `/decisions` is Decision Log. Those routes are on the service-worker shell list. Source locks: `decision-search-offline.test.ts`, `decisions-offline.test.ts`, `failure-patterns-offline.test.ts`, `grant-eligibility-matcher-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| My Hours / Knowledge-gap / Matching Gift / Onboarding Buddy last snapshot | Four more team and business boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "hours-self-view"` / `"knowledge-gap"` / `"matching-gift-finder"` / `"onboarding-buddy"`). Knowledge-gap keys the cache by season. Matching Gift still shows the usage cutoff banner. `/hours-self-view` stays labeled My Hours; `/hours` is Hours. Those routes are on the service-worker shell list. Source locks: `hours-self-view-offline.test.ts`, `knowledge-gap-offline.test.ts`, `matching-gift-finder-offline.test.ts`, `onboarding-buddy-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Risk Burndown / Spare Robot Kit / Sponsor Renewal ROI / Team Health last snapshot | Four more team, build, and business boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "risk-burndown"` / `"spare-robot-kit"` / `"sponsor-renewal-roi"` / `"team-health-dashboard"`). All four key the cache by season. Sponsor Renewal ROI still shows the usage cutoff banner. Those routes are on the service-worker shell list. Source locks: `risk-burndown-offline.test.ts`, `spare-robot-kit-offline.test.ts`, `sponsor-renewal-roi-offline.test.ts`, `team-health-dashboard-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Spare Forecast / Sketch-to-Brief / Scout-Assisted Count / Coverage Live last snapshot | Four more build and competition boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "spare-forecast"` / `"sketch-to-brief"` / `"scout-assisted-count"` / `"scout-coverage-live"`). Spare Forecast and Sketch-to-Brief key the cache by season. A failed Coverage Live refresh no longer blanks a painted cache via `setFetchFailed`. Those routes are on the service-worker shell list. Source locks: `spare-forecast-offline.test.ts`, `sketch-to-brief-offline.test.ts`, `scout-assisted-count-offline.test.ts`, `scout-coverage-live-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Field-Count Budget / Heat Signals / Data Impact / Disagreements last snapshot | Four more competition boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "scout-field-budget"` / `"scouting-heat-signals"` / `"scout-data-impact"` / `"scout-disagreements"`). Data Impact keys the cache by event. Disagreements keys the cache by season. 401/403 still show the sign-in shell instead of a cached board. `/scouting-heat-signals` is labeled Scouting Heat Signals, not Scouting. Those routes are on the service-worker shell list. Source locks: `scout-field-budget-offline.test.ts`, `scouting-heat-signals-offline.test.ts`, `scout-data-impact-offline.test.ts`, `scout-disagreements-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Scout Accuracy / Cross-Validation / Rule Impact / Season Planning last snapshot | Four more competition, build, and team boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "scout-accuracy"` / `"scout-crossval"` / `"rule-impact"` / `"season-planning-workspace"`). Scout Accuracy and Cross-Validation key the cache by event. Rule Impact and Season Planning key the cache by season. 401/403 on Accuracy and Cross-Validation still show the error/sign-in shell instead of a cached board. Those routes are on the service-worker shell list. Source locks: `scout-accuracy-offline.test.ts`, `scout-crossval-offline.test.ts`, `rule-impact-offline.test.ts`, `season-planning-workspace-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Product chrome CSS on marketing | Root layout imported leftover `soft-ui.css` + `styles.css` on every route, including landing. Those sheets now load from `product-styles.ts` (soft-ui then styles) via AppShell (`next/dynamic`, SSR) plus sign-in, pit TV, the offline boot, and showcase present. `system.css` stays on the root for tokens and a box-sizing/margin reset. css-integrity fails if `layout.tsx` imports the product chrome sheets again or if ThemeProvider statically imports AppShell. Production `next build` prerendered `/` HTML does not link the 163 KB leftover soft-ui chunk or the 97 KB leftover styles chunk; `/team` and `/video` do. | this commit |
| EmptyState related-strip leftovers | Team Data, Schedule, Fundraisers, Impact, Orders, Awards, and Grant writing empty/setup cards nested the header related strip (or a Next-actions wall) inside EmptyState. Related stays in the header; EmptyState keeps one primary; next-actions paint on ready. Visit Invites setup no longer repeats Next actions on the empty card. `empty-state-primary.test.ts` fails if a `*Related` component returns inside EmptyState. | this commit |
| Orders setup Next-actions leftover | `/orders` still painted the Next-actions panel on `setup_required` beside the one EmptyState primary. Setup now keeps that primary only. Writer setup had the same leftover wall; it now keeps one EmptyState primary. `empty-state-primary.test.ts` fails if a `*NextActions` component returns in a `setup_required` ternary. | this commit |
| Fundraisers / Impact / Orders / Grant writing last snapshot | Four Business boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "fundraisers"` / `"impact"` / `"orders"` / `"grants-writing"`). Fundraisers, Impact, and Grant writing key the cache by season. Those routes are on the service-worker shell list. `/impact` is labeled Community Impact, not Impact Essay. `/team/grants` is labeled Grant writing, not Team. Source locks: `fundraisers-offline.test.ts`, `impact-offline.test.ts`, `orders-offline.test.ts`, `grants-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Practice / Manufacturing / Attendance / Kickoff last snapshot | Four Team/Build boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "practice"` / `"manufacturing"` / `"attendance"` / `"kickoff"`). Attendance keys the cache by season. Those routes are on the service-worker shell list. Practice, Attendance, and Kickoff setup no longer paint a Next-actions wall beside the one primary. Attendance error no longer adds a second Choose your team sibling. Source locks: `practice-offline.test.ts`, `manufacturing-offline.test.ts`, `attendance-offline.test.ts`, `kickoff-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Match debrief / Ranking projection / Whiteboard last snapshot | Three Competition boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "match-debrief"` / `"ranking-projection"` / `"whiteboard"`). Match debrief keys the cache by season. Those routes are on the service-worker shell list. Whiteboard setup is one EmptyState primary. Ranking projection setup keeps zero primaries (the API message already says what is missing). Source locks: `match-debrief-offline.test.ts`, `ranking-projection-offline.test.ts`, `whiteboard-offline.test.ts`. Playwright offline those three not run this revision. | this commit |
| Robot / My Kit / Part requests / Recognition last snapshot | Four more student boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "robot"` / `"my-kit"` / `"part-requests"` / `"recognition"`). Part requests and Recognition key the cache by season. `/robot` is labeled Robot; `/robot-weigh-in` stays Robot weigh-in. Part requests error no longer paints Choose your team on a network failure. Those routes are on the service-worker shell list. Source locks: `robot-offline.test.ts`, `my-kit-offline.test.ts`, `part-requests-offline.test.ts`, `recognition-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Subteams / Budget / Sponsorship / Safety last snapshot | Four more Team and Business boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "subteams"` / `"budget"` / `"sponsorship"` / `"safety"`). Budget and Sponsorship key the cache by season. Budget 401/403 still shows the mentors-only shell instead of a cached board. `/sponsorship` is labeled Sponsorship; `/sponsor-suite` stays Sponsor Suite. Subteams and Budget error no longer paint Choose your team on a network failure. Those routes are on the service-worker shell list. Source locks: `subteams-offline.test.ts`, `budget-offline.test.ts`, `sponsorship-offline.test.ts`, `safety-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Writer / Learning / Training last snapshot | Three more student boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "writer"` / `"learning"` / `"training"`). Writer keys the cache by season. Writer, Learning, and Training 401/403 still show the error/sign-in shell instead of a cached board. Learning setup keeps one **Choose your team** primary; Gearbox / Power budget / Shooter table stay in the header related strip. Those routes are on the service-worker shell list. Source locks: `writer-offline.test.ts`, `learning-offline.test.ts`, `training-offline.test.ts`. Playwright offline those three not run this revision. | this commit |
| Tool checkout / Goals last snapshot | Two more Team boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "tool-checkout"` / `"goals"`). Goals keys the cache by season. Goals setup no longer paints a Next-actions wall. `/tool-checkout` and `/goals` are on the service-worker shell list. Source locks: `tool-checkout-offline.test.ts`, `goals-offline.test.ts`. Playwright offline those two not run this revision. | this commit |
| CAD Vault / CAD Learn student shells | `/cad-vault` empty/setup keep one EmptyState primary (Link a CAD document / Choose your team). Related (Learn CAD · Assembly manual · CAD workbench) stays in the header. Next-actions paint only on ready. Vault cards lead with the title and Open in Onshape / Open in Fusion — STL triangle counts are a face count, not kilograms. Fusion share URLs (`a360.co`, Autodesk hub) paste beside Onshape. `/cad-learn` uses PageHeader + the same related pattern; grader copy says how heavy / how hard it is to spin instead of MOI. Stuck no longer stacks sibling Vantage CTAs. Build CAD tab related strip is CAD vault · Learn CAD · Assembly manual (not Analyze). Last-snapshot wiring unchanged. | this commit |
| Season Costs / CAD Review Queue / Duties / Announcements last snapshot | Four more student boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "costs"` / `"cad-review-queue"` / `"duties"` / `"announcements"`). Season Costs and CAD Review Queue key the cache by season. Costs setup no longer paints a Next-actions wall. Duties setup keeps one **Choose your team** primary; My Day stays in the header. Announcements and Duties error no longer paint Choose your team on a network failure. `/cad-review-queue` is labeled CAD Review Queue, not CAD Change Radar. Those routes are on the service-worker shell list. Source locks: `costs-offline.test.ts`, `cad-review-queue-offline.test.ts`, `duties-offline.test.ts`, `announcements-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Localhost Google after Vercel env | `next dev` used production `BETTER_AUTH_URL` after a Vercel env pull, so Google callbacks left localhost. Local runtime now keeps `http://localhost:3001` unless `BETTER_AUTH_URL_LOCAL` says otherwise. Preview Vercel builds skip unless the branch is `main`. | this commit |
| Risks / Subsystem Sign-off / Prototypes / Equipment Maintenance last snapshot | Four more student boards went blank when venue Wi-Fi dropped. Each now restores the last IndexedDB copy (`feature: "risks"` / `"subsystem-signoff"` / `"prototype-tracker"` / `"equipment-maintenance"`). Risks, Subsystem Sign-off, and Prototypes key the cache by season. Risks, Sign-off, and Prototypes setup no longer paint a Next-actions wall. `/risks` is labeled Risk Register; `/risk-burndown` stays Risk-Register Burndown. Those routes are on the service-worker shell list. Source locks: `risks-offline.test.ts`, `subsystem-signoff-offline.test.ts`, `prototype-tracker-offline.test.ts`, `equipment-maintenance-offline.test.ts`. Playwright offline those four not run this revision. | this commit |
| Leftover Files / tags / cutoff student chrome | Open PRs #2–#40 already own last-snapshot gold and skip-list leftover. Files **not** in those PRs: Files Grid/List still used a raw `app-button` class; Drive-team tags next-action Open and Remove did too, and the board still said **Pairwise 2.0**; usage-cutoff banner said **Hard cut-off** on a native button; share-link defaulted to `className="app-button secondary"`; Account profile printed **SMS OTP / Twilio env** on native Save / Sign out; Help search said **subscription bridge**. Copy now uses `Button`, **At the limit**, **Tag robots as you watch**, **Needs setup**, and text-message phrasing. Source lock: `files-tags-cutoff-chrome.test.ts`. Fixture Playwright `files-tags-cutoff-chrome.spec.ts` **1 passed / 1** (9.7s) on :3310 + local `vantage_ci` (127.0.0.1, not production DATABASE_*). No season ±3 claim — fixture MAE **89.7** / band ±90 unchanged. | this commit |
| Research / Overnight brief student shell | `/intel` and `/overnight-intel` still used TBA/Statbotics, event keys, metered, org, and Next-actions on empty. Research is now a look-up: one empty card, related in the header, Next-actions only after a team is open, our scouting from real `match_scout_entries` / `pit_scout_entries`, pick save uses the active event. Overnight brief: one setup/empty primary, related in the header, Next-actions only on ready. Hub labels are **Research** and **Overnight brief**. Intel search stays uncached (multi-fetch). Fixture Playwright `intel-shell.spec.ts` **2 passed / 2** (8.2s) on :3310: h1 **Research** / **Overnight brief**, related strip visible, no Setup steps, no Statbotics / global team index / Grounding path. Did not wrap freebuff. Did not deploy. Master brief not complete. | this commit |
| Leftover extra buttons on student leaves | Nested Next-actions walls after EmptyState on Tuning Autopilot, Rule Impact, Spare Forecast, Control Map, Pit command, Match notes, Grant writing empty drafts, and the rest of the leftover Shell clones. Setup/empty keep one EmptyState primary; related stays in the header; Next-actions paint only on ready. Batteries empty no longer nests the related strip; setup keeps one **Choose your team** primary. QR handoff empty and Scouting no-forms keep one primary. Student copy no longer says **Select a repository / grant / expense**, **pick your organization**, or **your organization team number**. `empty-state-primary.test.ts` **8 passed / 8**. copy-lint **3 passed / 3** (3.19s) including the new Select-a / organization-picker bans. `tsx-line-limit.test.ts` **1 passed / 1**. Targeted vitest **84 passed / 84** (11 files). `npm run typecheck` all workspaces `--noEmit` this revision. Fixture Playwright `batteries-shell.spec.ts` **1 passed / 1** (3.7s) on :3310: empty fleet keeps one **Add a battery** primary; related stays in the Build hub strip; Next-actions are not on that empty card. Intel / Overnight Intel / Admin / partner-placements / Todos still skipped — open PRs. | this commit |
| Packing / Season Calendar / Team Calendar leftover blanking | Those three already cached, but a failed refresh still painted the error shell via `fetchFailed \|\| !view` (and IndexedDB `put` throwing after a live GET could set `fetchFailed`). Each now keeps a painted board (`if (!view)`), reads cache with `orgId \|\| "_"`, aborts at 8s, drops the board on 401/403, and persists from a helper in the client. Inspection, unused Match video (`video-client.tsx`), Match video re-scout, and Print Farm use the same error-shell rule. Source locks: `packing-offline.test.ts`, `calendar-offline.test.ts`, `team-calendar-offline.test.ts`, `inspection-offline.test.ts`, `video-offline.test.ts`, `video-rescout-offline.test.ts`, `print-farm-offline.test.ts`. Fixture Playwright `offline-competition.spec.ts` **6 passed / 6** (5.2s) on :3310: Season calendar, Packing, Team calendar, Inspection, Match video, Print Farm still render after `context.setOffline`. No season ±3 claim — fixture MAE **89.7** / band ±90 unchanged. | this commit |
| Notifications / Forms / Incidents / Season roles / Parts catalog / Parts Relay last snapshot | Those six student leaves were still blanking (or losing the in-stock overlay) when venue Wi-Fi dropped, and they were **not** in open PRs #2–#13. Each now restores the last IndexedDB copy (`feature: "notifications"` / `"forms"` / `"incidents"` / `"roles"` / `"parts-catalog"` / `"parts-relay"`). Notifications keys the cache by inbox filter. Incidents and Season roles key the cache by season. Notifications 401/403 drops the painted inbox. Inbox Next-actions paint only on a ready view; error EmptyState keeps one primary. Incidents and Roles setup keep one **Choose your team** primary. Those routes are on the service-worker shell list. Source locks: `notifications-offline.test.ts`, `forms-offline.test.ts`, `incidents-offline.test.ts`, `roles-offline.test.ts`, `parts-catalog-offline.test.ts`, `parts-relay-offline.test.ts`. Fixture Playwright `offline-student-inbox.spec.ts` **6 passed / 6** (6.1s) on :3310: Notifications, Forms, Safety Incident Log, Season roles, Parts catalog, Parts Relay still render after `context.setOffline`. `npm run typecheck` clean across workspaces. Targeted vitest **10 files passed / 10**, **22 passed / 22** (2.93s). No season ±3 claim — fixture MAE **89.7** / band ±90 unchanged. | this commit |
| Connectors catalog + Account API student copy | `/connectors` and `/api/account` still said **Onshape OAuth**, **Email (Resend)**, `ONSHAPE_OAUTH_*`, `RESEND_*`, and **Select a workspace**. That copy was **not** in PRs #26/#27 (those own CAD Connections / CAD setup). Members now get mentor-ask cards: no OAuth, no env-var list, no callback URL. Owners/admins still see the operator catalog (env names + register URL). Account Onshape/GitHub/Google/TBA/Discord/email/phone details match that voice. Account notifications no longer say Resend. Source locks: `catalog.test.ts` student suite, `account-api-related.test.ts`. This revision: `npm run typecheck` clean across workspaces. Targeted vitest **72 passed / 72** (5 files, 2.93s) including catalog student suite, account-api-related, platform-connector-contract, pairing-setup, copy-lint. Fixture Playwright `connector-student-copy.spec.ts` **1 passed / 1** (1.7s) on `:3310`: heading Connectors, zero `Onshape OAuth`, zero `Email (Resend)`. Help CAD “Onshape OAuth” stays in PR #27. | this commit |
| Leftover Pick a team chrome | Open PRs #2–#32 already own last-snapshot gold and Join-or-select (`cad-vault/view.ts` #11, reimbursements + command #12). Remaining last-snapshot clients are skip-list only. Files **not** in those PRs still said **Pick a team** on the All-apps team chip, Chat empty, onboarding GitHub hint (**OAuth or encrypted PAT**), and related/next-action details (Alumni, Bin/Shelf, CAD radar, Inspection Copilot, Logistics, Matching Gift, Rule Impact, Schedule, scout accuracy/coverage/crossval/impact/disagreements, QR handoff, Sponsor Renewal ROI, Team Data, Tuning Autopilot, Visit invites). Each now says **Choose your team**. Source lock: `leftover-pick-a-team.test.ts` **3 passed / 3**. Fixture Playwright `choose-your-team-leftover.spec.ts` **1 passed / 1** (1.9s) on :3310. `npm run typecheck` all workspaces `--noEmit`. Targeted vitest **93 passed / 93** (12 files, 3.15s). Duties still say **Pick a teammate**. copy-lint does not globally ban **Pick a team** yet — #4/#12 still own other occurrences. | this commit |
| Leftover Join or pick a team chrome | Open PRs #2–#33 already own last-snapshot gold, Join-or-select (`cad-vault/view.ts` #11, reimbursements + command #12), leftover **Pick a team** titles (#4/#12/#33), GitHub OAuth/PAT (#12), and connector student copy (#30). Remaining last-snapshot clients are skip-list only. Files **not** in those PRs still said **Join or pick a team** on Retro, Build Burndown, Cross-team scrim, Pair VS Code, and Team admin setup; Ask AI no-org next-action said **per org — pick a team first**; Team admin helper said **After you pick a team**; scout attach/voice said **pick a team** when they meant the FRC robot; form preview photos said **stored per organization**. Each org-picker now says **Choose your team**. Scout refuse copy says **choose which robot you are scouting**. Preview hint says **stored with this team**. Source lock: `leftover-join-or-pick.test.ts` **5 passed / 5**. Fixture Playwright `join-or-pick-leftover.spec.ts` **1 passed / 1** (29.6s) on :3310 (local `vantage_ci`). `npm run typecheck` all workspaces `--noEmit`. Targeted vitest **93 passed / 93** (9 files, 2.94s). Duties still say **Pick a teammate**. copy-lint does not globally ban **Pick a team** — #4/#12/#33 still own other occurrences. The All-apps chip still says **Pick a team** on main (PR #33). | this commit |
| Leftover Ask AI setup primaries | Open PRs #2–#34 already own last-snapshot gold, Join-or-pick, leftover **Pick a team** titles, and connector copy. Remaining last-snapshot clients are skip-list or multi-fetch (Memory / policy / usage not golded). Files **not** in those PRs still painted extra EmptyState buttons on Chat / Chat limits / Usage no-org cards; next-actions said **pick a team first** / **per org**; Usage eyebrow was **AI / USAGE & ACTIVITY**; Memory leaked `docs/DREAMING.md` and “injected into prompts”; Chat limits said **API keys** and **Organization hard limits**. Each no-org card now has one **Choose your team** primary. Empty/setup Ask AI shells keep one EmptyState primary; next-actions paint on ready. Source lock: `leftover-ask-ai-setup.test.ts` **3 passed / 3**. Fixture Playwright `ask-ai-setup-primary.spec.ts` **1 passed / 1** (6.6s) on :3310 (local `vantage_ci` not required — fixture session). `npm run typecheck --workspace=@vantage/web`. Targeted vitest **35 passed / 35** (5 files, 2.94s). | this commit |
| Alliance org-bound leftover | Open PRs #2–#37 already own last-snapshot gold and skip-list leftover. Files **not** in those PRs: Alliance board / Draft still said **org-bound** / **mentor token**; Pick desk / Pick clock / Strategy next-actions still said **team_event_metrics** / **membership-bound**; Team admin hub said **token hard limits** / **API-key powers**; Help articles linked **Permalink**; Attendance empty roster said **workspace members**. Mentor share copy now says the link only opens for this team. Source lock: `leftover-org-bound.test.ts`. Fixture Playwright `leftover-org-bound.spec.ts` **1 passed / 1** (11.3s) on :3310 + local `vantage_ci`. No season ±3 claim — fixture MAE **89.7** / band ±90 unchanged. | this commit |
| Scout membership-bound leftover | Open PRs #2–#38 already own last-snapshot gold and skip-list leftover. Files **not** in those PRs: Scout Accuracy KPI still said **membership-bound** / **pick-desk ready**; Data impact tile unit said **membership-bound**; Lineup header said **membership IDs**; Lineup double-scouted copy and related next-actions said **membership-bound**; Match video next-action said **membership-bound match entries**. Copy now says signed-in scouts / this team's match entries. Source lock: `leftover-membership-bound.test.ts`. Fixture Playwright `leftover-membership-bound.spec.ts` **1 passed / 1** (9.5s) on :3310 + local `vantage_ci`. No season ±3 claim — fixture MAE **89.7** / band ±90 unchanged. | this commit |
| Leftover student engineering copy | Open PRs #2–#39 already own last-snapshot gold and skip-list leftover. Files **not** in those PRs: CAD viewport still said **Onshape setup required** / **shaded-view PNG**; Files said **object storage** / **Checksumming** / **your node**; Why panel printed **setup required**; Print Farm listed **OctoPrint / G-code / slicer**; Event Day + Logistics next-action Open used a raw `app-button` class; sponsored-AI banner said **weighted round-robin**. Copy now says Connect Onshape / No picture yet, hosted storage, Needs setup, teammate-reported printer status, and `Button`. Source lock: `leftover-student-copy.test.ts`. Fixture Playwright `leftover-student-copy.spec.ts` **1 passed / 1** (9.0s) on :3310 + local `vantage_ci` (127.0.0.1, not production DATABASE_*). No season ±3 claim — fixture MAE **89.7** / band ±90 unchanged. | this commit |
| CAD setup wizard + pair student copy | `/cad/setup` on main still dumped the vantage-cad CLI install block, `ONSHAPE_OAUTH_*`, Vercel, mock/CI, and an AI-brain billing path. The wizard is now link-first: Onshape in the browser or Fusion on this computer; Ask a mentor when Onshape is not ready; Connect Onshape runs in the wizard. No CLI dump, env-var names, or billing-path radios. Last IndexedDB copy (`feature: "cad-setup"`) paints first; fetches abort at 8s. `/cad/setup` is on the service-worker shell list. `/cad/pair` dropped OAuth / CLI / Vercel / terminal copy. Account Onshape next-actions and Help CAD tips no longer name `ONSHAPE_OAUTH_*`. Source locks: `cad-setup-offline.test.ts`, `cad-setup-copy.test.ts`. Shared `onshape-setup-strings.ts` is **not** rewritten here — that copy is in PR #26 (CAD connections). This revision: `npm run typecheck` clean across workspaces. Targeted vitest **8 files / 33 passed** (3.11s). Fixture Playwright `cad-setup-shell.spec.ts` **2 passed / 2** (5.2s) on :3310 + fixture cookie: page headings, no leftover `VANTAGE /`, `ONSHAPE_OAUTH_CLIENT_ID`, `vantage-cad`, Copy commands, AI brain, or pair-page OAuth/Vercel. Playwright offline IndexedDB restore for CAD setup not run this revision. | this commit |
| Leftover Setup required / VANTAGE Forms student chrome | Remaining open PRs (#2, #5–#7, #15–#26, #28–#29, #31–#32, #36–#37) already own last-snapshot gold and skip-list leftover. Files **not** in those PRs: Consent still said **VANTAGE / FORMS** / **Setup required** and used native `primary-action` buttons; Match Video Index, Event-Day Plan, Match checklist, Tool checkout, and Match Simulator still badged **Setup required**; Match Simulator setup still named **TBA/Statbotics EPA**. Copy now uses **Needs setup**, **FORMS**, `Button`, and **Sync rankings**. Source lock: `leftover-setup-vantage-chrome.test.ts`. Fixture Playwright `leftover-setup-vantage-chrome.spec.ts` **1 passed / 1** (29.8s) on :3310 + local `vantage_ci` (127.0.0.1, not production DATABASE_*). `npm run typecheck --workspace=@vantage/web` clean. Targeted vitest **6 files / 41 passed** (3.41s). No season ±3 claim — fixture MAE **89.7** / band ±90 unchanged. | this commit |
| Student-week hubs + leftover chrome on latest main | Leftover chrome from `e5f4af37` was not on `origin/main` (`dfaa1d1f`). Cherry-picked, then Scouting / Strategy / Pick desk / Pick clock / Match video / Business / Orders / Impact / Grants / CAD setup / Ask AI Chat speak **Needs setup** and drop TBA/Statbotics from student chrome. Chat empty keeps one **New private chat** primary (related in the header; no Next-actions neighbor). Match Simulator and Match checklist last snapshot use `if (!view)` and cache `orgId \|\| "_"`. Source locks: `leftover-setup-vantage-chrome.test.ts`, `student-week-hubs-chrome.test.ts`, `match-sim-offline.test.ts`, `match-checklist-offline.test.ts`. This revision: `npm run typecheck` clean across workspaces. Targeted vitest **17 files / 147 passed** (1.02s). Fixture Playwright leftover + student-week **2 passed / 2** (54.5s) on :3410 + local `vantage_ci` (127.0.0.1, not production DATABASE_*). No season ±3 claim — fixture MAE **89.7** / band ±90 unchanged. | this commit |
| Student-week Event day / Hours / Pick desk | Second student-week slice on one branch: Event Day related strip is Packing · Match checklist · Tool checkout · Inspection (phone-first, **Needs setup**, last snapshot if the board blanks). My Hours Clock in/out on that board from Home/My Day. Pick desk featured under Strategy: rank/pick/lock, one **Lock this list** primary, gold last-snapshot `feature: "pick-desk"`. No invented hour totals or EPA/TBA in this slice’s student chrome. Source lock: `student-week-event-slice.test.ts`. Proof this session: `npm run typecheck` clean; targeted vitest **15 files / 166 passed**; fixture Playwright `student-week-event-slice.spec.ts` **1 passed (18.7s)** on `:3410` + local `vantage_ci` (127.0.0.1, not production DATABASE_*). Master brief is **not** done. | this commit |
| Marketing / waitlist / sign-in / onboarding | Public story: one login a student can use without help; hero has one **Join the waitlist** primary. Waitlist form works (`GET` ready / `POST` ok) and paints a calm empty state when the marketing database is missing — never **Setup required**. Sign-in: Google + email code, closed-membership waitlist copy, **Needs setup**. Onboarding first-run **Open Home** / **Choose your team**. Phone confirm toast is **Phone number confirmed.** (deployment-level, not per-team). Folded Pick desk copy-lint leftover. No open PRs to fix (`gh pr list` empty). Source locks: `marketing-chrome.test.ts`, waitlist / sign-in / onboarding unit tests. `npm run typecheck` clean. Targeted vitest **13 files / 117 passed**. Fixture Playwright **11 passed / 11** on `:3320` + local `vantage_ci` (127.0.0.1, not production DATABASE_*). Master brief is **not** done. | this commit |
| Team hub People / invite / closed membership | Team › People **Invites** (`/team/admin`) and featured **Season roles**. Closed membership: exact-email invite; waitlist for everyone else. Empty/setup one primary; **Needs setup**. Last snapshot `feature: "team-admin"` (`if (!view)`). Union `/team/admin` on feature-cache / shell-routes / `sw.js`. GitHub token copy, no OAuth chrome. Recovered after `bc-8099c87e`. Source locks: `student-week-team-hub.test.ts`, `team-admin-offline.test.ts`. `tsc --noEmit` clean. Targeted vitest **11 files / 84 passed** (0.93s). Fixture Playwright `student-week-team-hub.spec.ts` **1 passed / 1** (17.2s) on `:3435` + local `vantage_ci` (127.0.0.1, not production DATABASE_*). Master brief is **not** done. | this commit |
| CAD connectors GUI | `/connectors` and `/cad/connections` let a student connect Onshape, see status, disconnect, and pick a document from the list or paste a link — no client secret / OAuth / env names. Pair VS Code / Pair this computer keep fixture headings and picker label **Team**. Gold last-snapshot if those boards can blank; one primary; **Choose your team** on 401/403. Skipped `cad-client.tsx` / vault / learn / `setup-client.tsx` (Onshape viewport sibling). Source lock: `cad-connectors-gui.test.ts`. This revision: `npm run typecheck` clean. Targeted vitest **8 files / 79 passed** (0.43s). Fixture Playwright **5 passed / 5** (6.8s) on `:3422` + local `vantage_ci` (127.0.0.1, not production DATABASE_*). Master brief is **not** done. | this commit |
| Business / funding GUI | Student/mentor path: `/business` Overview · Money · Sponsors · Grants, `/budget`, grant calendar. Header related strip is **Sponsors · Grants · Budget** (hidden when `sponsorsAllowed === false` drops Sponsors). **Needs setup**, last snapshot (`feature: "business"` / `"season-finance"` / `"partner-placements"`), **Choose your team** on 401/403. Working funds stay **—** until a budget or recorded cash exists — no invented `$0`. Payment field placeholder is `https://` (no Stripe/OAuth). Skipped Event day, marketing, CAD viewport, Team people. Source lock: `business-funding-gui.test.ts`. `npm run typecheck` clean. Targeted vitest **12 files / 67 passed** (5.92s). Fixture Playwright `business-funding-gui.spec.ts` **1 passed / 1** (5.7s) on `:3422` + local `vantage_ci` (127.0.0.1, not production DATABASE_*). Master brief is **not** done. Task 14 stays in progress. | this commit |
| Offline phone student boards (Hours / Batteries / FMEA / Event readiness) | Those four still blanked a painted board via `fetchFailed \|\| !view` (Hours, Batteries) or `fetchFailed \|\| view == null` (FMEA, Event readiness). Each now keeps the last IndexedDB copy (`if (!view)`), reads `orgId \|\| "_"`, aborts at 8s, and drops the board only on 401/403. Skipped kiosk / admin / marketing / sign-in / intel multi-fetch / cad-client / knowledge / code / chat multi-fetch / display / audit / posture / sponsors / finance. Stayed off Event day packing and the CAD viewport. Union `feature-cache` / `shell-routes` / `sw.js` not shortened — `/team/admin` cache keys from Team hub stay. Source locks: `hours-offline.test.ts`, `batteries-offline.test.ts`, `fmea-offline.test.ts`, `event-readiness-offline.test.ts`. Incoming branch: `npm run typecheck --workspace=@vantage/web` clean. Targeted vitest **4 files / 4 passed** (224ms). Fixture Playwright `offline-phone-student.spec.ts` **4 passed / 4** (8.0s) on `:3510` + local `vantage_ci`. Merge session: `npm run typecheck --workspace=@vantage/web` clean. Targeted vitest **5 files / 10 passed** (261ms) including shell-routes `/team/admin`. Fixture Playwright `offline-phone-student.spec.ts` **4 passed / 4** (28.3s) on `:3518` + local `vantage_ci` (127.0.0.1, not production DATABASE_*): live mock paints Hours / Batteries / FMEA / Event readiness, then 500 on refresh; board heading and keep-text stay with “last copy on this device”. `/batteries` and `/fmea` redirect to `/build?tab=…` so the page h1 is **Build**; the spec passes `orgId` so the hub mounts the client and asserts the visible board heading (Add a battery / No failures logged yet), not a unique h1. Master brief is **not** done. | this commit |
| Home + remaining Competition student chrome | Home widgets, Strategy boards besides Pick desk, Match video, Event day, and Chat. Empty widgets never paint TBA/EPA payload messages. Draft / Pick clock / Chemistry empty is **Sync Team Data** (one primary). Alliance desk ranking flags, not TBA/EPA badges. Match video related **Event day · Match notes · Match video**; `/video` related Scouting · Event day. Chat empty one **New private chat**. Gold last-snapshot Strategy / Chemistry / Pick clock / Draft. Source lock: `home-competition-polish.test.ts`. Proof this session recorded after targeted vitest + fixture Playwright. Did not redo packing / hours / pick-desk / team-admin / business / Onshape. Union cache lists not shortened. No season ±3 claim — fixture MAE **89.7** / band ±90 unchanged. Master brief is **not** done. | this commit |
| Match prediction honest MAE copy | Remaining-student-boards died unreachable twice; this retry ships **one** board. Next-match score line uses `typicalScoreErrorCopy` so the band cannot drift back to a placeholder 8. Fixture MAE **89.7** → `FIXTURE_ERROR_BAND` **90**, copy **typical error ±90 (last measured set)**. Empty prediction / no-schedule messages are student words (Team Data, Choose your team) — no EPA / TBA / Statbotics, no invented %. Skip reasons say missing ratings, not EPA/OPR. Briefing setup is **Needs setup**. Source lock: `prediction-honest.test.ts`. `npm run typecheck --workspace=@vantage/web` clean. Targeted vitest **6 files / 32 passed** (1.07s). Fixture Playwright `prediction-honest.spec.ts` not run this revision. Did not redo Research, Pit, inbox, Safety, Parents, Impact, Home widgets, Strategy boards, Match Simulator, or packing. **Not a season ±3 claim.** Master brief is **not** done. | this commit |
| Student-week GUI Playwright (fixture cookie) | `student-week-gui.spec.ts` walks Home What to do now, My Day, Event day packing, Hours, Pick desk, Team invites, Business sponsors, CAD Edit in Onshape/Fusion, connectors pair, waitlist, and sign-in. Chromium already installed (`npx playwright install chromium` exit 0). Fixture cookie is not Better Auth (`Better Auth: User not found`): Home CTA `/workspace` bounces Home via `/signin`; Hours error shell is Retry, not Clock in. Spec accepts that bounce and Retry / load-failure. First run `:3478` **10 passed / 2 failed** (40.5s). After locator fix, those two **2 passed / 2** (12.4s) on `:3480` + local `vantage_ci` (127.0.0.1, not production DATABASE_*). No Vercel. Master brief is **not** done. | this commit |
| Season Goals / standup / meeting agenda student chrome | `/goals-tracker`, `/standup-digest`, and `/meeting-autopilot` still said **Setup required**; Meeting heading was **Meeting-agenda autopilot**. Now **Needs setup**, one EmptyState primary, related in the header, Next-actions on ready, gold last-snapshot `if (!view)`. Source lock: `season-goals-standup-chrome.test.ts`. Stayed off leftover-product, remaining-boards-2, Hours fixture redo, relays, and code-vs-match. Union cache lists not shortened. Incoming branch: `npm run typecheck --workspace=@vantage/web` clean; targeted vitest **5 files / 12 passed** (281ms); fixture Playwright **1 passed / 1** (6.8s) on `:3482`. Merge session: typecheck clean; targeted vitest **5 files / 12 passed** (291ms); fixture Playwright `season-goals-standup.spec.ts` **1 passed / 1** (20.7s) on `:3491` + local `vantage_ci` (127.0.0.1, not production DATABASE_*). Did not wrap freebuff. Did not deploy. Master brief is **not** done. | this commit |
| Leftover-product student chrome | Recovered after `bc-08d86965` timed out (`cursor/leftover-product-c0b5` never pushed). Billing/credits, Files/media, Help / What’s new, Print farm, Playbook, Inspection, remaining scout boards: **Needs setup**, one primary, related in the header, Next-actions on ready. Spend tiles **—** until a ledger exists (meteredAI unchanged). Print farm not 3D Print Farm. Playbook last snapshot `feature: "knowledge"` (`if (!view)`). Inspection without copilot jargon. Copy-lint Setup required / VANTAGE / TBA on student-week boards only. Web performance skipped. Source lock: `leftover-product-chrome.test.ts`. `npm run typecheck --workspace=@vantage/web` clean. Targeted vitest **11 files / 66 passed** (3.48s). Fixture Playwright leftover-product + print-farm **2 passed / 2** (9.8s) on `:3535` + local `vantage_ci` (127.0.0.1, not production DATABASE_*). Merged remaining-boards-2 already on main; did not redo it. Stayed off Hours, relays, and code-vs-match. Did not wrap freebuff. Did not deploy. Master brief is **not** done. | this commit |
| Code vs match / Software versions student chrome | `/code-perf` heading is **Code vs match**, not Code-vs-Match Detective. `/software-versions` badges **Needs setup**. One EmptyState primary, related in the header, Next-actions on ready, gold last-snapshot `if (!view)` (`feature: "code-perf"` / `"software-versions"`). `code-client.tsx` skip-list — no fake last-snapshot. Source lock: `code-vs-match-chrome.test.ts`. Incoming branch: `npm run typecheck --workspace=@vantage/web` clean; targeted vitest **5 files / 19 passed** (307ms); fixture Playwright `code-vs-match.spec.ts` **1 passed / 1** (11.5s) on `:3544` + local `vantage_ci`. Merge session: typecheck clean; targeted vitest **5 files / 19 passed** (311ms); leftover chrome + copy-lint still green (**2 files / 7 passed**); fixture Playwright `code-vs-match.spec.ts` **1 passed / 1** (6.1s) on `:3555` + local `vantage_ci` (127.0.0.1, not production DATABASE_*). Leftover-product already on main — not redone; stayed off leftover-product `bc-68fba019` chrome. Union cache lists not shortened (`/team/knowledge` + Playbook stay). Did not wrap freebuff. Did not deploy. Standing brief stays open. Master brief is **not** done. | this commit |

---

## Task progress

| # | Task | Status | Verification |
|---|---|---|---|
| 1 | Land / baseline / competitive notes | done | this file + `COMPETITIVE_NOTES.md`; latest full suite **883 files passed, 2 skipped (885), 8,354 passed, 10 skipped** (53.88s) |
| 2 | One design system | in progress | tokens in `system.css`; Button emits `.app-button`. **Root layout no longer imports `marketing.css` / `marketing-v3.css`.** Those sheets load from `components/marketing/marketing-styles.ts` via the marketing SiteHeader, so product routes do not pull the landing copper/paper sheet. css-integrity fails if `app/layout.tsx` imports them again. Product CSS/TS consumption of `var(--soft-*)` and `var(--app-*)` is **zero** (css-integrity, including `lib/`). Product CSS/TS consumption of `var(--m-*)` is also **zero** except marketing/legal sheets. Sign-in, Event Day, **and onboarding** consume `--bg` / `--surface` / `--ink` / `--accent` in **both** themes — sign-in light is no longer Apple gray hex, so the `html[data-theme="dark"]` counterpart is gone (same gold as onboarding). Leaf sheets (cad-learn, my-kit, reconcile, ranking-projection, Nexus queue, usage-cutoff, data-source banner, action-menu, export panel) now consume `--warning` / `--critical` / `--positive` instead of a second dark hex set. css-integrity fails if those competing dark hexes return, if `sign-in-flow.css` grows a dark counterpart, or if product CSS (except marketing/legal/system) grows `html[data-theme="dark"]` counterparts. **`--soft-*` / `--app-*` alias declarations are gone from `system.css`.** Team accent runtime writes only `--accent` / `--accent-soft` / `--accent-ink` / `--accent-hover` and strips leftover `--soft-*` / `--app-*` inline properties from a previous visit. css-integrity fails if those aliases return as declarations. `--m-*` stays in `system.css` for marketing. Playbook (knowledge) leftover `.button.primary` marketing classes now use the product `Button`. **Root layout no longer imports leftover `soft-ui.css` / `styles.css`.** Those sheets load from `app/product-styles.ts` (soft-ui then styles) via AppShell (`next/dynamic`, SSR) plus sign-in, pit TV, the offline boot, and showcase present. `system.css` stays on the root for tokens and a box-sizing/margin reset. css-integrity fails if `layout.tsx` imports the product chrome sheets again or if ThemeProvider statically imports AppShell. Production `next build` this revision: compiled **19.3s**, TypeScript **21.2s**, **695 pages**, no warnings in the log. Prerendered `/` HTML does **not** link `1mbin1gj2ty9j.css` (**162,657 B**, leftover `soft-ui.css` / `.soft-topbar` / `.signin-page`) or `286oovvzb4q82.css` (**97,364 B**, leftover `styles.css` / `.kiosk` / `.scout-app`). Product `/team` and `/video` HTML both link those two sheets. Shared `1uckbua-d1rkd.css` (**23,577 B**) is `system.css` (tokens + reset; a `:where(.soft-topbar…)` focus rule lives there on purpose). Leftover `.signin-*` in `soft-ui.css` now consumes `--bg` / `--surface` / `--ink` / `--accent` (onboarding and invite share it); leftover `html[data-theme="light"]` counterparts are gone from `styles.css`; css-integrity fails if those cream hexes or theme counterparts return. Ghost/danger `.app-button` no longer inherit leftover primary fill. `densityTokens` no longer writes `--soft-space-*`. |
| 3 | Full UI pass | in progress | Student copy says team, not workspace, on Home widgets, related strips, Event Day setup, intel crumbs, and `/workspace`. Empty/setup cards keep one primary. **Zero `<h2>Setup steps</h2>` remain under `apps/web`.** **Zero `className="strategy-setup-steps"` remain under `apps/web/app`.** **Zero student-visible “Select a team” / “Select an active event” / “Select event” / “Select an event” / “Select one and come back” / “Select the team” / “Select a page” / “Select a run” / “Select a preset” remain under `apps/web`.** HubOrgGate heading and button both say **Choose your team**. Alliance desk, Match video, My Day, Collaborative pick list, Defense planner, Draft, Lineup, Match strategy cards, Dossier, Pick desk, Pit repair, Drive-team signals, Field reset, Weigh-in, Event-day plan, Match-delta, Match video index, Team admin, Command, Inventory, Strategy, Inspection, Logistics, Scouting, Pit, Match checklist, FMEA, and the remaining product shells no longer paint a 4–5 step checklist beside the related strip. Team Data, Schedule, Fundraisers, Impact, Orders, Awards, and Grant writing empty/setup cards no longer nest the header related strip inside EmptyState; Visit Invites setup no longer repeats Next actions on the empty card. `/orders` setup no longer paints the Next-actions panel beside the one EmptyState primary. Writer setup keeps one EmptyState primary instead of a Next-actions wall. Practice, Attendance, and Kickoff setup keep one primary; Attendance error no longer adds a second Choose your team sibling. `empty-state-primary.test.ts` fails if a `*Related` component returns inside EmptyState or if a `*NextActions` component returns in a `setup_required` ternary. Video re-scout / Notes used a ToolStrip. Non-hub filters on `/docs` `/help` `/account` `/notifications` are ToolStrip chips. Fixture Playwright `docs-tool-strip.spec.ts` **1 passed / 1** (5.1s) on :3310: Manual views are `navigation` chips, not `role=tab`; Competition day filters the guide to **29 of 71** sections. Remaining TabBars are hub-level workbench chrome (Business, Media, `product-hub.tsx`) — `empty-states` still asserts Business's tablist stays above the empty card. Playwright landing/pricing headings match current copy. FEATURE_MAP parser: **143** visit-able product routes. Fixture HTTP walk (`next dev` :3310 + `vantage-e2e-session`): **138 OK, 5 404** (`/admin` `/admin/models` `/admin/outreach` `/admin/partners` `/admin/sponsored` — fixture is not in `platform_admins`), **0 500**, 20.7s cold / 5.0s warm. Signed-in `next start` :3001 + Better Auth cookie (seeded platform admin): **143 OK, 0 404, 0 500**, 1.3s (`auth: FEATURE_MAP_COOKIE`). Chromium walk of all 143 hangs the compiler; Playwright spec stays behind `FEATURE_MAP_PW=1`. Fixture APIs still 401. |
| 4 | Home widgets | in progress | Live student cards in `widgets/home-cards.tsx` (My Day, Learn, Files, Chat). Mentor/optional live cards in `widgets/extra-cards.tsx`. First-run `team_todos` + `onboarding_checklist` stay inlined in `widgets.tsx` so the student default does not download Extra. Extra widgets load through `ExtraWidgetView` (`next/dynamic`). Canvas helpers in `dashboard-canvas.ts`. Widget chrome/empty copy in `widgets/widget-shell.tsx` + `widgets/widget-empty-copy.ts` (student words; no EPA/org/TBA-sync). Competition/ops widgets in `widgets/ops-cards.tsx`. Board types in `dashboard-board-types.ts`. First-run banner, board switcher, widget palette, library sheet, boards modal, edit/preview docks, and **pointer-drag** (`use-dashboard-pointer-drag.ts`) are sibling modules. Board save/add/resize/keyboard lives in `use-dashboard-board-ops.ts`. `dashboard-client.tsx` is the poll/measure/compose shell (**314** lines); load/poll live in `use-dashboard-home-state.ts` and the painted Home is `dashboard-home-view.tsx` (**710**). **Empty-layout fallbacks no longer resurrect the competition board.** First paint, no-org, switch/reset, duplicate/activate-empty, and Create board now use `defaultDashboardLayoutForAudience` / `defaultLayoutForMember` (student: next match, my day, learn, tasks, files, chat, ask AI). `DEFAULT_DASHBOARD_LAYOUT` remains the competition-focus board only. Playwright tap-to-place now emulates `pointer: coarse` (library placement does not fire on Desktop Chrome mouse). **This revision:** empty/setup copy a new student can act on; first-run steps have exactly one `current` gate; onboarding card is one EmptyState primary (not a Done/Open wall); Jump back in + Start here launchpads and the header duplicate Choose-your-team/Connect-TBA buttons are gone; library `onPick` forwards `pointerType` into `prefersTapToPlace`. Venue weather: loader returns city + `isEventDay` and never a temperature; the card fetches Open-Meteo via `loadVenueForecast` / `venueWeatherCopy`. Live probe 2026-09-10T07:30Z Houston (29.76328, -95.36327): `temperature_2m` **26.1** °C, `weather_code` 0 → **26°C · Clear**. Same city 2026-09-10T09:30Z: **25.9** °C, `weather_code` 0 → **26°C · Clear**. Unit tests cover that mapping plus off-day copy ("Forecast shows on event day."). Playwright `home-weather.spec.ts` mocks Open-Meteo and adds the library card on desktop; fixture Home has no event city so the honest empty is **No venue weather**. Signed-in `next start` + seeded `2026e2ewx` (Houston, today): Playwright `signed-in-weather.spec.ts` **1 passed / 1** (3.5s with signed-in-start) — live card titled Venue weather, city Houston, Open-Meteo not mocked. |
| 2 | One design system | in progress | tokens in `system.css`; Button emits `.app-button`. **Root layout no longer imports `marketing.css` / `marketing-v3.css`.** Those sheets load from `components/marketing/marketing-styles.ts` via the marketing SiteHeader, so product routes do not pull the landing copper/paper sheet. css-integrity fails if `app/layout.tsx` imports them again. Product CSS/TS consumption of `var(--soft-*)` and `var(--app-*)` is **zero** (css-integrity, including `lib/`). Product CSS/TS consumption of `var(--m-*)` is also **zero** except marketing/legal sheets. Sign-in, Event Day, **and onboarding** consume `--bg` / `--surface` / `--ink` / `--accent` in **both** themes — sign-in light is no longer Apple gray hex, so the `html[data-theme="dark"]` counterpart is gone (same gold as onboarding). Leaf sheets (cad-learn, my-kit, reconcile, ranking-projection, Nexus queue, usage-cutoff, data-source banner, action-menu, export panel) now consume `--warning` / `--critical` / `--positive` instead of a second dark hex set. css-integrity fails if those competing dark hexes return, if `sign-in-flow.css` grows a dark counterpart, or if product CSS (except marketing/legal/system) grows `html[data-theme="dark"]` counterparts. **`--soft-*` / `--app-*` alias declarations are gone from `system.css`.** Team accent runtime writes only `--accent` / `--accent-soft` / `--accent-ink` / `--accent-hover` and strips leftover `--soft-*` / `--app-*` inline properties from a previous visit. css-integrity fails if those aliases return as declarations. `--m-*` stays in `system.css` for marketing. Playbook (knowledge) leftover `.button.primary` marketing classes now use the product `Button`. **Root layout no longer imports leftover `soft-ui.css` / `styles.css`.** Those sheets load from `app/product-styles.ts` (soft-ui then styles) via AppShell (`next/dynamic`, SSR) plus sign-in, pit TV, the offline boot, and showcase present. `system.css` stays on the root for tokens and a box-sizing/margin reset. css-integrity fails if `layout.tsx` imports the product chrome sheets again or if ThemeProvider statically imports AppShell. Production `next build` this revision: compiled **19.3s**, TypeScript **21.2s**, **695 pages**, no warnings in the log. Prerendered `/` HTML does **not** link `1mbin1gj2ty9j.css` (**162,657 B**, leftover `soft-ui.css` / `.soft-topbar` / `.signin-page`) or `286oovvzb4q82.css` (**97,364 B**, leftover `styles.css` / `.kiosk` / `.scout-app`). Product `/team` and `/video` HTML both link those two sheets. Shared `1uckbua-d1rkd.css` (**23,577 B**) is `system.css` (tokens + reset; a `:where(.soft-topbar…)` focus rule lives there on purpose). |
| 3 | Full UI pass | in progress | Student copy says team, not workspace, on Home widgets, related strips, Event Day setup, intel crumbs, and `/workspace`. Empty/setup cards keep one primary. **Zero `<h2>Setup steps</h2>` remain under `apps/web`.** **Zero `className="strategy-setup-steps"` remain under `apps/web/app`.** **Zero student-visible “Select a team” / “Select an active event” / “Select event” / “Select an event” / “Select one and come back” / “Select the team” / “Select a page” / “Select a run” / “Select a preset” remain under `apps/web`.** HubOrgGate heading and button both say **Choose your team**. Alliance desk, Match video, My Day, Collaborative pick list, Defense planner, Draft, Lineup, Match strategy cards, Dossier, Pick desk, Pit repair, Drive-team signals, Field reset, Weigh-in, Event-day plan, Match-delta, Match video index, Team admin, Command, Inventory, Strategy, Inspection, Logistics, Scouting, Pit, Match checklist, FMEA, and the remaining product shells no longer paint a 4–5 step checklist beside the related strip. Team Data, Schedule, Fundraisers, Impact, Orders, Awards, and Grant writing empty/setup cards no longer nest the header related strip inside EmptyState; Visit Invites setup no longer repeats Next actions on the empty card. `/orders` setup no longer paints the Next-actions panel beside the one EmptyState primary. Writer setup keeps one EmptyState primary instead of a Next-actions wall. Practice, Attendance, and Kickoff setup keep one primary; Attendance error no longer adds a second Choose your team sibling. `empty-state-primary.test.ts` fails if a `*Related` component returns inside EmptyState or if a `*NextActions` component returns in a `setup_required` ternary. Video re-scout / Notes used a ToolStrip. Non-hub filters on `/docs` `/help` `/account` `/notifications` are ToolStrip chips. Fixture Playwright `docs-tool-strip.spec.ts` **1 passed / 1** (5.1s) on :3310: Manual views are `navigation` chips, not `role=tab`; Competition day filters the guide to **29 of 71** sections. Remaining TabBars are hub-level workbench chrome (Business, Media, `product-hub.tsx`) — `empty-states` still asserts Business's tablist stays above the empty card. Playwright landing/pricing headings match current copy. FEATURE_MAP parser: **143** visit-able product routes. Fixture HTTP walk (`next dev` :3310 + `vantage-e2e-session`): **138 OK, 5 404** (`/admin` `/admin/models` `/admin/outreach` `/admin/partners` `/admin/sponsored` — fixture is not in `platform_admins`), **0 500**, 20.7s cold / 5.0s warm. Signed-in `next start` :3001 + Better Auth cookie (seeded platform admin): **143 OK, 0 404, 0 500**, 1.3s (`auth: FEATURE_MAP_COOKIE`). Chromium walk of all 143 hangs the compiler; Playwright spec stays behind `FEATURE_MAP_PW=1`. Fixture APIs still 401. Leftover extra-button pass this revision: `empty-state-primary.test.ts` **8 passed / 8**; copy-lint **3 passed / 3** (3.19s); `tsx-line-limit.test.ts` **1 passed / 1**; `npm run typecheck` all workspaces `--noEmit`; fixture Playwright `batteries-shell.spec.ts` **1 passed / 1** (3.7s) on :3310 — empty Batteries keeps one **Add a battery** primary. |
| 2 | One design system | in progress | tokens in `system.css`; Button emits `.app-button`. **Root layout no longer imports `marketing.css` / `marketing-v3.css`.** Those sheets load from `components/marketing/marketing-styles.ts` via the marketing SiteHeader, so product routes do not pull the landing copper/paper sheet. css-integrity fails if `app/layout.tsx` imports them again. Product CSS/TS consumption of `var(--soft-*)` and `var(--app-*)` is **zero** (css-integrity, including `lib/`). Product CSS/TS consumption of `var(--m-*)` is also **zero** except marketing/legal sheets. Sign-in, Event Day, **and onboarding** consume `--bg` / `--surface` / `--ink` / `--accent` in **both** themes — sign-in light is no longer Apple gray hex, so the `html[data-theme="dark"]` counterpart is gone (same gold as onboarding). Leaf sheets (cad-learn, my-kit, reconcile, ranking-projection, Nexus queue, usage-cutoff, data-source banner, action-menu, export panel) now consume `--warning` / `--critical` / `--positive` instead of a second dark hex set. css-integrity fails if those competing dark hexes return, if `sign-in-flow.css` grows a dark counterpart, or if product CSS (except marketing/legal/system) grows `html[data-theme="dark"]` counterparts. **`--soft-*` / `--app-*` alias declarations are gone from `system.css`.** Team accent runtime writes only `--accent` / `--accent-soft` / `--accent-ink` / `--accent-hover` and strips leftover `--soft-*` / `--app-*` inline properties from a previous visit. css-integrity fails if those aliases return as declarations. `--m-*` stays in `system.css` for marketing. Playbook (knowledge) leftover `.button.primary` marketing classes now use the product `Button`. **Root layout no longer imports leftover `soft-ui.css` / `styles.css`.** Those sheets load from `app/product-styles.ts` (soft-ui then styles) via AppShell (`next/dynamic`, SSR) plus sign-in, pit TV, the offline boot, and showcase present. `system.css` stays on the root for tokens and a box-sizing/margin reset. css-integrity fails if `layout.tsx` imports the product chrome sheets again or if ThemeProvider statically imports AppShell. Production `next build` this revision: compiled **19.3s**, TypeScript **21.2s**, **695 pages**, no warnings in the log. Prerendered `/` HTML does **not** link `1mbin1gj2ty9j.css` (**162,657 B**, leftover `soft-ui.css` / `.soft-topbar` / `.signin-page`) or `286oovvzb4q82.css` (**97,364 B**, leftover `styles.css` / `.kiosk` / `.scout-app`). Product `/team` and `/video` HTML both link those two sheets. Shared `1uckbua-d1rkd.css` (**23,577 B**) is `system.css` (tokens + reset; a `:where(.soft-topbar…)` focus rule lives there on purpose). |
| 3 | Full UI pass | in progress | Student copy says team, not workspace, on Home widgets, related strips, Event Day setup, intel crumbs, and `/workspace`. Empty/setup cards keep one primary. **Zero `<h2>Setup steps</h2>` remain under `apps/web`.** **Zero `className="strategy-setup-steps"` remain under `apps/web/app`.** **Zero student-visible “Select a team” / “Select an active event” / “Select event” / “Select an event” / “Select one and come back” / “Select the team” / “Select a page” / “Select a run” / “Select a preset” remain under `apps/web`.** HubOrgGate heading and button both say **Choose your team**. Alliance desk, Match video, My Day, Collaborative pick list, Defense planner, Draft, Lineup, Match strategy cards, Dossier, Pick desk, Pit repair, Drive-team signals, Field reset, Weigh-in, Event-day plan, Match-delta, Match video index, Team admin, Command, Inventory, Strategy, Inspection, Logistics, Scouting, Pit, Match checklist, FMEA, and the remaining product shells no longer paint a 4–5 step checklist beside the related strip. Team Data, Schedule, Fundraisers, Impact, Orders, Awards, and Grant writing empty/setup cards no longer nest the header related strip inside EmptyState; Visit Invites setup no longer repeats Next actions on the empty card. `/orders` setup no longer paints the Next-actions panel beside the one EmptyState primary. Writer setup keeps one EmptyState primary instead of a Next-actions wall. Practice, Attendance, and Kickoff setup keep one primary; Attendance error no longer adds a second Choose your team sibling. `empty-state-primary.test.ts` fails if a `*Related` component returns inside EmptyState or if a `*NextActions` component returns in a `setup_required` ternary. Video re-scout / Notes used a ToolStrip. Non-hub filters on `/docs` `/help` `/account` `/notifications` are ToolStrip chips. Fixture Playwright `docs-tool-strip.spec.ts` **1 passed / 1** (5.1s) on :3310: Manual views are `navigation` chips, not `role=tab`; Competition day filters the guide to **29 of 71** sections. Remaining TabBars are hub-level workbench chrome (Business, Media, `product-hub.tsx`) — `empty-states` still asserts Business's tablist stays above the empty card. Playwright landing/pricing headings match current copy. FEATURE_MAP parser: **143** visit-able product routes. Fixture HTTP walk (`next dev` :3310 + `vantage-e2e-session`): **138 OK, 5 404** (`/admin` `/admin/models` `/admin/outreach` `/admin/partners` `/admin/sponsored` — fixture is not in `platform_admins`), **0 500**, 20.7s cold / 5.0s warm. Signed-in `next start` :3001 + Better Auth cookie (seeded platform admin): **143 OK, 0 404, 0 500**, 1.3s (`auth: FEATURE_MAP_COOKIE`). Chromium walk of all 143 hangs the compiler; Playwright spec stays behind `FEATURE_MAP_PW=1`. Fixture APIs still 401. | This revision: leftover **Pick a team** chrome not in open PRs now says **Choose your team**. `leftover-pick-a-team.test.ts` **3 passed / 3**. Fixture Playwright `choose-your-team-leftover.spec.ts` **1 passed / 1** (1.9s) on :3310. `npm run typecheck` all workspaces `--noEmit`. Targeted vitest **93 passed / 93** (12 files, 3.15s).
| 2 | One design system | in progress | tokens in `system.css`; Button emits `.app-button`. **Root layout no longer imports `marketing.css` / `marketing-v3.css`.** Those sheets load from `components/marketing/marketing-styles.ts` via the marketing SiteHeader, so product routes do not pull the landing copper/paper sheet. css-integrity fails if `app/layout.tsx` imports them again. Product CSS/TS consumption of `var(--soft-*)` and `var(--app-*)` is **zero** (css-integrity, including `lib/`). Product CSS/TS consumption of `var(--m-*)` is also **zero** except marketing/legal sheets. Sign-in, Event Day, **and onboarding** consume `--bg` / `--surface` / `--ink` / `--accent` in **both** themes — sign-in light is no longer Apple gray hex, so the `html[data-theme="dark"]` counterpart is gone (same gold as onboarding). Leaf sheets (cad-learn, my-kit, reconcile, ranking-projection, Nexus queue, usage-cutoff, data-source banner, action-menu, export panel) now consume `--warning` / `--critical` / `--positive` instead of a second dark hex set. css-integrity fails if those competing dark hexes return, if `sign-in-flow.css` grows a dark counterpart, or if product CSS (except marketing/legal/system) grows `html[data-theme="dark"]` counterparts. **`--soft-*` / `--app-*` alias declarations are gone from `system.css`.** Team accent runtime writes only `--accent` / `--accent-soft` / `--accent-ink` / `--accent-hover` and strips leftover `--soft-*` / `--app-*` inline properties from a previous visit. css-integrity fails if those aliases return as declarations. `--m-*` stays in `system.css` for marketing. Playbook (knowledge) leftover `.button.primary` marketing classes now use the product `Button`. **Root layout no longer imports leftover `soft-ui.css` / `styles.css`.** Those sheets load from `app/product-styles.ts` (soft-ui then styles) via AppShell (`next/dynamic`, SSR) plus sign-in, pit TV, the offline boot, and showcase present. `system.css` stays on the root for tokens and a box-sizing/margin reset. css-integrity fails if `layout.tsx` imports the product chrome sheets again or if ThemeProvider statically imports AppShell. Production `next build` this revision: compiled **19.3s**, TypeScript **21.2s**, **695 pages**, no warnings in the log. Prerendered `/` HTML does **not** link `1mbin1gj2ty9j.css` (**162,657 B**, leftover `soft-ui.css` / `.soft-topbar` / `.signin-page`) or `286oovvzb4q82.css` (**97,364 B**, leftover `styles.css` / `.kiosk` / `.scout-app`). Product `/team` and `/video` HTML both link those two sheets. Shared `1uckbua-d1rkd.css` (**23,577 B**) is `system.css` (tokens + reset; a `:where(.soft-topbar…)` focus rule lives there on purpose). |
| 3 | Full UI pass | in progress | Student copy says team, not workspace, on Home widgets, related strips, Event Day setup, intel crumbs, and `/workspace`. Empty/setup cards keep one primary. **Zero `<h2>Setup steps</h2>` remain under `apps/web`.** **Zero `className="strategy-setup-steps"` remain under `apps/web/app`.** **Zero student-visible “Select a team” / “Select an active event” / “Select event” / “Select an event” / “Select one and come back” / “Select the team” / “Select a page” / “Select a run” / “Select a preset” remain under `apps/web`.** HubOrgGate heading and button both say **Choose your team**. Alliance desk, Match video, My Day, Collaborative pick list, Defense planner, Draft, Lineup, Match strategy cards, Dossier, Pick desk, Pit repair, Drive-team signals, Field reset, Weigh-in, Event-day plan, Match-delta, Match video index, Team admin, Command, Inventory, Strategy, Inspection, Logistics, Scouting, Pit, Match checklist, FMEA, and the remaining product shells no longer paint a 4–5 step checklist beside the related strip. Team Data, Schedule, Fundraisers, Impact, Orders, Awards, and Grant writing empty/setup cards no longer nest the header related strip inside EmptyState; Visit Invites setup no longer repeats Next actions on the empty card. `/orders` setup no longer paints the Next-actions panel beside the one EmptyState primary. Writer setup keeps one EmptyState primary instead of a Next-actions wall. Practice, Attendance, and Kickoff setup keep one primary; Attendance error no longer adds a second Choose your team sibling. `empty-state-primary.test.ts` fails if a `*Related` component returns inside EmptyState or if a `*NextActions` component returns in a `setup_required` ternary. Video re-scout / Notes used a ToolStrip. Non-hub filters on `/docs` `/help` `/account` `/notifications` are ToolStrip chips. Fixture Playwright `docs-tool-strip.spec.ts` **1 passed / 1** (5.1s) on :3310: Manual views are `navigation` chips, not `role=tab`; Competition day filters the guide to **29 of 71** sections. Remaining TabBars are hub-level workbench chrome (Business, Media, `product-hub.tsx`) — `empty-states` still asserts Business's tablist stays above the empty card. Playwright landing/pricing headings match current copy. FEATURE_MAP parser: **143** visit-able product routes. Fixture HTTP walk (`next dev` :3310 + `vantage-e2e-session`): **138 OK, 5 404** (`/admin` `/admin/models` `/admin/outreach` `/admin/partners` `/admin/sponsored` — fixture is not in `platform_admins`), **0 500**, 20.7s cold / 5.0s warm. Signed-in `next start` :3001 + Better Auth cookie (seeded platform admin): **143 OK, 0 404, 0 500**, 1.3s (`auth: FEATURE_MAP_COOKIE`). Chromium walk of all 143 hangs the compiler; Playwright spec stays behind `FEATURE_MAP_PW=1`. Fixture APIs still 401. This revision: leftover **Join or pick a team** setup copy not in open PRs now says **Choose your team**. Scout attach/voice names the robot being scouted. `leftover-join-or-pick.test.ts` **5 passed / 5**. Fixture Playwright `join-or-pick-leftover.spec.ts` **1 passed / 1** (29.6s) on :3310. `npm run typecheck` all workspaces `--noEmit`. Targeted vitest **93 passed / 93** (9 files, 2.94s). |
| 2 | One design system | in progress | tokens in `system.css`; Button emits `.app-button`. **Root layout no longer imports `marketing.css` / `marketing-v3.css`.** Those sheets load from `components/marketing/marketing-styles.ts` via the marketing SiteHeader, so product routes do not pull the landing copper/paper sheet. css-integrity fails if `app/layout.tsx` imports them again. Product CSS/TS consumption of `var(--soft-*)` and `var(--app-*)` is **zero** (css-integrity, including `lib/`). Product CSS/TS consumption of `var(--m-*)` is also **zero** except marketing/legal sheets. Sign-in, Event Day, **and onboarding** consume `--bg` / `--surface` / `--ink` / `--accent` in **both** themes — sign-in light is no longer Apple gray hex, so the `html[data-theme="dark"]` counterpart is gone (same gold as onboarding). Leaf sheets (cad-learn, my-kit, reconcile, ranking-projection, Nexus queue, usage-cutoff, data-source banner, action-menu, export panel) now consume `--warning` / `--critical` / `--positive` instead of a second dark hex set. css-integrity fails if those competing dark hexes return, if `sign-in-flow.css` grows a dark counterpart, or if product CSS (except marketing/legal/system) grows `html[data-theme="dark"]` counterparts. **`--soft-*` / `--app-*` alias declarations are gone from `system.css`.** Team accent runtime writes only `--accent` / `--accent-soft` / `--accent-ink` / `--accent-hover` and strips leftover `--soft-*` / `--app-*` inline properties from a previous visit. css-integrity fails if those aliases return as declarations. `--m-*` stays in `system.css` for marketing. Playbook (knowledge) leftover `.button.primary` marketing classes now use the product `Button`. **Root layout no longer imports leftover `soft-ui.css` / `styles.css`.** Those sheets load from `app/product-styles.ts` (soft-ui then styles) via AppShell (`next/dynamic`, SSR) plus sign-in, pit TV, the offline boot, and showcase present. `system.css` stays on the root for tokens and a box-sizing/margin reset. css-integrity fails if `layout.tsx` imports the product chrome sheets again or if ThemeProvider statically imports AppShell. Production `next build` this revision: compiled **19.3s**, TypeScript **21.2s**, **695 pages**, no warnings in the log. Prerendered `/` HTML does **not** link `1mbin1gj2ty9j.css` (**162,657 B**, leftover `soft-ui.css` / `.soft-topbar` / `.signin-page`) or `286oovvzb4q82.css` (**97,364 B**, leftover `styles.css` / `.kiosk` / `.scout-app`). Product `/team` and `/video` HTML both link those two sheets. Shared `1uckbua-d1rkd.css` (**23,577 B**) is `system.css` (tokens + reset; a `:where(.soft-topbar…)` focus rule lives there on purpose). |
| 3 | Full UI pass | in progress | Student copy says team, not workspace, on Home widgets, related strips, Event Day setup, intel crumbs, and `/workspace`. Empty/setup cards keep one primary. **Zero `<h2>Setup steps</h2>` remain under `apps/web`.** This revision: leftover Ask AI no-org cards (Chat / Chat limits / Usage) not in open PRs #2–#34 now keep one **Choose your team** primary. Memory / Chat limits copy no longer says pick a team first / per org. `leftover-ask-ai-setup.test.ts` **3 passed / 3**. Fixture Playwright `ask-ai-setup-primary.spec.ts` **1 passed / 1** (6.6s) on :3310. `npm run typecheck --workspace=@vantage/web`. Targeted vitest **35 passed / 35** (5 files, 2.94s). |
| 4 | Home widgets | in progress | Live student cards in `widgets/home-cards.tsx` (My Day, Learn, Files, Chat). Mentor/optional live cards in `widgets/extra-cards.tsx`. First-run `team_todos` + `onboarding_checklist` stay inlined in `widgets.tsx` so the student default does not download Extra. Extra widgets load through `ExtraWidgetView` (`next/dynamic`). Canvas helpers in `dashboard-canvas.ts`. Widget chrome/empty copy in `widgets/widget-shell.tsx`. Competition/ops widgets in `widgets/ops-cards.tsx`. Board types in `dashboard-board-types.ts`. First-run banner, board switcher, widget palette, library sheet, boards modal, edit/preview docks, and **pointer-drag** (`use-dashboard-pointer-drag.ts`) are sibling modules. Board save/add/resize/keyboard lives in `use-dashboard-board-ops.ts`. `dashboard-client.tsx` is the poll/measure/compose shell (**312** lines); load/poll live in `use-dashboard-home-state.ts` and the painted Home is `dashboard-home-view.tsx`. **Empty-layout fallbacks no longer resurrect the competition board.** First paint, no-org, switch/reset, duplicate/activate-empty, and Create board now use `defaultDashboardLayoutForAudience` / `defaultLayoutForMember` (student: next match, my day, learn, tasks, files, chat, ask AI). `DEFAULT_DASHBOARD_LAYOUT` remains the competition-focus board only. Playwright tap-to-place now emulates `pointer: coarse` (library placement does not fire on Desktop Chrome mouse). Venue weather: loader returns city + `isEventDay` and never a temperature; the card fetches Open-Meteo via `loadVenueForecast` / `venueWeatherCopy`. Live probe 2026-09-10T07:30Z Houston (29.76328, -95.36327): `temperature_2m` **26.1** °C, `weather_code` 0 → **26°C · Clear**. Same city 2026-09-10T09:30Z: **25.9** °C, `weather_code` 0 → **26°C · Clear**. Unit tests cover that mapping plus off-day copy ("Forecast shows on event day."). Playwright `home-weather.spec.ts` mocks Open-Meteo and adds the library card on desktop; fixture Home has no event city so the honest empty is **No venue weather**. Signed-in `next start` + seeded `2026e2ewx` (Houston, today): Playwright `signed-in-weather.spec.ts` **1 passed / 1** (3.5s with signed-in-start) — live card titled Venue weather, city Houston, Open-Meteo not mocked. |
| 5 | Offline shell | in progress | Season calendar, packing, batteries, pit, season-tasks already queued. **Home** (`/dashboard`) now restores the last IndexedDB snapshot (`feature: "dashboard"`) when `/api/dashboards` fails or times out at 8s — same pattern as My Day. The painted Home shows `OfflineBanner feature="Home"` and does not claim **Synced** while that cache is on screen. Source lock: `dashboard-offline.test.ts`. **Assembly manual** restores the last run list (`feature: "assembly-manual"`) the same way; finished PDFs were already kept via `keepOfflineFile`. Source lock: `assembly-manual-offline.test.ts`. **Alliance chemistry** and **Pick clock** restore the last IndexedDB snapshot (`feature: "chemistry"` / `"pick-clock"`) so Event Day tabs do not go blank; `/chemistry` and `/pick-clock` are on the service-worker shell list. Source locks: `chemistry-offline.test.ts`, `pick-clock-offline.test.ts`. **Alliance selection desk**, **Print Farm**, **Inventory**, and **Defense planner** restore the last IndexedDB snapshot (`feature: "alliance-desk"` / `"print-farm"` / `"inventory"` / `"defense-planner"`) the same way; `/alliance-selection-desk`, `/print-farm`, `/inventory`, and `/defense-planner` are on the service-worker shell list. Source locks: `alliance-desk-offline.test.ts`, `print-farm-offline.test.ts`, `inventory-offline.test.ts`, `defense-planner-offline.test.ts`. **Collaborative pick list**, **Dossier**, **Pit repair triage**, and **Event-day plan** restore the last IndexedDB snapshot (`feature: "picklist-collab"` / `"dossier"` / `"pit-repair"` / `"event-day-plan"`); `/picklist-collab`, `/dossier`, `/pit-repair-triage`, and `/event-day-plan` are on the service-worker shell list. Source locks: `picklist-collab-offline.test.ts`, `dossier-offline.test.ts`, `pit-repair-offline.test.ts`, `event-day-plan-offline.test.ts`. **Field reset timer**, **Drive-team signals**, **Robot weigh-in**, **Match-delta watcher**, and **Match video index** restore the last IndexedDB snapshot (`feature: "field-reset"` / `"drive-signals"` / `"weigh-in"` / `"match-delta"` / `"match-video-index"`); `/field-reset-timer`, `/drive-team-signals`, `/robot-weigh-in`, `/match-delta-watcher`, and `/match-video-index` are on the service-worker shell list. Source locks: `field-reset-offline.test.ts`, `drive-team-signals-offline.test.ts`, `robot-weigh-offline.test.ts`, `match-delta-offline.test.ts`, `match-video-index-offline.test.ts`. **Alliance board (Draft)** and **Lineup & coverage** restore the last IndexedDB snapshot (`feature: "draft"` / `"lineup"`); a failed fetch no longer clears Draft (`setData(null)`) or forces Lineup's error shell via `fetchFailed` while a cached board is on screen. `/strategy/draft` and `/scouting/lineup` are on the service-worker shell list. Source locks: `draft-offline.test.ts`, `lineup-offline.test.ts`. **Match strategy cards**, **Match Copilot**, and **Event readiness** restore the last IndexedDB snapshot (`feature: "strategy-cards"` / `"match-copilot"` / `"event-readiness"`); `/match-strategy-cards`, `/match-copilot`, and `/event-readiness` are on the service-worker shell list. Source locks: `match-strategy-cards-offline.test.ts`, `match-copilot-offline.test.ts`, `event-readiness-offline.test.ts`. **Match video**, **Inspection**, **Inspection Copilot**, and **FMEA** restore the last IndexedDB snapshot (`feature: "match-video"` / `"inspection"` / `"inspection-copilot"` / `"fmea"`); `/video`, `/inspection-copilot`, `/inspection`, and `/fmea` are on the service-worker shell list. `/video-analysis` stays labeled Video; `/video` is Match video. `/inspection-copilot` is labeled Inspection Copilot, not Inspection. Source locks: `video-offline.test.ts`, `inspection-offline.test.ts`, `inspection-copilot-offline.test.ts`, `fmea-offline.test.ts`. **Match Simulator**, **Pit Map Planner**, **Pairwise ranking**, and **Drive-team tags** restore the last IndexedDB snapshot (`feature: "match-sim"` / `"pit-map"` / `"pairwise"` / `"team-tags"`); `/match-sim`, `/pit-map-planner`, `/pairwise`, and `/team-tags` are on the service-worker shell list. Source locks: `match-sim-offline.test.ts`, `pit-map-planner-offline.test.ts`, `pairwise-offline.test.ts`, `team-tags-offline.test.ts`. **Scout shift balancer**, **Counter-book**, **Overnight Intel**, and **Alliance-Partner Brief** restore the last IndexedDB snapshot (`feature: "shift-balancer"` / `"counter-book"` / `"overnight-intel"` / `"alliance-brief"`); `/shift-balancer`, `/counter-book`, `/overnight-intel`, and `/alliance-partner-brief` are on the service-worker shell list. Source locks: `shift-balancer-offline.test.ts`, `counter-book-offline.test.ts`, `overnight-intel-offline.test.ts`, `alliance-partner-brief-offline.test.ts`. **Pick-list Justifier**, **Opponent Watchlist**, **Alliance Sim**, and **Pre-match briefing** restore the last IndexedDB snapshot (`feature: "picklist-justifier"` / `"opponent-watchlist"` / `"alliance-sim"` / `"briefing"`); `/picklist-justifier`, `/opponent-watchlist`, `/alliance-sim`, and `/briefing` are on the service-worker shell list. Source locks: `picklist-justifier-offline.test.ts`, `opponent-watchlist-offline.test.ts`, `alliance-sim-offline.test.ts`, `briefing-offline.test.ts`. **Award Tracker**, **EPA Trend Alerts**, and **Rankings** restore the last IndexedDB snapshot (`feature: "award-tracker"` / `"epa-trend"` / `"rankings"`); Rankings uses status `ready`, not `live`. `/award-tracker`, `/epa-trend-alerts`, and `/rankings` are on the service-worker shell list. Source locks: `award-tracker-offline.test.ts`, `epa-trend-alerts-offline.test.ts`, `rankings-offline.test.ts`. **Grant Report**, **Media Kit**, **Sponsor Wall**, and **Sponsor Suite** restore the last IndexedDB snapshot (`feature: "grant-report"` / `"media-kit"` / `"sponsor-wall"` / `"sponsor-suite"`); `/grant-report`, `/media-kit`, `/sponsor-wall`, and `/sponsor-suite` are on the service-worker shell list. Source locks: `grant-report-offline.test.ts`, `media-kit-offline.test.ts`, `sponsor-wall-offline.test.ts`, `sponsor-suite-offline.test.ts`. **Outreach Calendar**, **Visit Invites**, **Judge-Pitch**, and **Impact Essay** restore the last IndexedDB snapshot (`feature: "outreach-calendar"` / `"visit-invites"` / `"judge-sim"` / `"impact-essay"`); Visit Invites uses status `ready`, not `live`. `/outreach-calendar`, `/visit-invites`, `/judge-sim`, and `/impact-essay` are on the service-worker shell list. Source locks: `outreach-calendar-offline.test.ts`, `visit-invites-offline.test.ts`, `judge-sim-offline.test.ts`, `impact-essay-offline.test.ts`. **Battery Rotation**, **Vendors**, **Season Report**, and **Media** restore the last IndexedDB snapshot (`feature: "battery-rotation"` / `"vendors"` / `"season-report"` / `"media"`); `/battery-rotation`, `/vendors`, `/season-report`, and `/media` are on the service-worker shell list. `/media-kit` stays labeled Media Kit. Source locks: `battery-rotation-offline.test.ts`, `vendors-offline.test.ts`, `season-report-offline.test.ts`, `media-offline.test.ts`. **Battery Health Forecast**, **Vendor Lead Times**, **Bin/Shelf Locator**, and **Build Burndown** restore the last IndexedDB snapshot (`feature: "battery-health-forecast"` / `"vendor-lead-times"` / `"bin-shelf-locator"` / `"build-burndown"`); `/battery-health-forecast`, `/vendor-lead-times`, `/bin-shelf-locator`, and `/build-burndown` are on the service-worker shell list. Source locks: `battery-health-forecast-offline.test.ts`, `vendor-lead-times-offline.test.ts`, `bin-shelf-locator-offline.test.ts`, `build-burndown-offline.test.ts`. **CAD Change Radar**, **Code Deploy Log**, **Control Map**, and **Cross-Team Scrims** restore the last IndexedDB snapshot (`feature: "cad-change-radar"` / `"code-deploy-log"` / `"control-map"` / `"cross-team-scrim"`); Control Map uses status `ready`, not `live`. `/cad-change-radar`, `/code-deploy-log`, `/control-map`, and `/cross-team-scrim` are on the service-worker shell list. Source locks: `cad-change-radar-offline.test.ts`, `code-deploy-log-offline.test.ts`, `control-map-offline.test.ts`, `cross-team-scrim-offline.test.ts`. **Decision Search**, **Decision Log**, **Repeat Failure Patterns**, and **Grant Eligibility Matcher** restore the last IndexedDB snapshot (`feature: "decision-search"` / `"decisions"` / `"failure-patterns"` / `"grant-eligibility-matcher"`); `/decision-search`, `/decisions`, `/failure-patterns`, and `/grant-eligibility-matcher` are on the service-worker shell list. `/decision-search` is labeled Decision Search, not Decision Log. Source locks: `decision-search-offline.test.ts`, `decisions-offline.test.ts`, `failure-patterns-offline.test.ts`, `grant-eligibility-matcher-offline.test.ts`. **My Hours**, **Knowledge-gap detective**, **Matching Gift Finder**, and **Onboarding Buddy** restore the last IndexedDB snapshot (`feature: "hours-self-view"` / `"knowledge-gap"` / `"matching-gift-finder"` / `"onboarding-buddy"`); `/hours-self-view`, `/knowledge-gap`, `/matching-gift-finder`, and `/onboarding-buddy` are on the service-worker shell list. `/hours-self-view` is labeled My Hours, not Hours. Source locks: `hours-self-view-offline.test.ts`, `knowledge-gap-offline.test.ts`, `matching-gift-finder-offline.test.ts`, `onboarding-buddy-offline.test.ts`. **Risk-Register Burndown**, **Spare Robot Kit**, **Sponsor Renewal ROI**, and **Team Health** restore the last IndexedDB snapshot (`feature: "risk-burndown"` / `"spare-robot-kit"` / `"sponsor-renewal-roi"` / `"team-health-dashboard"`); `/risk-burndown`, `/spare-robot-kit`, `/sponsor-renewal-roi`, and `/team-health-dashboard` are on the service-worker shell list. Source locks: `risk-burndown-offline.test.ts`, `spare-robot-kit-offline.test.ts`, `sponsor-renewal-roi-offline.test.ts`, `team-health-dashboard-offline.test.ts`. **Spare-Parts Failure Forecast**, **Sketch-to-Brief**, **Scout-Assisted Count**, and **Scout Coverage Live** restore the last IndexedDB snapshot (`feature: "spare-forecast"` / `"sketch-to-brief"` / `"scout-assisted-count"` / `"scout-coverage-live"`); `/spare-forecast`, `/sketch-to-brief`, `/scout-assisted-count`, and `/scout-coverage-live` are on the service-worker shell list. Source locks: `spare-forecast-offline.test.ts`, `sketch-to-brief-offline.test.ts`, `scout-assisted-count-offline.test.ts`, `scout-coverage-live-offline.test.ts`. **Field-Count Budget**, **Scouting Heat Signals**, **Scout Data Impact**, and **Scout Disagreements** restore the last IndexedDB snapshot (`feature: "scout-field-budget"` / `"scouting-heat-signals"` / `"scout-data-impact"` / `"scout-disagreements"`); `/scout-field-budget`, `/scouting-heat-signals`, `/scout-data-impact`, and `/scout-disagreements` are on the service-worker shell list. `/scouting-heat-signals` is labeled Scouting Heat Signals, not Scouting. Source locks: `scout-field-budget-offline.test.ts`, `scouting-heat-signals-offline.test.ts`, `scout-data-impact-offline.test.ts`, `scout-disagreements-offline.test.ts`. **Scout Accuracy**, **Scout Cross-Validation**, **Rule Impact Analyzer**, and **Season Planning** restore the last IndexedDB snapshot (`feature: "scout-accuracy"` / `"scout-crossval"` / `"rule-impact"` / `"season-planning-workspace"`); `/scout-accuracy`, `/scout-crossval`, `/rule-impact`, and `/season-planning-workspace` are on the service-worker shell list. Source locks: `scout-accuracy-offline.test.ts`, `scout-crossval-offline.test.ts`, `rule-impact-offline.test.ts`, `season-planning-workspace-offline.test.ts`. **Retro**, **Team Data**, **Tuning Autopilot**, and **Match video re-scout** restore the last IndexedDB snapshot (`feature: "retro"` / `"team-data"` / `"tuning-autopilot"` / `"video-rescout"`); `/retro`, `/team/data`, and `/tuning-autopilot` are on the service-worker shell list (`/video` already was). `/team/data` is labeled Team Data, not Team. **Fundraisers**, **Community Impact**, **Orders**, and **Grant writing** restore the last IndexedDB snapshot (`feature: "fundraisers"` / `"impact"` / `"orders"` / `"grants-writing"`); `/fundraisers`, `/impact`, `/orders`, and `/team/grants` are on the service-worker shell list. `/impact` is labeled Community Impact, not Impact Essay. `/team/grants` is labeled Grant writing, not Team. Intel search stays uncached (multi-fetch, not a single-view snapshot). Awards stays uncached (list + essay-item fetches, not a single-view snapshot). **Practice**, **Manufacturing**, **Attendance**, and **Kickoff** restore the last IndexedDB snapshot (`feature: "practice"` / `"manufacturing"` / `"attendance"` / `"kickoff"`); `/practice`, `/manufacturing`, `/attendance`, and `/kickoff` are on the service-worker shell list. Attendance keys the cache by season. **My Day**, **Schedule**, and **Event Day** (`/command`) restore the last IndexedDB snapshot when the fetch fails. `/dashboard`, `/assembly-manual`, `/my-day` and `/command` are on the service-worker shell list. Cold-boot status says **team remembered**, not workspace; setup detail says **choose your team first**, not pick an org. copy-lint bans **workspace remembered** / **pick an org**. Fixture Playwright covers `/competition`, `/calendar`, `/my-day`, `/command`, `/packing`, `/batteries`, and `/pit` offline reloads. Signed-in `next start` Playwright `signed-in-offline.spec.ts` **1 passed / 1** (1.9s) for `/competition` `/packing` `/batteries` `/pit` `/command`. **Writer**, **Learning**, and **Training** restore the last IndexedDB snapshot (`feature: "writer"` / `"learning"` / `"training"`); `/writer`, `/learning`, and `/training` are on the service-worker shell list. Writer keys the cache by season. **Tool checkout**, **Goals**, **CAD Vault**, **CAD Learn**, and **Reimbursements** restore the last IndexedDB snapshot (`feature: "tool-checkout"` / `"goals"` / `"cad-vault"` / `"cad-learn"` / `"reimbursements"`); `/tool-checkout`, `/goals`, `/cad-vault`, `/cad-learn`, and `/reimbursements` are on the service-worker shell list. Goals and CAD Vault key the cache by season. CAD Learn keeps the curriculum on a failed GET. `/cad-vault` and `/cad-learn` are labeled before CAD Change Radar. **Season Costs**, **CAD Review Queue**, **Duties**, and **Announcements** restore the last IndexedDB snapshot (`feature: "costs"` / `"cad-review-queue"` / `"duties"` / `"announcements"`); `/costs`, `/cad-review-queue`, `/duties`, and `/announcements` are on the service-worker shell list. Costs and CAD Review Queue key the cache by season. `/cad-review-queue` is labeled CAD Review Queue. **Risk Register**, **Subsystem Sign-off**, **Prototype-to-Decision Tracker**, and **Equipment Maintenance** restore the last IndexedDB snapshot (`feature: "risks"` / `"subsystem-signoff"` / `"prototype-tracker"` / `"equipment-maintenance"`); `/risks`, `/subsystem-signoff`, `/prototype-tracker`, and `/equipment-maintenance` are on the service-worker shell list. Risks, Sign-off, and Prototypes key the cache by season. `/risks` is labeled Risk Register, not Risk-Register Burndown. **Notifications**, **Forms**, **Safety Incident Log**, **Season roles**, **Parts catalog**, and **Parts Relay** restore the last IndexedDB snapshot (`feature: "notifications"` / `"forms"` / `"incidents"` / `"roles"` / `"parts-catalog"` / `"parts-relay"`); `/notifications`, `/forms`, `/incidents`, `/roles`, `/parts-catalog`, and `/parts-relay` are on the service-worker shell list. Notifications keys the cache by inbox filter. Incidents and Season roles key the cache by season. `/forms` is labeled Forms, not Scouting (`/scouting/forms` stays Scouting). `/incidents` is labeled Safety Incident Log. **Do not claim Playwright offline Home, assembly-manual, chemistry, pick-clock, alliance-desk, print-farm, inventory, defense-planner, picklist-collab, dossier, pit-repair, event-day-plan, field-reset, drive-signals, weigh-in, match-delta, match-video-index, draft, lineup, strategy-cards, match-copilot, event-readiness, match-video, video-rescout, inspection, inspection-copilot, fmea, match-sim, pit-map, pairwise, team-tags, shift-balancer, counter-book, overnight-intel, alliance-brief, picklist-justifier, opponent-watchlist, alliance-sim, briefing, award-tracker, epa-trend, rankings, grant-report, media-kit, sponsor-wall, sponsor-suite, outreach-calendar, visit-invites, judge-sim, impact-essay, battery-rotation, vendors, season-report, media, battery-health-forecast, vendor-lead-times, bin-shelf-locator, build-burndown, cad-change-radar, code-deploy-log, control-map, cross-team-scrim, decision-search, decisions, failure-patterns, grant-eligibility-matcher, hours-self-view, knowledge-gap, matching-gift-finder, onboarding-buddy, risk-burndown, spare-robot-kit, sponsor-renewal-roi, team-health-dashboard, spare-forecast, sketch-to-brief, scout-assisted-count, scout-coverage-live, scout-field-budget, scouting-heat-signals, scout-data-impact, scout-disagreements, scout-accuracy, scout-crossval, rule-impact, season-planning-workspace, retro, team-data, tuning-autopilot, fundraisers, impact, orders, grants-writing, practice, manufacturing, attendance, kickoff, writer, learning, training, tool-checkout, goals, cad-vault, cad-learn, reimbursements, costs, cad-review-queue, duties, announcements, risks, subsystem-signoff, prototype-tracker, equipment-maintenance, notifications, forms, incidents, roles, parts-catalog, or parts-relay without a run.** |
| 6 | Desktop installers + auto-update | in progress | `/api/desktop/release`, NSIS+MSI+DMG workflow, unsigned license. macOS artifacts cannot be built in this image. |
| 7 | freebuff Pi fleet | in progress | `docs/FREEBUFF.md`, pairing API, `/team/relays` node list, compact+prompt. TTFT unmeasured (no Pi). |
| 8 | Match prediction ±3 | in progress | Next match widget shows calibrated score + ±band when year ratings exist. Fixture (2 matches, 4 alliance scores): MAE **89.7**, RMSE **89.88**, within ±3 **0%**, within ±5 **0%**. UI band is `FIXTURE_ERROR_BAND` = **90** (`errorBandFromMae(89.7)`), copy **typical error ±90 (last measured set)**. Placeholder 8 is gone. Empty prediction copy is student words (no EPA). Source lock: `prediction-honest.test.ts`. Cached 2026 backtest in `docs/PREDICTION_RESULTS.md`: **0** scored matches. **Not a season ±3 claim.** Task 8 stays open. |
| 9 | Video analysis | in progress | schema + queue UI + confirm-as-evidence. **`/video-analysis` student shell is Video** (not Analyze video): session team from `/api/me` when the URL has no orgId, **one** live-org primary **Analyze this video** on the paste form (empty EmptyState is copy, not a second CTA), related in the header, Next-actions only on ready, no jobs/file-id/confidence numbers. `/video` stays Match video (`video-rescout-client.tsx`) — paste-and-confirm already on main, not redone. Last IndexedDB copy (`feature: "video-analysis"`) still paints first. Confirmed events still appear on Match notes as **From video**. Worker skips without a vision model. No live Pi. Fixture Playwright `video-analysis-shell.spec.ts` **1 passed / 1** (23.2s) on `:3591` + local `vantage_ci` (127.0.0.1, not production DATABASE_*): h1 **Video**, related strip Event day · Match notes · Match video, paste region + one **Analyze this video**, no Setup steps, no video jobs / Pick a team first. Screenshot `/opt/cursor/artifacts/video-analysis-after-shell.png`. Did not wrap freebuff. Did not deploy. Master brief not complete. |
| 10 | Connectors | audit (already on main) | Account `?tab=integrations` redirects to `/connectors`. TBA next-action from Account goes there too. |
| 11 | Copy sweep | in progress | copy-lint bans allowlist, PAYG / kill switch / BYOK, **BYO key**, leftover workspace-as-team-picker phrases, **workspace slug**, **Select a team** / **Select an active event**, leftover **Select event** / **Select an event**, **Select one and come back**, **Select the team**, **Select a page**, **Select a run**, and **Select a preset**. Empty titles, compute/API messages, Home header, HubOrgGate, and leftover **Choose team** CTAs now say **Choose your team**. Event titles say **Set active event**. Scan covers `.tsx`, `*-related.ts`, `compute-*`, API routes, and `dashboard-home-model.ts`. Bugbot / Chat / Usage / Team admin student copy now says "your own keys" or "the team's keys". `/workspace` page title is Your team. Admin create-team label is **Team slug**. Sign-in setup no longer says "workspace database". Onshape/Slack "workspace" kept. Copy-lint now bans uppercase **MODEL** as a badge (Event Day, Chemistry, kiosk, Intel, AI keys, usage, pick-list notes, kickoff chip). Partner-fit notes say the score and "verify with pit notes", not a model id. Copy-lint now also bans **event-scoped** / "org- and event-scoped" (`(?:org\|event)[-\s]scoped`). Chemistry, Pick clock, Pick desk, Draft, Lineup, and the Strategy marketing page no longer say it. **Zero Setup steps headings remain in product UI.** Leftover `strategy-setup-steps` lists are gone from `apps/web/app`. This revision also bans **Select a repository / grant / expense**, **pick your organization**, and **your organization team number**; copy-lint **3 passed / 3** (3.19s). |
| 9 | Video analysis | in progress | schema + queue UI + confirm-as-evidence. Student shell is **Match video** (`video-rescout-client.tsx`): one primary, no setup checklist, fetches abort at 8s. Last IndexedDB copy (`feature: "video-rescout"`) now paints first when venue Wi-Fi drops. Unused `video-client.tsx` still caches `"match-video"`. `/video-analysis` already used `useOfflineSnapshot("video-analysis")`. Confirmed events now appear on the match-notes timeline as **From video**. Worker skips without a vision model. No live Pi. Playwright offline Match video not run this revision. |
| 10 | Connectors | audit (already on main) | Account `?tab=integrations` redirects to `/connectors`. TBA next-action from Account goes there too. **This revision:** member cards never say Onshape OAuth / Email (Resend) / env-var names; owners/admins still get the operator catalog. CAD connectors GUI: connect / status / disconnect / paste-link document picker on `/connectors` and `/cad/connections`; student permissions copy; gold last-snapshot; Choose your team on 401/403. |
| 11 | Copy sweep | in progress | copy-lint bans allowlist, PAYG / kill switch / BYOK, **BYO key**, leftover workspace-as-team-picker phrases, **workspace slug**, **Select a team** / **Select an active event**, leftover **Select event** / **Select an event**, **Select one and come back**, **Select the team**, **Select a page**, **Select a run**, and **Select a preset**. Empty titles, compute/API messages, Home header, HubOrgGate, and leftover **Choose team** CTAs now say **Choose your team**. Event titles say **Set active event**. Scan covers `.tsx`, `*-related.ts`, `compute-*`, API routes, and `dashboard-home-model.ts`. Bugbot / Chat / Usage / Team admin student copy now says "your own keys" or "the team's keys". `/workspace` page title is Your team. Admin create-team label is **Team slug**. Sign-in setup no longer says "workspace database". Onshape/Slack "workspace" kept. Copy-lint now bans uppercase **MODEL** as a badge (Event Day, Chemistry, kiosk, Intel, AI keys, usage, pick-list notes, kickoff chip). Partner-fit notes say the score and "verify with pit notes", not a model id. Copy-lint now also bans **event-scoped** / "org- and event-scoped" (`(?:org\|event)[-\s]scoped`). Chemistry, Pick clock, Pick desk, Draft, Lineup, and the Strategy marketing page no longer say it. **Zero Setup steps headings remain in product UI.** Leftover `strategy-setup-steps` lists are gone from `apps/web/app`. **This revision:** Account API + student Connectors catalog no longer say Onshape OAuth / Email (Resend) / Select a workspace. Operator `/connectors` cards still name env vars. CAD Connections leftover OAuth is PR #26/#27 — not redone. |
| 11 | Copy sweep | in progress | copy-lint bans allowlist, PAYG / kill switch / BYOK, **BYO key**, leftover workspace-as-team-picker phrases, **workspace slug**, **Select a team** / **Select an active event**, leftover **Select event** / **Select an event**, **Select one and come back**, **Select the team**, **Select a page**, **Select a run**, and **Select a preset**. Empty titles, compute/API messages, Home header, HubOrgGate, and leftover **Choose team** CTAs now say **Choose your team**. Event titles say **Set active event**. Scan covers `.tsx`, `*-related.ts`, `compute-*`, API routes, and `dashboard-home-model.ts`. Bugbot / Chat / Usage / Team admin student copy now says "your own keys" or "the team's keys". `/workspace` page title is Your team. Admin create-team label is **Team slug**. Sign-in setup no longer says "workspace database". Onshape/Slack "workspace" kept. Copy-lint now bans uppercase **MODEL** as a badge (Event Day, Chemistry, kiosk, Intel, AI keys, usage, pick-list notes, kickoff chip). Partner-fit notes say the score and "verify with pit notes", not a model id. Copy-lint now also bans **event-scoped** / "org- and event-scoped" (`(?:org\|event)[-\s]scoped`). Chemistry, Pick clock, Pick desk, Draft, Lineup, and the Strategy marketing page no longer say it. **Zero Setup steps headings remain in product UI.** Leftover `strategy-setup-steps` lists are gone from `apps/web/app`. This revision: leftover **Pick a team** on the All-apps chip, Chat empty, onboarding GitHub hint, and related-strip details not in open PRs #2–#32 now say **Choose your team**. `leftover-pick-a-team.test.ts` **3 passed / 3**; copy-lint **3 passed / 3**; fixture Playwright `choose-your-team-leftover.spec.ts` **1 passed / 1** (1.9s) on :3310. |
| 11 | Copy sweep | in progress | copy-lint bans allowlist, PAYG / kill switch / BYOK, **BYO key**, leftover workspace-as-team-picker phrases, **workspace slug**, **Select a team** / **Select an active event**, leftover **Select event** / **Select an event**, **Select one and come back**, **Select the team**, **Select a page**, **Select a run**, and **Select a preset**. Empty titles, compute/API messages, Home header, HubOrgGate, and leftover **Choose team** CTAs now say **Choose your team**. Event titles say **Set active event**. Scan covers `.tsx`, `*-related.ts`, `compute-*`, API routes, and `dashboard-home-model.ts`. Bugbot / Chat / Usage / Team admin student copy now says "your own keys" or "the team's keys". `/workspace` page title is Your team. Admin create-team label is **Team slug**. Sign-in setup no longer says "workspace database". Onshape/Slack "workspace" kept. Copy-lint now bans uppercase **MODEL** as a badge (Event Day, Chemistry, kiosk, Intel, AI keys, usage, pick-list notes, kickoff chip). Partner-fit notes say the score and "verify with pit notes", not a model id. Copy-lint now also bans **event-scoped** / "org- and event-scoped" (`(?:org\|event)[-\s]scoped`). Chemistry, Pick clock, Pick desk, Draft, Lineup, and the Strategy marketing page no longer say it. **Zero Setup steps headings remain in product UI.** Leftover `strategy-setup-steps` lists are gone from `apps/web/app`. This revision: leftover **Join or pick a team** on Retro, Build Burndown, Cross-team scrim, Pair VS Code, Team admin, and Ask AI no-org copy not in open PRs #2–#33 now says **Choose your team**. Scout refuse copy says **choose which robot you are scouting**. Form preview photos say **stored with this team**. `leftover-join-or-pick.test.ts` **5 passed / 5**; copy-lint **3 passed / 3**; fixture Playwright `join-or-pick-leftover.spec.ts` **1 passed / 1** (29.6s) on :3310. |
| 11 | Copy sweep | in progress | copy-lint bans allowlist, PAYG / kill switch / BYOK, **BYO key**, leftover workspace-as-team-picker phrases, **workspace slug**, **Select a team** / **Select an active event**, leftover **Select event** / **Select an event**, **Select one and come back**, **Select the team**, **Select a page**, **Select a run**, and **Select a preset**. Empty titles, compute/API messages, Home header, HubOrgGate, and leftover **Choose team** CTAs now say **Choose your team**. Event titles say **Set active event**. Scan covers `.tsx`, `*-related.ts`, `compute-*`, API routes, and `dashboard-home-model.ts`. Bugbot / Chat / Usage / Team admin student copy now says "your own keys" or "the team's keys". `/workspace` page title is Your team. Admin create-team label is **Team slug**. Sign-in setup no longer says "workspace database". Onshape/Slack "workspace" kept. Copy-lint now bans uppercase **MODEL** as a badge (Event Day, Chemistry, kiosk, Intel, AI keys, usage, pick-list notes, kickoff chip). Partner-fit notes say the score and "verify with pit notes", not a model id. Copy-lint now also bans **event-scoped** / "org- and event-scoped" (`(?:org\|event)[-\s]scoped`). Chemistry, Pick clock, Pick desk, Draft, Lineup, and the Strategy marketing page no longer say it. **Zero Setup steps headings remain in product UI.** Leftover `strategy-setup-steps` lists are gone from `apps/web/app`. This revision: leftover Ask AI Memory / Chat limits / Usage copy not in open PRs #2–#34 no longer says **pick a team first** or **per org**. |
| 12 | Bugbot / agents | in progress | quotes required (existing); prompt now forbids push/PR; compact wired in HTTP adapter. Header prints model, files scanned, and wall-clock duration from `Date.now()` around the chunk loop (`formatScanElapsed`). No live scan-time number without a run. |
| 13 | CAD / assembly manual | in progress | **This revision:** Fusion GUI human-edit — paste a share link and **Edit in Fusion** from vault, CAD setup, and CAD connections. Missing Fusion config is **Needs setup**; 401/403 is **Choose your team**. Onshape viewport not rewritten. Assembly manual on a real Onshape document still unverified. |
| 13 | CAD / assembly manual | in progress | Ask AI plans `cad.vault` for heavy-part / fastener questions; session facts list vault titles and Onshape/Fusion links (no fabricated kg). Prompt pins "Cite vault documents by title". `/cad-vault` empty/setup keep one EmptyState primary; related in the header; next-actions on ready only. Vault cards lead with title + Open in Onshape/Fusion. Fusion share URLs paste beside Onshape. `/cad-learn` PageHeader + related; grader says how heavy / how hard it is to spin. Build CAD tab related strip is CAD vault · Learn CAD · Assembly manual. Last-snapshot wiring unchanged. Assembly manual on a real Onshape document still unverified. |
| 13 | CAD / assembly manual | in progress | Ask AI plans `cad.vault` for heavy-part / fastener questions; session facts list vault titles and Onshape links (no fabricated kg). Prompt pins "Cite vault documents by title". `/cad-vault`, `/cad-learn`, and `/cad-review-queue` restore the last IndexedDB copy (`feature: "cad-vault"` / `"cad-learn"` / `"cad-review-queue"`) when venue Wi-Fi drops; Vault and Review Queue key the cache by season. **CAD setup** (`/cad/setup`) is a student wizard (Onshape in the browser / Fusion on this computer; Ask a mentor; no CLI / env-var dump) with last IndexedDB copy (`feature: "cad-setup"`). Pair desktop student copy. Assembly manual on a real Onshape document still unverified. |
| 14 | Business funding models | in progress | `funding_model` column, onboarding radios, Business default tab + sponsor hide. Hub TabBar uses `filterSponsorTabs`. Overview/FundraisingGlance omit CRM CTAs when `sponsorsAllowed === false`. Funding-model default tab applies once on a bare `/business` visit, so a banned `?tab=sponsors` stays on Overview instead of bouncing to Money. Unit: `business-helpers.test.ts` hides sponsors/packages/partners. Playwright `business-funding-model.spec.ts` **3 passed / 3** on fixture :3310: school-funded no-sponsors lands on Money and hides Sponsors; `?tab=sponsors` returns to Overview; sponsored teams still offer Sponsors. **This revision (GUI, not closing Task 14):** student/mentor funding chrome — strip Sponsors · Grants · Budget, **Needs setup**, last snapshot, Working funds **—** until recorded, no Stripe/OAuth. Playwright `business-funding-gui.spec.ts` **1 passed / 1** (5.7s) on `:3422`. |
| 15 | Modular monolith | in progress | ESLint `no-restricted-imports` on dashboard/offline/drive/workspace/account/invite/connectors/onboarding/help/bugbot/business/cad-vault/packing/season-calendar vs scouting/messages/pit/hours. `FEATURE_DIRS` includes bugbot, business, and cad-vault. File-path matching covers `packing.ts` / `season-calendar.ts`. `funding-profile.ts` is shared onboarding types. `/training` and `/roles` are on Team › People. Scout meta jobs (`scout-accuracy` and seven siblings) and Task board (`/tasks`) stay registered but `inStrip: false` — not deleted. Work still opens `/tasks` from **Open build board**. Home board mutations live in `use-dashboard-board-ops.ts`; load/poll is `use-dashboard-home-state.ts`; the painted Home is `dashboard-home-view.tsx`; `dashboard-client.tsx` is **312** lines. Media load/mutate/shell is `media-client.tsx` (**444** lines). Account load/mutate/shell is `account-client.tsx` (**497** lines). Business load/mutate/shell is `business-client.tsx` (**404** lines). Team Calendar load/mutate/shell is `team-calendar-client.tsx` (**844** lines). Scouting load/mutate/shell is `scouting-client.tsx` (**717** lines); chrome, field widgets, quarantine, media downscale, and the ready workbench are sibling modules. Fixture `:3310` Playwright `scouting-shell.spec.ts` **1 passed / 1**. CAD load/mutate/shell is `cad-client.tsx` (**830** lines); session helpers, activity, tools, step pane, and the painted workbench are sibling modules. Agent Simple/Plan/Multitask stay a `role="group"` (`aria-label="Agent mode"`), not a nested TabBar. DEMO / blank returned ids stay rejected. Honesty contract concatenates `cad-client.tsx` + `cad-session.ts` + `cad-ready-view.tsx`. Fixture `:3310` Playwright `cad-shell.spec.ts` **1 passed / 1**. Inspection Copilot load/mutate/shell is `inspection-copilot-client.tsx` (**245** lines); chrome, summary tiles, checks list, and the log-check form are sibling modules. The form is **94** lines; catalog/payload live in `inspection-new-check-model.ts` (**575**); section panels are `inspection-form-sections.tsx` (**200**). Default 115 yardstick copy stays on the form (`default-target-honesty-contract.test.ts`). Setup next actions derive from `inspectionSetupSteps` via `setupActionsFrom`; Batteries / FMEA / Weigh-in stay in the related strip. Remaining >1k TSX clients: **none**. App chrome is `app-shell.tsx` (**609**); helpers live in `app-shell-model.ts`. `tsx-line-limit.test.ts` fails if any `apps/web/**/*.tsx` exceeds 1000 lines. Files load/mutate/shell is `files-client.tsx` (**481** lines); rails stay a `navigation` (`aria-label="File spaces"`), not a nested TabBar. Who-can-see-what copy stays at the top of the page. Chat load/mutate/shell is `messages-client.tsx` (**812** lines); labels/helpers live in `messages-model.ts` and the painted workbench is `messages-ready-view.tsx`. Channels stay sidebar buttons, not a nested TabBar. Two-adult / supervision banner copy is unchanged. Fixture `:3310` Playwright `messages-shell.spec.ts` **1 passed / 1**. Code Coach load/mutate/shell is `code-client.tsx` (**874** lines); types live in `code-model.ts`; chrome is `code-ready-view.tsx` (**184**); coach workbench is `code-coach-panel.tsx` (**210**); Bugbot is `code-bugbot-panel.tsx` (**680**). Bugbot billing stays a `role="group"` (`aria-label="Bugbot billing mode"`), not a nested TabBar. Findings stay empty until a review matches real rules. Fixture `:3310` Playwright `code-shell.spec.ts` **1 passed / 1**. Form builder load/mutate/shell is `forms-client.tsx` (**717** lines); chrome is `forms-chrome.tsx`; option/settings editors and preview are sibling modules. Match / Pit stay a ToolStrip (`aria-label="Form type"`), not a nested TabBar. Pit starter still has no claimed-scoring roles (`forms-model.test.ts`). Do not add `media` to `FEATURE_DIRS` without uncoupling `lib/media` → scouting. Kickoff load/mutate/shell is `kickoff-client.tsx` (**257** lines); intelligence, scoring, priorities, and rules are sibling modules. Hub TabBar stays Kickoff · CAD · Code · Robot. AI keys load/mutate/shell is `ai-keys-client.tsx` (**485** lines); chrome, provider cards, and the painted workbench are sibling modules. Mine / Team stay sections, not a nested TabBar. Logistics load/mutate/shell is `logistics-client.tsx` (**286** lines); chrome, day-of, mentor planning, and trips are sibling modules. Trip switcher is a ToolStrip (`aria-label="Trips"`), not a nested TabBar. Team admin load/mutate/shell is `team-admin-client.tsx` (**743** lines); chrome, access requests, invites, GitHub, and leftover providers are sibling modules. Onboarding load/mutate/shell is `onboarding-client.tsx` (**513** lines); chrome, funding fields, step forms, pending, and landing are sibling modules. No nested TabBar. Inventory load/mutate/shell is `inventory-client.tsx` (**267** lines); chrome, item rows, locations, BOM, and stock toolbar are sibling modules. Stock / Locations / BOM stay a ToolStrip (`aria-label="Inventory sections"`), not a nested TabBar. Command load/mutate/shell is `command-client.tsx` (**326** lines); chrome, event picker, and the painted workbench are sibling modules. No nested TabBar. Sign-in load/mutate/shell is `sign-in-client.tsx` (**653** lines); chrome, identity, code, and already-signed-in panels are sibling modules. No nested TabBar. Print Farm load/mutate/shell is `print-farm-client.tsx` (**121** lines); chrome, queue, printers, filament, and recently finished are sibling modules. No nested TabBar. Batteries load/mutate/shell is `batteries-client.tsx` (**250** lines); chrome, fleet, forms, and assign row are sibling modules. No nested TabBar. Remaining >1k TSX clients: **none**. App chrome is `app-shell.tsx` (**609**). Inspection Copilot form split: `inspection-new-check-form.tsx` is **94** lines. Orders load/mutate/shell is `orders-client.tsx` (**994** lines). Attendance load/mutate/shell is `attendance-client.tsx` (**969** lines). Kickoff load/mutate/shell is `kickoff-client.tsx` (**313** lines). |
| 16 | Supabase readiness | in progress | preflight dual URLs + rehearsal script. **Not connected.** |
| 17 | Performance / cost | in progress | Home, Event day, Pit, Display, assembly-manual, exports, storage, hours kiosk, and the outbox badge skip while the tab is hidden. Student Home first paint no longer statically imports ops/extra widgets or Edit Home chrome (`home-split.test.ts`). Product chrome CSS is off marketing (see Task 2). Production `next build` this revision: compiled **19.3s**, TypeScript **21.2s**, **695 pages**, no warnings in the log. Route first-load uncompressed JS (`.next/diagnostics/route-bundle-stats.json`): Home `/dashboard` **674 KB** (689,665 B; was 797), Scouting **746 KB**, Strategy **657 KB**, Pit **590 KB**, `/workspace` **561 KB**, landing `/` **582 KB**. Next 16 Turbopack does not print a First Load JS column. **This revision:** `npm run test:browser` shards 4 ways and evicts `.next-pw` between processes so a 100+ spec walk does not OOM Next. Named specs stay unsharded. Browser CI timeout is 45 minutes. Full-suite proof is still pending — do not treat production as proven. |
| 18 | Final verification | pending | GHA on Saturday/pit `7280c6d5` is the latest push before this leftover-list conversion. Hours `e6f3daee` (`34501107216`) is the last full Playwright green: **112 passed, 2 flaky**, **12 skipped**. Draft PRs no longer run Playwright (private-repo minutes). Do not treat production as proven. `CRON_SECRET` still owner-must. |
