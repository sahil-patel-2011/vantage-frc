-- SUPERSEDED — intentionally a no-op.
-- Driver practice tables (driver_sessions, driver_cycles) are created by
-- 0103_driver_practice.sql, which shipped the integrated Practice Planner
-- (session goals + attendance/build-task links). This file is kept only to
-- preserve the numbered migration sequence for databases that recorded it.
SELECT 1;
