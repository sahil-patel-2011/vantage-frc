-- Petals volunteer swarm last-resort path: usage logged at cost 0, no hosted debit.
-- Prompts leave Vantage; the swarm is not a quota grant and is not unlimited.
ALTER TYPE key_source ADD VALUE IF NOT EXISTS 'public_swarm';
